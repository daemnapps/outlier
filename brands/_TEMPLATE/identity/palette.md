# <Brand> — colour

Measured off the brand's own product photography (`../products/**/images/`),
<date>. **Sampled, not chosen** — these are the colours the product actually is.
Rows are written by `brand-identity/measure.py palette`; the **Name**
and **What it is** columns are a person's — the tool never coins a name.

| Name | Hex | What it is | Sampled from |
|---|---|---|---|
| open | `#000000` | <the part of the product this colour IS — the tube body, the cap, the label type> | <file> |
| open | `#000000` | <…> | <file> |
| **Ground** | `#FFFFFF` | <what the swiped formats sit on> | <the swipe folder, or open> |
| **Type** | `#111111` | <headline and body copy on that ground> | <…> |

## Lanes

<If this brand runs more than one visual lane that must never blend, say which
lane THIS file is, and name the other lane's file. Otherwise: one lane.>
