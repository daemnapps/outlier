{"built": "2026-09-26", "items": [{"doc": "PRIZM LABS — the tools", "section": "PRIZM LABS — the tools", "tool": "", "path": "README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/README.md", "text": "The generative marketing machine. This is Damon's brain for the business: the\nstrategy, the brands and the calls stay with him. What's here are the tools our\ncreative marketers use to turn those calls into finished work.\n\nTake a video that already worked — a competitor's ad, an organic post,\nanything — keep its structure, and rebuild it for one of our brands. You get a\nscript, a shot list, and a prompt for every frame.\n\n**One way to use it:** clone this repo into Higgsfield Supercomputer (or Claude\nCode) and tell it what you want. No kits, no copies.\n\n**→ daemn.co** — see it working, with the same ad rebuilt\nthree different ways.\n\n---"}, {"doc": "PRIZM LABS — the tools", "section": "Start here", "tool": "", "path": "README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/README.md#start-here", "text": "Everything is plain text. Clone it and it is all there; say \"pull for\nupdates\" and it stays current.\n\nWhat it is — Read it.\n**01** — **The brand folder** — every tool reads it, so a brand is set up once and never explained again. — `brands/_TEMPLATE/`."}, {"doc": "PRIZM LABS — the tools", "section": "Take something apart", "tool": "", "path": "README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/README.md#take-something-apart", "text": "Something already worked. These pull it down to the structure underneath, then\nput our brand where theirs was.\n\nWhat it is — Read it.\n**02** — **Video teardown** — any video in, your brief out. Written for someone who has never done this. — `tools/02-video-teardown/`.\n**03** — **Organic swipe pack** — 48 formats pulled apart from 377 posts that worked, some past a hundred million views. Rebuilt 2026-09-22. — `tools/03-organic-swipe-pack/`.\n**04** — **Paid ad swipe pack** — 6,269 live competitor ads across nine markets, reduced to the ten angle shapes that keep working. Rebuilt 2026-09-22. — `tools/04-paid-ad-swipe-pack/`.\n**06** — **Image teardown** — the same chain pointed at a single frame. — `tools/06-image-teardown/`.\n**07** — **Copy teardown** — for a piece of writing rather than a video. — `tools/07-copy-teardown/`.\n**09** — **Page teardown** — a landing page, same treatment. — `tools/09-page-teardown/`."}, {"doc": "PRIZM LABS — the tools", "section": "Make something", "tool": "", "path": "README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/README.md#make-something", "text": "What it is — Read it.\n**05** — **AI video production** — the brief becomes scenes, motion and finished shots. The chain that builds a brief from scratch. — `tools/05-ai-video-production/`.\n**10** — **Image production** — stage two for statics, the twin of the video line. — `tools/10-image-production/`.\n**13** — **Pages** — swipe, construct, inject, base, then one variation per sub-avatar. No page gets written freehand. — `tools/13-pages/`.\n**14** — **Copywriter** — the copy chain. — `tools/14-copywriter/`."}, {"doc": "PRIZM LABS — the tools", "section": "Plan it and finish it", "tool": "", "path": "README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/README.md#plan-it-and-finish-it", "text": "What it is — Read it.\n**15** — **Outlier brief** — the second door. Start from an idea instead of someone else's video. — `tools/15-outlier-brief/`.\n**16** — **Video edit** — cut sheets, in plain words rather than a timeline. — `tools/16-video-edit/`.\n**20** — **Asset index** — a folder of footage becomes records you can search. — `tools/20-asset-index/`.\n**21** — **Editor onboarding** — the front door for creative marketers: the walkthrough, the SOP, the two prompts they paste into Higgsfield, and the brief queue. — `tools/21-editor-onboarding/` · the page.\n**22** — **Swipe library** — one front door to everything swiped: the formats, the torn-down videos, one feed per customer type, every competitor's live ads. Public half on the site, the rest on Drive. — `tools/22-swipe-library/` · the page."}, {"doc": "PRIZM LABS — the tools", "section": "The shared parts", "tool": "", "path": "README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/README.md#the-shared-parts", "text": "The tools import these. You do not run them yourself, but nothing runs without\nthem: the chain runner, the element system, the marketing doctrine, the run\nlayout, the name maps, the research gatherer and the quality checks.\n`components/`\n\n**Do 01 first.** Nothing else works well on an empty brand folder — the tools\nare built to say *\"I don't know this\"* rather than invent an answer, so an\nempty file shows up as a question instead of a fake.\n\nThen read\n`tools/02-video-teardown/HOW-TO-RUN-IT.md`\n— the whole process in seven numbered steps.\n\n---"}, {"doc": "PRIZM LABS — the tools", "section": "What you need", "tool": "", "path": "README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/README.md#what-you-need", "text": "1. **Higgsfield Supercomputer** with this repo cloned into it — where the\n   video gets watched, the chain runs and your scenes get made.\n   (affiliate link — costs you nothing\n   extra.) Claude Code with the repo cloned works the same way.\n2. **Google Drive** — where finished briefs and delivered work live, so the\n   people who make the ads can reach them. `tools/21-editor-onboarding`.\n\nNothing gets installed. Nothing runs on your computer.\n\nThe exact models and settings are in\n`WHICH-MODELS.md` — don't leave it\non Auto.\n\n---"}, {"doc": "PRIZM LABS — the tools", "section": "The two rules", "tool": "", "path": "README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/README.md#the-two-rules", "text": "**Copy the structure, not the content.** The shape is what earned the views;\nthe product is what changes. A scalp treatment and a countertop demo can be\nthe same format — that's the whole point.\n\n**Nothing invented.** If the brand folder doesn't say what the product looks\nlike or how the customer talks, the tools say they don't know rather than\nguessing. That's them working correctly. Ask Damon for that part.\n\n---"}, {"doc": "PRIZM LABS — the tools", "section": "What it won't do", "tool": "", "path": "README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/README.md#what-it-wont-do", "text": "Publish anything. Spend anything on ads. Invent a customer quote. Make a\nmedical claim. Write in a voice it hasn't been given evidence for.\n\nThose are hard stops in the prompts, not guidelines.\n\n---"}, {"doc": "PRIZM LABS — the tools", "section": "What you need", "tool": "", "path": "README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/README.md#what-you-need", "text": "A Higgsfield account and Claude. Sign up to Higgsfield through the link on the\nsite.\n\n---"}, {"doc": "PRIZM LABS — the tools", "section": "Also in here", "tool": "", "path": "README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/README.md#also-in-here", "text": "`docs/` — The site itself — daemn.co is served straight from this folder..\n`SECURITY.md` — What's exposed, what isn't, and the commit guard that keeps keys out..\n`receiver/` — The small workers behind the site's Ask box and forms..\nLicense: MIT (see `LICENSE`)."}, {"doc": "Asking a question", "section": "Asking a question", "tool": "", "path": "ASK.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/ASK.md", "text": "There is no group chat, no support email and no DMs.\n\nUse the question form on **daemn.co** — it writes the\nmessage for you and opens it in your own email. Say what you were trying to\ndo and what actually happened; that is usually enough.\n\nOr open an issue on this repo, which is public and searchable, so the answer\nhelps whoever hits it next.\n\n**Never paste an API key, a password or a customer's details** into either.\nIf you already have, rotate the key at the provider — deleting the message\ndoes not un-send it."}, {"doc": "How to run a teardown — one way", "section": "How to run a teardown — one way", "tool": "02-video-teardown", "path": "tools/02-video-teardown/HOW-TO-RUN-IT.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/HOW-TO-RUN-IT.md", "text": "You work inside **Higgsfield Supercomputer** with this repo cloned into it.\n(Claude Code with the repo cloned works the same way.) You do not paste files\ninto a chat, you do not copy anything into a Drive, you do not install\nanything. You say what you want and it runs the chain.\n\n---"}, {"doc": "How to run a teardown — one way", "section": "Once — clone the repo (two minutes)", "tool": "02-video-teardown", "path": "tools/02-video-teardown/HOW-TO-RUN-IT.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/HOW-TO-RUN-IT.md#once--clone-the-repo-two-minutes", "text": "1. In Supercomputer: **Connectors → Explore** → connect **GitHub** and\n   **Google Drive**.\n2. New chat → paste `https://github.com/daemnapps/prizm-labs` and say\n   **\"clone this\"**.\n3. Any time after: **\"pull my GitHub repository and look for updates.\"**\n   That is the whole update process."}, {"doc": "How to run a teardown — one way", "section": "Once per brand — fill in the brand folder (one afternoon)", "tool": "02-video-teardown", "path": "tools/02-video-teardown/HOW-TO-RUN-IT.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/HOW-TO-RUN-IT.md#once-per-brand--fill-in-the-brand-folder-one-afternoon", "text": "Say: **\"Copy brands/_TEMPLATE to brands/<my brand> and walk me through\nfilling it in.\"** It asks you the questions in the template's README, in\norder, and writes your answers into the files. Every tool reads this folder,\nso you explain your brand once.\n\n**The one people skip, and regret:** the product document. Describe the\nproduct the way a camera sees it — the container, the colour, the finish,\nevery word printed on the label. Skip it and every image you generate shows\na product that is not yours."}, {"doc": "How to run a teardown — one way", "section": "Every video — the teardown", "tool": "02-video-teardown", "path": "tools/02-video-teardown/HOW-TO-RUN-IT.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/HOW-TO-RUN-IT.md#every-video--the-teardown", "text": "Say: **\"Tear down this video for <my brand>\"** and give it the video — a\nlink, or a file you drop in.\n\nIt runs `the-chain/` in order and stops to show you at each step:\n\n1. **Watch** — it watches the video and writes the scene record: what is on\n   screen, what is said, what is printed, when.\n2. **Tear down** — the structure underneath: the beats, the hook, the proof,\n   the close. What earned the views, separated from what was sold.\n3. **Spec** — the video described as something rebuildable, with the\n   brand-specific parts pulled out and left blank.\n4. **Inject** — your brand folder goes into the blanks: your customer, your\n   words, your product where theirs was. Anything the folder does not say,\n   it flags as *not known* rather than inventing.\n5. **Hooks and brief** — the hook options, then the brief: the scenes, what\n   is said, what the person wears, and a prompt for every frame.\n\nThe brief is written to `brands/<my brand>/briefs/` — and, when Drive is\nconnected, to the brand's `briefs/` folder on Drive, where it shows on the\nqueue for whoever makes the video (`tools/21-editor-onboarding`)."}, {"doc": "How to run a teardown — one way", "section": "Then — the scenes", "tool": "02-video-teardown", "path": "tools/02-video-teardown/HOW-TO-RUN-IT.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/HOW-TO-RUN-IT.md#then--the-scenes", "text": "Say: **\"Make the scenes for this brief.\"** `tools/05-ai-video-production`\ntakes it from here: stills, motion, voice, the editor pack. The models and\nsettings it uses are in `WHICH-MODELS.md` — it does not leave anything on\nAuto.\n\nTwo things decide whether it looks real, and the chain enforces both:\n\n- **Every blank is filled from the brand folder.** `{PRODUCT}`, `{SUBJECT}`\n  are questions, not decoration. Nothing is guessed.\n- **Every person and product carries its reference in every frame.** That is\n  how the same human appears in every shot."}, {"doc": "How to run a teardown — one way", "section": "When it goes wrong", "tool": "02-video-teardown", "path": "tools/02-video-teardown/HOW-TO-RUN-IT.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/HOW-TO-RUN-IT.md#when-it-goes-wrong", "text": "**\"It made up a product that isn't mine.\"** The product document is missing\nor thin. Describe the packaging properly, then say \"regenerate\".\n\n**\"The script doesn't sound like my customer.\"** The language file needs\nmore real sentences — reviews, comments, tickets, copied exactly.\n\n**\"It says it doesn't know something.\"** Correct behaviour. It is telling\nyou which part of the brand folder is empty. Fill that part in.\n\n**\"The scenes don't match each other.\"** A reference was missing on a frame.\nSay which scene; it regenerates from the cast sheet."}, {"doc": "How to run a teardown — one way", "section": "What good looks like", "tool": "02-video-teardown", "path": "tools/02-video-teardown/HOW-TO-RUN-IT.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/HOW-TO-RUN-IT.md#what-good-looks-like", "text": "Hand the finished brief to someone who has never seen your brand. If they\ncan make the video without asking you a single question, it worked. If they\nhave to interpret anything, go back and make that part specific."}, {"doc": "The video teardown machine", "section": "The video teardown machine", "tool": "02-video-teardown", "path": "tools/02-video-teardown/MACHINE-README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/MACHINE-README.md", "text": "**Two lines, and the one thing they share.**\n\n \n\nReading a swiped video is one job. What you do with it is two.\n\nTearing a source down — what is on screen, who it is for, the reusable machine\nunderneath — does not care who performs the result. Everything after that does,\nand the two jobs have almost nothing in common: one ends with a person holding\na phone in their own bathroom, the other with nobody on set at all.\n\n \n\n**The front half stays shared on purpose.** Two teardowns of the same source\nwould drift, and then the two lines would be arguing about what the video\nactually was.\n\n**What differs downstream**, and why each one matters:\n\nCreator — AI.\nStage 6 (frames) — runs — a person filming needs to see the shot — skipped.\nStage 7b (one-sheet) — runs — it is the page she reads — skipped; the storyboard is what this line reads.\nBrief shape — a shot list with reference frames — scenes typed A / B / C.\nProduction instruction — what to film, with what they own — CUT owned footage, GENERATE, or COMPOSITE.\nCamera equipment — theirs, and fine — they are filming — never named; it ends up in the shot.\nProps — what they can buy — named products shown with their labels.\nRuled 2026-09-11, after a run where \"the same workflows are activating for\nboth\". Until then it was one chain with a `--route` flag, and the flag was a\nnoun the stages repeated back rather than a fork they obeyed: a brief written\nfor a generator went out saying \"film this\", and the stage that writes the page\na creator reads ran on a line with no creator.\n\n`line.py` is a door, not a new machine. Each line calls the sa"}, {"doc": "The video teardown machine", "section": "The video teardown machine", "tool": "02-video-teardown", "path": "tools/02-video-teardown/MACHINE-README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/MACHINE-README.md", "text": "me stages with\nthe right range and route, so nothing another brand's runs depend on moved.\n\n---\n\nA swiped video goes in. A shot-ready creator brief comes out — script, hooks,\nwardrobe, and a picture on every scene."}, {"doc": "The video teardown machine", "section": "What it actually does", "tool": "02-video-teardown", "path": "tools/02-video-teardown/MACHINE-README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/MACHINE-README.md#what-it-actually-does", "text": "1. **Reads the video.** Watches it (Gemini), decides what kind of video it\n   is, and writes down every scene shot by shot — what's on screen, what's\n   said, the wardrobe.\n2. **Makes it ours.** Substitutes our brand into the format that already\n   works, without changing anything that's *worn* or *revealed on camera* —\n   only what's held up or referred to.\n3. **Runs the DR loop.** Writes the opening hooks straight off the source,\n   then decides where our product enters, then (if the source needs it)\n   builds out the middle, then closes it, then checks the whole thing\n   against the source and the rules.\n4. **Writes the brief.** One document — the concept, the shots, the wardrobe\n   on every scene, a picture for every scene. Nothing the maker doesn't need.\n5. **Makes the pictures.** One generated reference image per scene, seeded\n   so the room and the person stay consistent shot to shot.\n6. **Puts it in the Drive.** The finished brief becomes a real Google Doc\n   in the run's own folder — editable, pictures in it, beside the source\n   video. Re-running the same brief updates that Doc rather than leaving\n   a second copy, so a link you have sent stays the right one."}, {"doc": "The video teardown machine", "section": "The one decision that changes everything: the lane", "tool": "02-video-teardown", "path": "tools/02-video-teardown/MACHINE-README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/MACHINE-README.md#the-one-decision-that-changes-everything-the-lane", "text": "The very first stage (**Triage**) looks at the source and calls one of four\nlanes. Everything downstream reads that call:\n\nLane — What it means — What changes.\n**ALREADY AN AD** — The source already has a hook, a proof beat, an offer — **Expansion is skipped.** Nothing gets added — a proof run added zero beats to an ad and it still worked. The close builds straight off the injection..\n**ORGANIC** — No pitch anywhere in the source — **Full chain runs.** Nothing commercial exists yet — the hooks, the placement, the expansion, all of it gets built from nothing..\n**STATIC / SEQUENTIAL** — Not a video — a still or a carousel — This chain stops. Belongs in the image lane instead.."}, {"doc": "The video teardown machine", "section": "The tracker is the list, not a copy of it (2026-08-27)", "tool": "02-video-teardown", "path": "tools/02-video-teardown/MACHINE-README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/MACHINE-README.md#the-tracker-is-the-list-not-a-copy-of-it-2026-08-27", "text": "`tracker.py` reads the brand's own creator sheet through the service account.\n`--check` says what the sheet has that the machine doesn't and the other way\nround; `--sync` rewrites `creators.json` from it. The sheet id lives in\n`brands/<brand>/tracker.json`, never here.\n\nThis exists because a hand-typed copy silently went stale: a signed partner sat\non the sheet marked *waiting for brief* and the machine had never heard of her,\nand two more appeared during a single afternoon. Run `--check` before a pull;\na row that contradicts itself gets said out loud rather than quietly doing\nnothing."}, {"doc": "The video teardown machine", "section": "Views come first (Damon, 2026-08-27)", "tool": "02-video-teardown", "path": "tools/02-video-teardown/MACHINE-README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/MACHINE-README.md#views-come-first-damon-2026-08-27", "text": "Any list of creators or posts is sorted by view count, biggest first, before\nanything else is applied. Fit, usability and how many videos someone still\nowes are filters laid over that order — never a reordering. Reach is the thing\nbeing bought; a list re-sorted on someone's judgement hides it."}, {"doc": "The video teardown machine", "section": "What counts as a source (Damon, 2026-08-27)", "tool": "02-video-teardown", "path": "tools/02-video-teardown/MACHINE-README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/MACHINE-README.md#what-counts-as-a-source-damon-2026-08-27", "text": "Only **video ads** get torn down: the creator performing, on screen. A quote\ncard over stock footage, a still, a carousel, or b-roll whose whole content is\nthe caption over it is not a source, no matter how well it scored.\n\nTriage will not stop these — its STATIC / SEQUENTIAL lane tests the *file*, so\nanything that arrives as a real mp4 with motion and sound passes as ORGANIC\nand the full chain runs. And the sourcing rank will not stop them either: that\nis `views + likes + comments` with no eyes on it. **The check happens before\nthe run, by whoever picks the post — look at the thumbnail.** A quote card\nranked first on a creator's page on 2026-08-26 and took thirteen stages to\nproduce a brief with nobody in it.\n\nA second decision happens after placement: it asks not just *\"where does our\nproduct go\"* but *\"where does the viewer cross into recognising their own\nproblem\"* — because on organic sources, that crossing is usually a **problem\nnamed on screen**, with no product in frame yet, not a product shot. Only if\nneither exists at all does a brief become a *hook asset* (an opening that\nearns a click and hands off, never asked to sell)."}, {"doc": "The video teardown machine", "section": "The chain, in the order it actually runs", "tool": "02-video-teardown", "path": "tools/02-video-teardown/MACHINE-README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/MACHINE-README.md#the-chain-in-the-order-it-actually-runs", "text": "Labels below match run order — not the order they sit in the shared team\nconfig (`chain_config.json`), which uses its own lettering for its own\nreasons. See `prompts/README.md` for the mapping between the two if you ever\nneed to trace a stage back to the shared file.\n\n# — Stage — Reads or writes.\n0 — Triage — reads — decides the lane.\n1 — Teardown — reads — the objective record, scene by scene.\n2 — Spec — reads — abstracted into a brand-free construct.\n3 — Injection — writes — our brand substituted in, never a worn/revealed object.\n4a — Read — reads — lane, opening beat, awareness entry (before the hooks exist).\n4b — Hooks — writes — control + five variations, all filmed, none picked here.\n4c — Placement — writes — where the product enters, seeing the hooks first.\n4d — Expansion — writes — gated moves, skipped entirely on an already-ad.\n4e — Close — writes — the objection, the offer.\n4f — Audit — reads — seven checks against the source, reports only.\n5 — Brief — writes — the one document the maker receives.\n6 — Frames — writes — a generated reference picture per scene.\n7 — Final brief — assembles — pictures placed into the brief, nothing else touched.\n8 — Document — lays the brief out for Google Docs, pictures in place.\n9 — Doc — creates it in the Drive folder, editable, link on the run."}, {"doc": "The video teardown machine", "section": "Where the prompts live", "tool": "02-video-teardown", "path": "tools/02-video-teardown/MACHINE-README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/MACHINE-README.md#where-the-prompts-live", "text": "`prompts/` — one folder per stage. **Each folder shows exactly one current\nprompt.** Every earlier version is in that folder's own `archive/`. If two\nprompts sit at the top of the same folder, something is wrong — the machine\nalways resolves to the highest version number, so an old file left visible is\nconfusing, not dangerous, but it shouldn't happen.\n\nThe machine picks up a new version automatically — drop a file in, bump the\nnumber, nothing else to change or point anywhere."}, {"doc": "The video teardown machine", "section": "Where this lives", "tool": "02-video-teardown", "path": "tools/02-video-teardown/MACHINE-README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/MACHINE-README.md#where-this-lives", "text": "> **PRIMARY, SETTLED 2026-08-25 (Dayu).** Nothing was ahead in\n> `~/devel/daemn` when this moved — confirmed by Dayu, not an open question.\n> Work here directly; the sandbox mirror is retired for this build.\n>\n> **GRADUATED 2026-08-25 (Dayu ruling): THIS folder is primary.** The line\n> below recorded the pre-graduation state and is kept for history. From today\n> Damon works here directly — same prompts, same sessions, same board, new\n> path — and the `~/devel/daemn` mirror stops. Damon's correction pass on\n> this note outranks it.\n\nSource of truth *(pre-graduation)*: `~/devel/daemn/video-teardown-machine/` (Damon's private\nrepo). Mirrored into `ai-workspace/video-teardown-machine/` so the\nteam can see it — that copy is a bridge, not a fork. Changes get made in\ndaemn first, then re-bridged the same way; the ai-workspace copy is never\nedited directly."}, {"doc": "The video teardown machine", "section": "Running it", "tool": "02-video-teardown", "path": "tools/02-video-teardown/MACHINE-README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/MACHINE-README.md#running-it", "text": "The board (`board.html`, served on your Mac) is the working surface — every\nprompt beside its output, version tabs per video, a self-check that runs\nevery 10 minutes and flags anything it can't fix on its own."}, {"doc": "Video Teardown", "section": "Video Teardown", "tool": "02-video-teardown", "path": "tools/02-video-teardown/START-HERE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/START-HERE.md", "text": "Take any video that already worked — a competitor's ad, an organic post, a\ncreator video someone sent you — and get back a finished brief for **your**\nbrand: the scenes, what is said, what people wear, the hooks, and a prompt\nfor every shot you need to make.\n\nNothing gets installed. Nothing runs on your computer."}, {"doc": "Video Teardown", "section": "What you need", "tool": "02-video-teardown", "path": "tools/02-video-teardown/START-HERE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/START-HERE.md#what-you-need", "text": "1. **Higgsfield Supercomputer** with this repo cloned into it — where the\n   video gets watched, the chain runs, and your scenes get made. Claude Code\n   with the repo cloned works the same way.\n2. **Your brand folder** — `brands/_TEMPLATE/`, copied to `brands/<your\n   brand>/` and filled in once. Say \"walk me through it\" and it does."}, {"doc": "Video Teardown", "section": "Start here", "tool": "02-video-teardown", "path": "tools/02-video-teardown/START-HERE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/START-HERE.md#start-here", "text": "**Read `HOW-TO-RUN-IT.md`.** Clone once, fill the brand folder once, then\n\"tear down this video for <brand>\". Everything else in this folder is what\nthe chain runs."}, {"doc": "Video Teardown", "section": "What is in this folder", "tool": "02-video-teardown", "path": "tools/02-video-teardown/START-HERE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/START-HERE.md#what-is-in-this-folder", "text": "- `HOW-TO-RUN-IT.md` — the one way to run it. Start here.\n- `WHICH-MODELS.md` — the exact models and settings to use. Do not leave it\n  on Auto; this names what actually works and what to do when a shot looks\n  wrong.\n- `the-chain/` — the prompts the chain runs, one file per step, numbered in\n  the order they run. Read them to see how a brief is made; edit them to\n  change how every brief after is made.\n- `reference--the-automated-version.md` — ignore this one unless you have a\n  developer. It describes a version that runs the whole thing unattended."}, {"doc": "Video Teardown", "section": "The two rules", "tool": "02-video-teardown", "path": "tools/02-video-teardown/START-HERE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/START-HERE.md#the-two-rules", "text": "**Rebuild the structure, not the content.** The shape is what earned the\nviews. Your product is what changes. A scalp treatment and a countertop demo\ncan be the same format — that is the point.\n\n**Nothing invented.** If your brand folder does not say what your product\nlooks like or what your customer sounds like, the tools will tell you they do\nnot know rather than make something up. That is them working correctly. Go\nfill in that part.\n\n---\n\n*Prizm Labs — daemn.co. Free, MIT licensed.*"}, {"doc": "Which models to use, and the settings that work", "section": "Which models to use, and the settings that work", "tool": "02-video-teardown", "path": "tools/02-video-teardown/WHICH-MODELS.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/WHICH-MODELS.md", "text": "Do not leave this on Auto. These are the exact models and settings behind the\nvideos this system has actually produced — not a guess at what might work.\n\nEverything below runs inside **Higgsfield Supercomputer**.\n\n---"}, {"doc": "Which models to use, and the settings that work", "section": "Reading the video — Gemini", "tool": "02-video-teardown", "path": "tools/02-video-teardown/WHICH-MODELS.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/WHICH-MODELS.md#reading-the-video--gemini", "text": "When you upload a swipe video and ask it to describe every scene, that is a\nGemini model doing the watching.\n\nUse — Model.\nFirst choice — `gemini-3-flash-preview`.\nIf it is busy — `gemini-3.5-flash`.\nFor a long or complicated video — `gemini-3.1-pro-preview`.\nFlash is the right default — it is fast and it does not miss scenes. Only\nreach for Pro when a video is long, dense, or full of overlapping dialogue.\n\n---"}, {"doc": "Which models to use, and the settings that work", "section": "Making the frames — Soul v2", "tool": "02-video-teardown", "path": "tools/02-video-teardown/WHICH-MODELS.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/WHICH-MODELS.md#making-the-frames--soul-v2", "text": "Every still starts here: `text2image_soul_v2`.\n\nSetting — Value — Why.\nSize — 1536 × 2048 — portrait, enough resolution to crop.\nStyle — General — anything else fights your prompt.\n`enhance_prompt` — **off** — on, it rewrites you and drifts off the brief.\nSeed — fixed, one per character — the same number gives you the same face.\n**The character-sheet trick.** Before you generate any scene, make one image\nper person as a split-screen sheet: full body on the left, chest-up close-up\non the right, both the same character. Then feed that sheet in as the\nreference for every scene they appear in. That is what stops them being a\ndifferent human in every shot — it is the single biggest difference between\nwork that looks made and work that looks broken.\n\n---"}, {"doc": "Which models to use, and the settings that work", "section": "Making the shots — Seedance 2.0", "tool": "02-video-teardown", "path": "tools/02-video-teardown/WHICH-MODELS.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/WHICH-MODELS.md#making-the-shots--seedance-20", "text": "Frames become video with `seedance_2_0`.\n\nSetting — Value.\nRatio / size — 9:16, 1080 × 1920.\n`mode` — `std`.\n`generate_audio` — `true` — it invents its own ambience and dialogue.\n`multi_shots` — `false` — one shot per generation, always.\n`speedramp` — `auto`.\nLength — **4s per scene beat**, 6s for silent b-roll.\nLonger single takes (8–15s) work when one continuous moment carries the beat,\nbut cutting 4-second shots is what holds attention.\n\nFeed each generation the character sheet plus a full cinematic description of\nthe moment — not a shot label. \"Cinematic drama, golden morning market stall.\nThe elderly woman from the reference image reaches across the counter…\" beats\n\"scene 3, woman at stall\" every time.\n\n---"}, {"doc": "Which models to use, and the settings that work", "section": "Voice, if you need it — Seed Audio", "tool": "02-video-teardown", "path": "tools/02-video-teardown/WHICH-MODELS.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/WHICH-MODELS.md#voice-if-you-need-it--seed-audio", "text": "`seed_audio`, wav, 24000 Hz.\n\nSetting — Value.\n`speech_rate` — −5 to −15 (slower reads as more serious).\n`loudness_rate` · `pitch_rate` — 0.\n`expression_intensity` — 5.\nNo voice presets. Describe the person in words, and clone from an uploaded\nreference file when you want a specific voice.\n\nNote: if Seedance is already generating audio for a shot, you do not need a\nseparate voice track for it. Use Seed Audio for narration over the cut.\n\n---"}, {"doc": "Which models to use, and the settings that work", "section": "The order you do it in", "tool": "02-video-teardown", "path": "tools/02-video-teardown/WHICH-MODELS.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/WHICH-MODELS.md#the-order-you-do-it-in", "text": "1. Character sheets — one per person, fixed seed. **Do this first, always.**\n2. Frames — the brief's image prompt plus the character sheet as reference.\n3. Shots — Seedance, 4 seconds, one shot per generation.\n4. Voice — only if the cut needs narration."}, {"doc": "Which models to use, and the settings that work", "section": "When something looks wrong", "tool": "02-video-teardown", "path": "tools/02-video-teardown/WHICH-MODELS.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/02-video-teardown/WHICH-MODELS.md#when-something-looks-wrong", "text": "**A different face every shot** — you skipped the character sheet, or changed\nthe seed. Go back to step 1.\n\n**It ignored half your prompt** — `enhance_prompt` is on. Turn it off.\n\n**Everything feels stiff** — your prompts are shot labels, not descriptions.\nWrite what a person watching would see.\n\n**The product looks wrong** — nothing in the prompt told it what the product\nlooks like. That comes from your brand folder; fill that part in."}, {"doc": "The organic swipe pack", "section": "The organic swipe pack", "tool": "03-organic-swipe-pack", "path": "tools/03-organic-swipe-pack/START-HERE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/03-organic-swipe-pack/START-HERE.md", "text": "Built 2026-09-22 · 48 formats · 377 real posts, taken apart\n\nEvery format in here came from a post that actually worked — some past a\nhundred million views. Nothing is theory and nothing is invented."}, {"doc": "The organic swipe pack", "section": "How to use it", "tool": "03-organic-swipe-pack", "path": "tools/03-organic-swipe-pack/START-HERE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/03-organic-swipe-pack/START-HERE.md#how-to-use-it", "text": "1. **Pick a format** in `formats/`. Each folder is one proven structure.\n   Open its `00-THE-FORMAT.md` first — what it is, why it works, and the\n   beats in the order they happen.\n2. **Look at a real one.** Every other file in the folder is one real post\n   taken apart beat by beat, with a link to it at the bottom. `frames/`\n   shows the six key moments as a single picture if you would rather not\n   open the video.\n3. **Make it yours.** Copy the image prompt and the motion note into\n   Higgsfield Supercomputer: https://higgsfield.ai/supercomputer/skills?employeeId=0d38d619-0ac2-45c9-bfd5-3371837ccad3\n   Fill in every blank in curly brackets — `{PRODUCT}`, `{SUBJECT}` — from\n   your own brand folder. Those are questions, not decoration.\n4. **Cut it to the beats.** The beat list in each sheet is your edit order.\n\n**Which models?** `WHICH-MODELS.md` in this folder names them and the\nsettings that work. Do not leave it on Auto."}, {"doc": "The organic swipe pack", "section": "Two things that decide whether it works", "tool": "03-organic-swipe-pack", "path": "tools/03-organic-swipe-pack/START-HERE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/03-organic-swipe-pack/START-HERE.md#two-things-that-decide-whether-it-works", "text": "- **Copy the structure, not the content.** The shape is what earned the\n  views; your product is what changes. A scalp treatment and a countertop\n  demo can be the same format — that is the whole point.\n- **Never let it guess.** If you do not know what your product looks like\n  on camera or how your customer talks, go write that down first. A\n  generated shot that guesses is worse than no shot."}, {"doc": "The organic swipe pack", "section": "Want it done for you instead?", "tool": "03-organic-swipe-pack", "path": "tools/03-organic-swipe-pack/START-HERE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/03-organic-swipe-pack/START-HERE.md#want-it-done-for-you-instead", "text": "The **Video Teardown** kit does this to any video you choose, and writes\nthe brief with your brand already in it. Both are free at daemn.co."}, {"doc": "Which models to use, and the settings that work", "section": "Which models to use, and the settings that work", "tool": "03-organic-swipe-pack", "path": "tools/03-organic-swipe-pack/WHICH-MODELS.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/03-organic-swipe-pack/WHICH-MODELS.md", "text": "Do not leave this on Auto. These are the exact models and settings behind the\nvideos this system has actually produced — not a guess at what might work.\n\nEverything below runs inside **Higgsfield Supercomputer**.\n\n---"}, {"doc": "Which models to use, and the settings that work", "section": "Reading the video — Gemini", "tool": "03-organic-swipe-pack", "path": "tools/03-organic-swipe-pack/WHICH-MODELS.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/03-organic-swipe-pack/WHICH-MODELS.md#reading-the-video--gemini", "text": "When you upload a swipe video and ask it to describe every scene, that is a\nGemini model doing the watching.\n\nUse — Model.\nFirst choice — `gemini-3-flash-preview`.\nIf it is busy — `gemini-3.5-flash`.\nFor a long or complicated video — `gemini-3.1-pro-preview`.\nFlash is the right default — it is fast and it does not miss scenes. Only\nreach for Pro when a video is long, dense, or full of overlapping dialogue.\n\n---"}, {"doc": "Which models to use, and the settings that work", "section": "Making the frames — Soul v2", "tool": "03-organic-swipe-pack", "path": "tools/03-organic-swipe-pack/WHICH-MODELS.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/03-organic-swipe-pack/WHICH-MODELS.md#making-the-frames--soul-v2", "text": "Every still starts here: `text2image_soul_v2`.\n\nSetting — Value — Why.\nSize — 1536 × 2048 — portrait, enough resolution to crop.\nStyle — General — anything else fights your prompt.\n`enhance_prompt` — **off** — on, it rewrites you and drifts off the brief.\nSeed — fixed, one per character — the same number gives you the same face.\n**The character-sheet trick.** Before you generate any scene, make one image\nper person as a split-screen sheet: full body on the left, chest-up close-up\non the right, both the same character. Then feed that sheet in as the\nreference for every scene they appear in. That is what stops them being a\ndifferent human in every shot — it is the single biggest difference between\nwork that looks made and work that looks broken.\n\n---"}, {"doc": "Which models to use, and the settings that work", "section": "Making the shots — Seedance 2.0", "tool": "03-organic-swipe-pack", "path": "tools/03-organic-swipe-pack/WHICH-MODELS.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/03-organic-swipe-pack/WHICH-MODELS.md#making-the-shots--seedance-20", "text": "Frames become video with `seedance_2_0`.\n\nSetting — Value.\nRatio / size — 9:16, 1080 × 1920.\n`mode` — `std`.\n`generate_audio` — `true` — it invents its own ambience and dialogue.\n`multi_shots` — `false` — one shot per generation, always.\n`speedramp` — `auto`.\nLength — **4s per scene beat**, 6s for silent b-roll.\nLonger single takes (8–15s) work when one continuous moment carries the beat,\nbut cutting 4-second shots is what holds attention.\n\nFeed each generation the character sheet plus a full cinematic description of\nthe moment — not a shot label. \"Cinematic drama, golden morning market stall.\nThe elderly woman from the reference image reaches across the counter…\" beats\n\"scene 3, woman at stall\" every time.\n\n---"}, {"doc": "Which models to use, and the settings that work", "section": "Voice, if you need it — Seed Audio", "tool": "03-organic-swipe-pack", "path": "tools/03-organic-swipe-pack/WHICH-MODELS.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/03-organic-swipe-pack/WHICH-MODELS.md#voice-if-you-need-it--seed-audio", "text": "`seed_audio`, wav, 24000 Hz.\n\nSetting — Value.\n`speech_rate` — −5 to −15 (slower reads as more serious).\n`loudness_rate` · `pitch_rate` — 0.\n`expression_intensity` — 5.\nNo voice presets. Describe the person in words, and clone from an uploaded\nreference file when you want a specific voice.\n\nNote: if Seedance is already generating audio for a shot, you do not need a\nseparate voice track for it. Use Seed Audio for narration over the cut.\n\n---"}, {"doc": "Which models to use, and the settings that work", "section": "The order you do it in", "tool": "03-organic-swipe-pack", "path": "tools/03-organic-swipe-pack/WHICH-MODELS.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/03-organic-swipe-pack/WHICH-MODELS.md#the-order-you-do-it-in", "text": "1. Character sheets — one per person, fixed seed. **Do this first, always.**\n2. Frames — the brief's image prompt plus the character sheet as reference.\n3. Shots — Seedance, 4 seconds, one shot per generation.\n4. Voice — only if the cut needs narration."}, {"doc": "Which models to use, and the settings that work", "section": "When something looks wrong", "tool": "03-organic-swipe-pack", "path": "tools/03-organic-swipe-pack/WHICH-MODELS.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/03-organic-swipe-pack/WHICH-MODELS.md#when-something-looks-wrong", "text": "**A different face every shot** — you skipped the character sheet, or changed\nthe seed. Go back to step 1.\n\n**It ignored half your prompt** — `enhance_prompt` is on. Turn it off.\n\n**Everything feels stiff** — your prompts are shot labels, not descriptions.\nWrite what a person watching would see.\n\n**The product looks wrong** — nothing in the prompt told it what the product\nlooks like. That comes from your brand folder; fill that part in."}, {"doc": "Start here", "section": "Start here", "tool": "04-paid-ad-swipe-pack", "path": "tools/04-paid-ad-swipe-pack/START-HERE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/04-paid-ad-swipe-pack/START-HERE.md", "text": "This pack is built from a sweep of nine competitor brands' **live paid ads** —\n6,269 ads running right now, spread across 600 different pitches.\n\nYou do not need to have run an ad before to use it. Read this page, then the\nstrategy page, then pick a shape and build.\n\n---"}, {"doc": "Start here", "section": "What a paid swipe is", "tool": "04-paid-ad-swipe-pack", "path": "tools/04-paid-ad-swipe-pack/START-HERE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/04-paid-ad-swipe-pack/START-HERE.md#what-a-paid-swipe-is", "text": "A paid swipe is a collection of ads that companies are **currently paying to\nshow people**. Not their best posts. Not what went viral. The ads they are\nbuying space for, today, with money."}, {"doc": "Start here", "section": "Why that is different from an organic swipe", "tool": "04-paid-ad-swipe-pack", "path": "tools/04-paid-ad-swipe-pack/START-HERE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/04-paid-ad-swipe-pack/START-HERE.md#why-that-is-different-from-an-organic-swipe", "text": "An organic swipe — the videos and posts people share and comment on — tells you\n**what people choose to watch**. That is genuinely useful. It tells you what\nholds attention, what feels native, what a person will stop for when nothing is\npushing it at them.\n\nA paid swipe tells you something else: **what someone is paying to put in front\nof a person who did not ask for it.** That is a harder test, and it is the test\nyour ads will actually face.\n\nHere is why the difference matters so much.\n\nAd budgets do not spread out. They concentrate, and they concentrate fast. A\ncompany launches fifteen pitches, two of them make money, and within weeks\nalmost all of the budget is behind those two. Everything else gets switched off.\nNobody keeps paying to run an ad that loses money — there is no sentiment in it,\nand there is no ego in it, because the numbers come in every single day.\n\nSo when you look at a brand's live ads and see that one pitch is carrying 835 of\ntheir 1,901 ads, you are not looking at a creative preference. You are looking\nat **a survivor**. That pitch beat everything else they tried, and they kept\nputting money behind it because it kept paying them back.\n\nThat is the whole trick of a paid swipe: **the angles carrying the most ads are\nthe angles converting.** You do not have to guess which of their ideas worked.\nThey already spent the money finding out, and the shape of their account tells\nyou the answer.\n\nOne honest limit, stated up front: we can count their ads, not their bank\nstatement. A big pile of ads on one pitch is strong evidence it is winning, not\nproof. The str"}, {"doc": "Start here", "section": "Why that is different from an organic swipe", "tool": "04-paid-ad-swipe-pack", "path": "tools/04-paid-ad-swipe-pack/START-HERE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/04-paid-ad-swipe-pack/START-HERE.md#why-that-is-different-from-an-organic-swipe", "text": "ategy page says more about where that reasoning stops.\n\n---"}, {"doc": "Start here", "section": "What is in the pack", "tool": "04-paid-ad-swipe-pack", "path": "tools/04-paid-ad-swipe-pack/START-HERE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/04-paid-ad-swipe-pack/START-HERE.md#what-is-in-the-pack", "text": "**`what-the-spend-says.md`** — the strategy lesson, read straight off these\nnine brands' numbers. How hard they concentrate, how many versions they build of\na winner, how much is video versus still images, and what all of that says about\nhow they sell. Every claim carries its number.\n\n**`angle-shapes/`** — ten files, one per **angle shape** that showed up again\nand again across these brands.\n\nAn angle shape is **the structure of the pitch, not the subject**. \"Hair loss\"\nis a subject. \"Name the two solutions everyone already knows, disqualify both on\na specific cost, then present yours as the third one\" is a shape — and a men's\nshampoo brand, a testosterone supplement, a skincare brand and a probiotic brand\nare all running it right now about four completely different products.\n\nThat is why shapes are the useful unit. A subject only helps you if you sell\nthat thing. A shape works on anything.\n\nEach shape file gives you:\n\n- what the headline does, what the first line does, what the proof is, what the\n  close asks for\n- a fill-in-the-blank template with `{PLACEHOLDER}` slots\n- why it works on a person\n- how many ads across how many brands are sitting on it\n- a worked example, written out for a made-up brand\n- an image prompt and a motion note, so you can make a video version\n\nNo brand is named anywhere in this pack. Each one is described by its market\nonly. No body copy is copied — every template is a rewrite of the structure."}, {"doc": "Start here", "section": "The ten shapes, biggest first", "tool": "04-paid-ad-swipe-pack", "path": "tools/04-paid-ad-swipe-pack/START-HERE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/04-paid-ad-swipe-pack/START-HERE.md#the-ten-shapes-biggest-first", "text": "Shape — Ads on it — Brands — What it does, in one line.\n`the-spec-sheet` — 1,558 — 6 — Names it flat, ticks off seven facts, asks for the sale.\n`the-third-option` — 969 — 5 — Disqualifies the two solutions they already know, offers a third.\n`the-two-minute-swap` — 805 — 2 — Not a new routine — replace one item in one they already have.\n`the-hidden-culprit` — 534 — 6 — It was never your fault, it was this specific thing.\n`the-switch-story` — 408 — 6 — One person, everything they tried, what finally moved.\n`the-sorter` — 228 — 2 — You don't have one problem, you have four — here's which.\n`the-deadline` — 172 — 7 — A reason today is different from tomorrow.\n`the-moment-it-got-noticed` — 160 — 3 — The scene where it stopped being private.\n`the-insider-reveal` — 155 — 5 — A study or a professional says the category has it wrong.\n`the-somewhere-else-secret` — 123 — 5 — They do it differently over there, and here's why that works.\nThose ten account for 5,112 ads. The biggest pitches readable in this sweep\ncover 5,260 of the 6,269 live ads — 84% of everything running. The remainder\nsits in a long tail of small tests.\n\nTwo further shapes recur but sit on too few ads to earn a template; both are\ndescribed at the end of `what-the-spend-says.md`.\n\n---"}, {"doc": "Start here", "section": "How to use it", "tool": "04-paid-ad-swipe-pack", "path": "tools/04-paid-ad-swipe-pack/START-HERE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/04-paid-ad-swipe-pack/START-HERE.md#how-to-use-it", "text": "**1. Read `what-the-spend-says.md` once.** It is short. It tells you how much to\nconcentrate and how many versions to build, and those two decisions matter more\nthan which shape you pick.\n\n**2. Pick two or three shapes, not ten.** Start with the ones carrying the most\nads across the most brands — `the-spec-sheet`, `the-third-option`,\n`the-hidden-culprit`, `the-deadline`. Those four are running in five to seven of\nthe nine brands. They travel.\n\n**3. Fill the slots from the brand folder.** Every `{PLACEHOLDER}` in a template\nis a question about your brand — who the customer is, what they already tried,\nwhat the thing actually does, what the offer is. The brand folder has those\nanswers already written down. Do not invent them at the keyboard; go get them.\nIf a slot has no answer in the brand folder, that is a gap worth naming, not a\ngap to paper over with a guess.\n\n**4. Hand it to the teardown kit to build.** The shapes here are the *pitch*.\nThe teardown kit turns a pitch into a shot-ready piece: the hook, the beats, the\non-screen text, the shot list. Each shape file ends with an image prompt and a\nmotion note written to feed straight into that step.\n\n**5. Build several versions of the one that works.** This is the single biggest\nlesson in the numbers, and it is on the strategy page in detail. These brands do\nnot find a winner and move on. They find a winner and build it another two\nhundred times.\n\n---"}, {"doc": "Start here", "section": "Making the videos", "tool": "04-paid-ad-swipe-pack", "path": "tools/04-paid-ad-swipe-pack/START-HERE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/04-paid-ad-swipe-pack/START-HERE.md#making-the-videos", "text": "Paste the prompts into Higgsfield Supercomputer —\nhttps://higgsfield.ai/supercomputer/skills?employeeId=0d38d619-0ac2-45c9-bfd5-3371837ccad3"}, {"doc": "Which models to use, and the settings that work", "section": "Which models to use, and the settings that work", "tool": "04-paid-ad-swipe-pack", "path": "tools/04-paid-ad-swipe-pack/WHICH-MODELS.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/04-paid-ad-swipe-pack/WHICH-MODELS.md", "text": "Do not leave this on Auto. These are the exact models and settings behind the\nvideos this system has actually produced — not a guess at what might work.\n\nEverything below runs inside **Higgsfield Supercomputer**.\n\n---"}, {"doc": "Which models to use, and the settings that work", "section": "Reading the video — Gemini", "tool": "04-paid-ad-swipe-pack", "path": "tools/04-paid-ad-swipe-pack/WHICH-MODELS.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/04-paid-ad-swipe-pack/WHICH-MODELS.md#reading-the-video--gemini", "text": "When you upload a swipe video and ask it to describe every scene, that is a\nGemini model doing the watching.\n\nUse — Model.\nFirst choice — `gemini-3-flash-preview`.\nIf it is busy — `gemini-3.5-flash`.\nFor a long or complicated video — `gemini-3.1-pro-preview`.\nFlash is the right default — it is fast and it does not miss scenes. Only\nreach for Pro when a video is long, dense, or full of overlapping dialogue.\n\n---"}, {"doc": "Which models to use, and the settings that work", "section": "Making the frames — Soul v2", "tool": "04-paid-ad-swipe-pack", "path": "tools/04-paid-ad-swipe-pack/WHICH-MODELS.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/04-paid-ad-swipe-pack/WHICH-MODELS.md#making-the-frames--soul-v2", "text": "Every still starts here: `text2image_soul_v2`.\n\nSetting — Value — Why.\nSize — 1536 × 2048 — portrait, enough resolution to crop.\nStyle — General — anything else fights your prompt.\n`enhance_prompt` — **off** — on, it rewrites you and drifts off the brief.\nSeed — fixed, one per character — the same number gives you the same face.\n**The character-sheet trick.** Before you generate any scene, make one image\nper person as a split-screen sheet: full body on the left, chest-up close-up\non the right, both the same character. Then feed that sheet in as the\nreference for every scene they appear in. That is what stops them being a\ndifferent human in every shot — it is the single biggest difference between\nwork that looks made and work that looks broken.\n\n---"}, {"doc": "Which models to use, and the settings that work", "section": "Making the shots — Seedance 2.0", "tool": "04-paid-ad-swipe-pack", "path": "tools/04-paid-ad-swipe-pack/WHICH-MODELS.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/04-paid-ad-swipe-pack/WHICH-MODELS.md#making-the-shots--seedance-20", "text": "Frames become video with `seedance_2_0`.\n\nSetting — Value.\nRatio / size — 9:16, 1080 × 1920.\n`mode` — `std`.\n`generate_audio` — `true` — it invents its own ambience and dialogue.\n`multi_shots` — `false` — one shot per generation, always.\n`speedramp` — `auto`.\nLength — **4s per scene beat**, 6s for silent b-roll.\nLonger single takes (8–15s) work when one continuous moment carries the beat,\nbut cutting 4-second shots is what holds attention.\n\nFeed each generation the character sheet plus a full cinematic description of\nthe moment — not a shot label. \"Cinematic drama, golden morning market stall.\nThe elderly woman from the reference image reaches across the counter…\" beats\n\"scene 3, woman at stall\" every time.\n\n---"}, {"doc": "Which models to use, and the settings that work", "section": "Voice, if you need it — Seed Audio", "tool": "04-paid-ad-swipe-pack", "path": "tools/04-paid-ad-swipe-pack/WHICH-MODELS.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/04-paid-ad-swipe-pack/WHICH-MODELS.md#voice-if-you-need-it--seed-audio", "text": "`seed_audio`, wav, 24000 Hz.\n\nSetting — Value.\n`speech_rate` — −5 to −15 (slower reads as more serious).\n`loudness_rate` · `pitch_rate` — 0.\n`expression_intensity` — 5.\nNo voice presets. Describe the person in words, and clone from an uploaded\nreference file when you want a specific voice.\n\nNote: if Seedance is already generating audio for a shot, you do not need a\nseparate voice track for it. Use Seed Audio for narration over the cut.\n\n---"}, {"doc": "Which models to use, and the settings that work", "section": "The order you do it in", "tool": "04-paid-ad-swipe-pack", "path": "tools/04-paid-ad-swipe-pack/WHICH-MODELS.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/04-paid-ad-swipe-pack/WHICH-MODELS.md#the-order-you-do-it-in", "text": "1. Character sheets — one per person, fixed seed. **Do this first, always.**\n2. Frames — the brief's image prompt plus the character sheet as reference.\n3. Shots — Seedance, 4 seconds, one shot per generation.\n4. Voice — only if the cut needs narration."}, {"doc": "Which models to use, and the settings that work", "section": "When something looks wrong", "tool": "04-paid-ad-swipe-pack", "path": "tools/04-paid-ad-swipe-pack/WHICH-MODELS.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/04-paid-ad-swipe-pack/WHICH-MODELS.md#when-something-looks-wrong", "text": "**A different face every shot** — you skipped the character sheet, or changed\nthe seed. Go back to step 1.\n\n**It ignored half your prompt** — `enhance_prompt` is on. Turn it off.\n\n**Everything feels stiff** — your prompts are shot labels, not descriptions.\nWrite what a person watching would see.\n\n**The product looks wrong** — nothing in the prompt told it what the product\nlooks like. That comes from your brand folder; fill that part in."}, {"doc": "Video Machine (lab — Damon)", "section": "Video Machine (lab — Damon)", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/CLAUDE.md", "text": "**video-production** — takes **an AI video brief**, makes **the voice, the\nstills and the clips**. Does not **write the script**. Triggered by\n`python3 machine/run.py start <brief.md | plan.json> --brand <brand> --format <format>`\n(`--dry-run` spends nothing), then `record` and `pack`.\n\nThe model-agnostic AI-video production process: brief in, finished clips\nout, across four generation stations — cast, voices, scenes, motion. Being\nrebuilt to run fully local. The edit (cut sheet + rendering) is a separate\nprocess, deliberately out of scope here."}, {"doc": "Video Machine (lab — Damon)", "section": "The route this machine is (ruled 2026-09-02)", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/CLAUDE.md#the-route-this-machine-is-ruled-2026-09-02", "text": "**Teardown makes the brief. This machine turns the brief into output.**\nThey are two halves of one road and neither does the other's job:\n\n`chain_config.json` is the whole route, stage by stage, the same shape the\nteardown machine's chain uses so the two read alike. Thirteen stages: six\nthat write (format read · production document · identity plan · ledgers ·\nscene prompts · handoff), the four generation stations that were always\nhere (cast · voices · scenes · motion), and the machine steps between them\n(train · generate · re-roll).\n\nThe named method docs are the doctrine behind the chain —\nwritten 2026-09-01 from a real production Damon ran and generalised on his\nword, brand-agnostic and format-agnostic. `formats/` holds the profiles: a\nprofile says what a scene CONTAINS (micro-drama, single presenter,\ndemonstration, testimonial montage); it never changes the method. Adding a\nformat costs one profile.\n\n**Proven on the route, 2026-09-16.** Six pieces ran it end to end across\nthree formats — a song ad, an expert-consult ad and meme-style ads. The\n`chain_config.json` stages remain the design; the checklist protocol\n(`formats/RUN-PROTOCOL.md`) is what runs, by hand, with the session\npasting the prompts' variables. That day also cost ~$55 an ad against a\n$3–4 floor, which is what the hard rules below are for. Lab quarantine\nholds."}, {"doc": "Video Machine (lab — Damon)", "section": "Hard rules (2026-09-17)", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/CLAUDE.md#hard-rules-2026-09-17", "text": "- The platform is detected and the models shown before anything runs.\n- Nothing submits without a green preflight.\n- Nothing moves unverified — a still goes to motion only after it is\n  verified against its cast sheet, in writing.\n- A ceiling ships FLAGGED with zero re-rolls.\n- A voice anchor id is first-class when element registration is\n  rate-limited.\n- One blocking poll per run; state restored from the run record.\n- Sweep every ledger page before quoting spend.\n- Every format enters through `formats/bank.json`, written from\n  `formats/TEMPLATE.md`.\n- The protocol is `formats/RUN-PROTOCOL.md`; profiles never carry gates.\n- Two QC layers, two verdicts — the inspector says it is the scene\n  specified; the director says it will stop a thumb; a clip ships with\n  both on file.\n- Every video prompt carries the no-cut line; every result shown keeps the\n  anti-perfection budget (the course write-up, Damon 09-14).\n- Voices are made on our ElevenLabs account, one custom voice per\n  character, generated before any picture and piped into the provider; the\n  song is Lyria 3 Pro on fal with timings from fal's speech-to-text.\n- Two hands (ruled again 2026-09-18 evening, Damon: \"let's just do the\n  higgsfield seedance frame\"): the machine makes cast, stills, edits, voice\n  and song directly; motion AND the talking beat run on Seedance 2.5 through\n  Higgsfield — the first frame as start_image, the last frame as end_image,\n  the scene's voice slice as audio_references, one clip per scene up to\n  thirty seconds, proven live the same day (line check 100%). The fal Omni\n  door from the after"}, {"doc": "Video Machine (lab — Damon)", "section": "Hard rules (2026-09-17)", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/CLAUDE.md#hard-rules-2026-09-17", "text": "noon ruling is an alternate, not the print door.\n- Every talking clip is transcribed and compared to its line before it\n  ships; lip-sync to the wrong words is a FLAG.\n- **One image door** (2026-09-18, Damon: \"gpt for all image work\"): every still\n  and every edit runs on GPT Image 2.5 direct — edits with input fidelity\n  high and two references (previous frame, cast sheet), colour-matched to\n  the first frame after. Nano Banana Pro is an alternate in the registry.\n- **No scene length is ever a number** (2026-09-18). A scene plays out\n  fully — its characters, its elements, its words, its pain or proof —\n  and its length falls out of that; a format profile gives the length\n  philosophy, never a number, and a motion model's duration cap splits a\n  scene at its boundary only, never by line. A scene that cannot name\n  its Emotion and its Outcome is a transition — merge it or cut it; this\n  rule outranks the profile.\n- **A scene is a paragraph** (2026-09-18). One setting, one section of the\n  argument, one continuous run of speech — and the voice for the whole\n  piece is ONE read, chunked per scene and stitched, never a call per line.\n- **Frames, not scenes** (2026-09-18). A scene generates exactly two\n  stills — the FIRST FRAME and the LAST FRAME, the last an edit of the\n  first with two references and a colour match back to it. What happens\n  between them is beats on the clip's own timeline, one verb each, timed\n  off the voice track. A beat is never a generated picture.\n- **The contract gate** (2026-09-18). Every item is validated against\n  `machine/model-inputs.json` — the "}, {"doc": "Video Machine (lab — Damon)", "section": "Hard rules (2026-09-17)", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/CLAUDE.md#hard-rules-2026-09-17", "text": "model input contract — before anything\n  submits: a missing required field, a beat with two verbs, a frame with no\n  delta, a to-camera scene with no audio slice, more beats than the maker\n  takes. Red names the field, and nothing submits red.\n- **Scene shapes and mood are bound from the doctrine, not authored\n  here** (2026-09-18). `components/marketing-doctrine/` is where a\n  technique's scene shape and the mood-building rules live; this machine\n  reads them, bound as `{techniques}` and `{mood}`, and never restates\n  them in a prompt."}, {"doc": "Video Machine (lab — Damon)", "section": "Gates — what holds failing work (listed 2026-09-20)", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/CLAUDE.md#gates--what-holds-failing-work-listed-2026-09-20", "text": "Nothing here is new except the last row; this is the one place they are all\nnamed. A gate REFUSES — red names the item and what to do, and nothing submits\nor ships past it.\n\ngate — where — what it holds.\n**Preflight** — `machine/preflight.py check` — rules 1–12 — a batch before it is submitted: a person or product with no reference, post effects in a prompt, a start image that is not a verified frame, a model the provider does not offer, a batch over the cap, a line claimed twice, an unknown or deprecated format (rule 9, the local `formats/bank.json`), no no-cut line, and the model input contract (rule 12).\n**Still QC** (the inspector) — `prompts/stage-5-qc/` → `preflight.py verdict --layer inspector`, then `preflight.py verify` — a still that is not the scene specified; an unverified frame never goes to motion (rule 6).\n**Motion QC** — `prompts/stage-5b-motion-qc/` → `verdict --layer motion` — a clip that did not do what the beats said.\n**Director** — `prompts/stage-5c-director/` → `verdict --layer director` — a clip that will not stop a thumb; two FLAGs on one item is a weak concept, sent back to the script.\n**Line parity** — `machine/lineparity.py`, run by `run.py record` — a talking clip that says other words than its line.\n**Coverage** — `preflight.py coverage`, run by `run.py pack` — a pack with a line uncovered or doubled, or a clip missing its motion, director or parity verdict.\n**The element library** (new 2026-09-20) — `machine/library_check.py`, preflight **rule 13** — a `format` that is not a `format/video` row in `components/elements` — and a row that exists th"}, {"doc": "Video Machine (lab — Damon)", "section": "Gates — what holds failing work (listed 2026-09-20)", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/CLAUDE.md#gates--what-holds-failing-work-listed-2026-09-20", "text": "ere only as a name (`[TO DEFINE …`) is refused as **named but not defined yet**; a `style_id` that is not a `style/video` row; a scene delivery dial (`humor`, `style`, `register`, `pacing`) whose value is not on its `delivery/<dial>` list. Every refusal names the real ids. `unfilled` and empty dials are passed over. Asked on every run that uses the real bank; a run on another `--bank` is asked only when it also names `--library`. If the library cannot be read, preflight WARNS and falls back to the bank alone.\n**A new format is two steps now.** Write it into `formats/bank.json` from the\ntemplate, then rebuild the library (`python3 components/elements/machine/elements.py\nbuild`) — until then rule 13 refuses it and says exactly that.\n\nThe piece's `style` is a grade sentence, not an id, and is never asked about —\na look that IS an id goes in `style_id` (run.json, or the brief's `piece`)."}, {"doc": "Video Machine (lab — Damon)", "section": "The variation chain — where scale actually comes from (2026-09-02)", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/CLAUDE.md#the-variation-chain--where-scale-actually-comes-from-2026-09-02", "text": "One approved production feeds unlimited variations. That is the multiplier:\nthe expensive part (deciding the film, casting it, proving it) is paid once,\nand every restyle after it is cheap.\n\n**A variation is not a new film.** Screenplay, blocking, dialogue,\nmechanics and edit are frozen; only the rendering swaps. `method-05-format-\nvariation.md` is the doctrine, written from a real run — including its two\nrecorded failures, which the chain is shaped to prevent:\n\n- **Re-planning.** Handing approved prompts to a model to \"restyle\" made it\n  re-plan the film — new framing, paraphrased dialogue — and the result read\n  as a different film. So **stage v3 is not a model.** `machine/restyle.py`\n  does exactly three mechanical things (style lock prepended · media swapped\n  · photoreal descriptors stripped) and prints its own diff, so the\n  guarantee that every approved word survived is auditable.\n- **Re-describing.** Generating a \"new base\" from a text description\n  produced people who were not the approved cast, and a product that was\n  not the real product. So v2 converts FROM the refined source — the trained\n  identity, the approved plate, the real packshot — and marks any asset with\n  no source `⚠ NO SOURCE` rather than inventing one.\n\n**The axis distinction, because it decides which chain runs.** A variation\nchanges how the film LOOKS. Changing the avatar, the problem or the angle\nchanges what the film SAYS — that is a new production through stage 0b with\ndifferent inputs, not a variation. Both scale; they are not the same lever\nand conflating them is how a \"variation\" quietly "}, {"doc": "Video Machine (lab — Damon)", "section": "The variation chain — where scale actually comes from (2026-09-02)", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/CLAUDE.md#the-variation-chain--where-scale-actually-comes-from-2026-09-02", "text": "becomes a remake."}, {"doc": "Video Machine (lab — Damon)", "section": "Frames are made by EDITING (ruled 2026-09-02)", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/CLAUDE.md#frames-are-made-by-editing-ruled-2026-09-02", "text": "**An edit instruction must never describe anything the source image already\nshows.** The seed still already holds the framing, blocking, room, light and\npose; describing them again tells the model to rebuild them, which is the\ngeneration we were escaping. Measured on one scene: 4,262 characters of\nself-contradicting description → 204–386 characters of delta, and text\nrendered legibly for the first time.\n\nThe chain, on Damon's call: **image teardown to UNDERSTAND the frame\n(stages 1–2 of `image-teardown`), then that understanding as the\nreference for the edit** — keep what the replication spec calls\nload-bearing, change what it calls swappable. The frames stage stops\nguessing who is in the picture and reads it.\n\n**Type is a layer, never a generation.** Generate a clean plate; compose the\nwords on top (`image-teardown/tools/compose.py`). Text cannot garble, copy\nstays editable, and one plate carries every hook variation.\n\nFull account, with the numbers and the three defects that hid inside\n\"edit\": `frames-by-edit.md`."}, {"doc": "Video Machine (lab — Damon)", "section": "Formats vs building the avatar — keep these apart (2026-09-03)", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/CLAUDE.md#formats-vs-building-the-avatar--keep-these-apart-2026-09-03", "text": "**Building a character is its own process**, done once per person, living\nin `brands/<brand>/ai-cast/<name>/`. A FORMAT consumes a character and\nnever makes one: it takes that character's master and the skin crops\nfrom their own boards as references. Same cast serves a song ad, a micro-drama and a demonstration\nwith no rework. See `building-the-cast.md`."}, {"doc": "Video Machine (lab — Damon)", "section": "How a song ad actually gets built (worked 2026-09-03)", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/CLAUDE.md#how-a-song-ad-actually-gets-built-worked-2026-09-03", "text": "`formats/song-ad.md` is the profile; the exact calls from its two runs are\nthe provider pages, `providers/fal.md` and `providers/higgsfield.md` —\nwritten from a real end-to-end run with Damon: words first, record the song\nand KEEP THE TIMING SHEET, cast from the AI cast and use what the character\nalready has, one call per shot with references doing the work, ask for the\nphone-camera look explicitly, animate only approved stills, cut to the\nlyric timings.\n\n**The rule underneath all of it: a reference beats a description.** Every\nfailure that day was something written in words that should have been\npointed at with a picture — the person, the skin, the scene, the style.\n\n**Motion is 94% of the cost** (~62¢ a clip against ~4¢ a still), which is\nwhy stills are approved before anything moves. A 30-second ad is about $4."}, {"doc": "Video Machine (lab — Damon)", "section": "What is in this folder", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/CLAUDE.md#what-is-in-this-folder", "text": "**The live method docs** — how the work is actually done today:\n\n`SCENE-GENERATION.md` — **how a scene is generated, end to end** — what a scene is, the six steps in order, the four-block prompt with the call that worked, what the gate refuses.\n`frames-by-edit.md` — how a frame is made. The core finding: a reference beats a description.\n`building-the-cast.md` — how a character is built. **Its own process, not a format** — done once per person, serves every format.\n`format-variation.md` — restyling an approved piece into a new visual format.\n`handoff-and-edit.md` — what the editor receives.\n`BRIEF-SPEC.md` — the brief shape, ruled 2026-09-14.\n`PLATE-CHECK.md` — every still judged by measurement before anyone sees it.\n`AI-CREATIVE-LINE.md` — the story of the whole line, swipe to motion.\n**`formats/`** — the bank: one profile per format (`bank.json` +\n`TEMPLATE.md`), `RUN-PROTOCOL.md` is the one run every format follows.\n\n**`providers/`** — `MODEL-HOUSE.md` (which door per model, with the evidence in `research/`); `fal.md` house notes.\n\n**The machine** — `machine/`, `prompts/`, `chain_config.json`.\n\n**`ROADMAP.md`** — the plan; three legs, in order.\n\n**Stale, kept for history** — see `archive/README.md` for what moved there\nand what superseded each piece."}, {"doc": "Video Machine (lab — Damon)", "section": "Files", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/CLAUDE.md#files", "text": "- Output lands at `runs/video-machine/<brand>/<label>/` per `runs/README.md`\n  (2026-09-17).\n- `prompts/` — one folder per stage; the CURRENT prompt files live at the\n  top, versioned in the filename (`...-v2-damon.md`). **Never edit a prompt\n  in place** — a change is a new file with a bumped version; retired\n  versions move to `archive/` inside the stage folder. The board renders\n  whatever is current.\n- `machine/` — the one runner and its doors:\n  - **`run.py` — the one command**: `start · record · pack`. `start` makes\n\n  - **The direct doors**: `voice.py` (ElevenLabs), `direct_openai.py`\n\n  - **The contract** (2026-09-18): `model-inputs.json` — per station on\n\n  - **The gate code** (provider-neutral): `platform.py` (the plan, the\n\n  - **The by-hand tools stay, updated to the doors** (Damon, 2026-09-18:"}, {"doc": "Video Machine (lab — Damon)", "section": "Where brand context lives (Damon's ruling, 2026-08-30)", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/CLAUDE.md#where-brand-context-lives-damons-ruling-2026-08-30", "text": "Brand data NEVER lives inside the machine. It lives in\n`brands/<brand>/` — products (one folder per product:\nproduct.md + images/ + Drive links), core-avatars, existing-content,\ncast. The machine pulls from there by the run's `brand` field. The\n<brand> folder is the structural baseline; a product file is what the\nthing IS — offers live in the brand's offer-bank."}, {"doc": "Video Machine (lab — Damon)", "section": "Rules for working here", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/CLAUDE.md#rules-for-working-here", "text": "1. Agnostic on every axis, everywhere except the evidence files: model-,\n   brand-, product- and avatar-agnostic (Damon, 2026-08-30 — we are\n   building a system; it must run on any content for any brand).\n   Stations are slots with audition criteria; a model earns a slot by\n   passing the audition, never by name in the process docs.\n2. **The live board is Damon's working surface** (his ruling 2026-08-30):\n   today that is the Cinema line's board (`machine/board.py`, :8456) for a\n   run — the cast, the prompts, the plain-words redo box — and the run\n   record (`media/`, `direct.json`, `verdicts.json`) beside it. The artifact\n   **The Road to $10** (https://claude.ai/artifact/NpVDsCvGoXrdnU7PKiy3Lw)\n   is the shareable picture of the cost story; **The Video Machine** (see\n   `context/artifacts.md`) is how this system is set up and the\n   roadmap — that old link is dead, and this is the live one.\n3. Stage files may name candidate models under \"candidates\" — that's where\n   model specifics belong, marked proven/testing/rejected."}, {"doc": "AI video production", "section": "AI video production", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/README.md", "text": "**A brief goes in. Finished clips come out — one command, nobody at a keyboard.**\n\nThis lane is brand-, provider- and format-agnostic. Nothing in `machine/`,\n`prompts/` or `formats/` names a brand, a product, a person or a model — a run\nnames its brand and its format, and the provider is detected or picked. It is\nthe twin of `image-production`, which is stage two for statics."}, {"doc": "AI video production", "section": "The spine", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/README.md#the-spine", "text": "Full detail: `formats/RUN-PROTOCOL.md`.\n\n1. **Teardown classifies and injects** → a baseline brief (`components/video-teardown`, its own machine).\n2. **At stage 5 the route is decided** — creator (a real person) or AI. This protocol reads that decision; it never re-decides it.\n3. **Stages 5–8 build the brief** for whichever route was picked.\n4. **Creator route** → the brief goes to the person through the Google Sheet system. This machine stops.\n5. **AI route** → the AI-lane brief is this machine's intake. Everything below is the AI route."}, {"doc": "AI video production", "section": "Before a run: three choices", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/README.md#before-a-run-three-choices", "text": "**Format** — a row in `formats/bank.json`, written from `formats/TEMPLATE.md`.\nAn unknown format is refused; a new one gets a profile and a row first.\nBank + all seven profiles: `formats/README.md`.\n\n**The door** — not a choice since 2026-09-18: every station is a direct\ncall on our own keys (the one hand, below). `--provider fal |\nhiggsfield-mcp | higgsfield-ui` still exists to force a piece into a house\non purpose; nothing uses it by default. The ruling per model and door:\n`providers/MODEL-HOUSE.md`; rates, ceilings, request shapes and the history\nof every door ruling: `machine/providers.json`.\n\n**Brand** — read, never written. A run's `brand` field points at\n`brands/<brand>/`; there is no code to change to add one.\n\nthe run names — and the lane reads.\n`\"brand\": \"<brand>\"` — `brands/<brand>/element-facts.json` — what is true of each product, and which elements are blocked.\n`cast.presenter` / `cast.who` / `cast.says` — the prompt shape names nobody; the identity comes from the plan.\n`cast.face` / `.room` — the cast sheet and the room reference the stills are seeded from.\n`brands/<brand>/ai-cast/<name>/voice.json` — the character's cloned ElevenLabs `voice_id`.\n— — Drive `Shared Assets/brands/<brand>/finals/<run>/`.\n**To add a brand:** create `brands/<brand>/element-facts.json`, build its\ncast (`building-the-cast.md`) with a `voice.json` per character, and point\na run at it. There is no code to change."}, {"doc": "AI video production", "section": "The run — the protocol's gates, in order", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/README.md#the-run--the-protocols-gates-in-order", "text": "One command runs the whole protocol (`machine/RUN.md`):\n\nFull detail, including which gates each verb runs: `formats/RUN-PROTOCOL.md`.\n\n# — Gate.\n1 — Platform plan shown.\n2 — Preflight green.\n3 — Generate stills.\n4 — QC each still.\n5 — Verify each still against its cast sheet.\n6 — Motion, only on verified frames.\n7 — Motion QC.\n8 — Director pass.\n9 — Coverage gate.\n10 — Receipt.\n11 — Naming gate.\n12 — Editor pack."}, {"doc": "AI video production", "section": "One hand (2026-09-18)", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/README.md#one-hand-2026-09-18", "text": "Every station is made by the machine, directly, in one command — the\n17 Sep \"two hands\" folded when the talking beat and silent motion moved to\nfal's Omni (Damon: \"we don't need Higgsfield MCP for any of this now\").\n\nStation — Door — Proof.\nvoice — ElevenLabs, direct — cloned voice per character, made first — live 18 Sep.\ncast · still — OpenAI, direct — GPT Image 2.5 sunburst, fidelity high, moderation low — live 18 Sep.\nedit — Google, direct — Nano Banana Pro — live 18 Sep.\nsong — Google Lyria, direct — live 18 Sep.\n**talking** — **fal Omni** → ElevenLabs speech-to-speech → remux · parity ≥85% twice (`machine/omni.py`) — live 18 Sep, one call, 63 s.\n**motion** (silent) — **fal Omni**, sound stripped — dry-run; first real cutaway proves it.\nWired and waiting: Seedance direct on BytePlus (`machine/direct_byteplus.py`)\n— blocked on US billing. Declined: the Higgsfield API. Forced houses only:\nHiggsfield (MCP / app), fal-as-a-house. Full ruling: `providers/MODEL-HOUSE.md`.\n\n**The prompts are files.** `prompts/stage-0-read-brief` … `stage-6-handoff`,\nversioned, on the teardown machine's convention. Each carries one fenced\n`shape` block that `prompt.py` loads at run time. **Edit the file and every\nbrief made after it changes** — uniformly, without touching code. They are\nrendered verbatim on the board, because a rule nobody can read is a rule\nnobody can correct."}, {"doc": "AI video production", "section": "Running it", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/README.md#running-it", "text": "Or from the board — `python3 machine/board.py runs/<run>/` (:8456): press\nGenerate on a card and `machine/drain.py` makes the take on the direct\ndoors in the background (frame → line → Omni recipe; cutaway → Omni\nsilent), then the clip appears in the card. Proven live 2026-09-18 from a\ntwo-order queue. `chain.py` (the gates as text), `pull.py` (a take back\nfrom a house), `ship.py` (→ Drive finals) are unchanged in role."}, {"doc": "AI video production", "section": "How a scene is generated (2026-09-18)", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/README.md#how-a-scene-is-generated-2026-09-18", "text": "One setting, one paragraph of voice, one clip up to thirty seconds; two stills\n(first and last) and the middle as timed beats. Voice first as one continuous\ntake with timestamps → first frame (GPT Image 2.5, cast sheet as reference) →\nlast frame (GPT edit, two references, colour-matched) → the clip (Seedance 2.5\non Higgsfield: start image + end image + the voice slice + the four-block\nprompt) → line check → director. The method: `SCENE-GENERATION.md`; the\ncontract the gate enforces: `machine/model-inputs.json`."}, {"doc": "AI video production", "section": "The doctrine and the research (2026-09-18)", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/README.md#the-doctrine-and-the-research-2026-09-18", "text": "The swipe side runs on Eugene Schwartz's frameworks, held once in\n`components/marketing-doctrine/` and pulled into every machine: five\nawareness levels (each with a must-not), five sophistication stages, seven\ntechniques, and the sections as a vocabulary of what each part of a\nvideo is doing. Every swipe gets a doctrine read right after ingestion and\nis banked in the framework bank; expansion reaches for a technique only with\na bank row or a research receipt; the spice pass adds the creative layer\nwith a cast voice cloned then adjusted, rights on file. The brief that\narrives here carries Section · Technique · Emotion · Outcome per scene, and\nthis machine's scene shapes and mood rules are bound from the same doctrine\n(`{techniques}`, `{mood}`). Research fires by wiring: the language-bank\nquery and the research gatherer (`components/research-gatherer/`) resolve\nas stage inputs on every run. Record: `PROMPT-CHANGES.md`."}, {"doc": "AI video production", "section": "Where output goes", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/README.md#where-output-goes", "text": "`runs/video-machine/<brand>/<label>/`, per the repo-root `runs/README.md`\n(2026-09-17 ruling): `run.json`, `frames.json`, `lines.json`, `batches/`,\n`ledger.json`, `deliverable/`. Media never enters git; `.gitignore` enforces\nit. The local `M/runs/` and `machine/runs/` folders are gitignored media from\nbefore that ruling — not the live destination."}, {"doc": "AI video production", "section": "The map of this folder", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/README.md#the-map-of-this-folder", "text": "What it is — Live / archive.\n`AI-CREATIVE-LINE.md` — the story of the whole line, swipe to motion — live.\n`BRIEF-SPEC.md` — the brief shape, ruled 2026-09-14 — live.\n`PLATE-CHECK.md` — the plate check — every still judged by measurement before anyone sees it — live.\n`building-the-cast.md` — how a character is built, once per person, serves every format — live.\n`frames-by-edit.md` — how a frame is made — a reference beats a description — live.\n`format-variation.md` — restyling an approved piece into a new visual format — live.\n`handoff-and-edit.md` — what the editor receives — live.\n`archive/THE-CINEMA-LINE.md` — the coded Higgsfield talking-ad route, proven 2026-09-12/13 — superseded by the Omni door — archive.\n`REPO-AND-DRIVE.md` — mirror of the Repo and Drive artifact — live.\n`PRINT-RUN-260912.md` — a print record — two finished ads, one torn-down source — live (record).\n`chain_config.json` — the 13-stage design; only `machine/pipeline.py` reads it; two stages point at parked prompts — live (design, not the running code).\n`formats/` — the bank (`bank.json`), one profile per format, `RUN-PROTOCOL.md` the run, `TEMPLATE.md` — live.\n`providers/` — `MODEL-HOUSE.md` (the door ruling per model) + `research/` dossiers + `fal.md` house notes; `higgsfield.md` and `EDITOR-SHEET.md` moved to `archive/` — live.\n`prompts/` — one folder per stage, versioned files, current at the top — live.\n`prompts/stage-5b-motion-qc/` — the motion QC prompt — gate 7 — live.\n`prompts/stage-5c-director/` — the director-pass prompt — gate 8 — live.\n`machine/` — the code — `run.py` + the direct doors (`d"}, {"doc": "AI video production", "section": "The map of this folder", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/README.md#the-map-of-this-folder", "text": "irect_openai`, `direct_google`, `voice`, `omni`, `direct_byteplus`), the gates (`platform`, `preflight`, `lineparity`, `deliver`), `providers_fal` for a forced house — live.\n`machine/run.py` — the one runner — `start · record · pack` — live.\n`machine/omni.py` — the talking beat and silent motion on fal Omni — the controlled recipe as one call — live.\n`machine/providers_fal.py` — fal as a forced house, and the queue helpers `omni.py` reuses — live.\n`machine/RUN.md` — the runner's own page — authoritative for `run.py` — live.\n`ROADMAP.md` — the plan — three legs, in order — live.\n`archive/` — history — process, workplan, stages/, sequence-flow, evidence, parked-v1, the Cinema line, the editor sheet, Higgsfield house rules, See `archive/README.md` — archive.\n`runs/` — local pre-ruling media, gitignored — archive (superseded).\nrepo-root `runs/video-machine/` — where filed runs land, per `runs/README.md` — now holds the three filed 16 Sep runs — live."}, {"doc": "AI video production", "section": "What is next", "tool": "05-ai-video-production", "path": "tools/05-ai-video-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/05-ai-video-production/README.md#what-is-next", "text": "See `ROADMAP.md` — three legs, in order: one command (leg 1, landed), the\nprint line (leg 2), assembly (leg 3)."}, {"doc": "image-teardown — a static ad in, its labels and brief out", "section": "image-teardown — a static ad in, its labels and brief out", "tool": "06-image-teardown", "path": "tools/06-image-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/06-image-teardown/CLAUDE.md", "text": "**image-teardown** — takes **a static ad**, makes **its element labels and a\nbrief to remake it**. Does not **make the image**. Triggered by\n`python3 chain.py start --brand <brand> --product <product> --avatar <avatar> --swipe <swipe>`.\n\n**Owner:** Damon. **Group:** teardowns. How to run it for a brand: `RUN.md`.\nHistory and why each part exists: `README.md`. The draft pictures that follow a\nbrief are the draft standard (`DRAFT-STANDARD.md`) — a separate job that reads\nthis tool's briefs."}, {"doc": "image-teardown — a static ad in, its labels and brief out", "section": "Steps", "tool": "06-image-teardown", "path": "tools/06-image-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/06-image-teardown/CLAUDE.md#steps", "text": "Declared once, in `tools/run.py` `STAGES`. `chain.py` is the entry; it opens\nruns and hands the stages to `tools/run.py`.\n\nStep — What it does — Runs on — Writes (`runs/<run>/out/`).\n1 — teardown — the ad, as an objective record — Gemini (reads the picture) — `01-teardown.md`.\n2 — the format — the ad with its brand stripped out — Gemini — `02-replication-spec.md`.\n2b — **element labels** — which library rows the ad IS — Claude · `checks` tier — `02b-elements.md` → `elements.json`.\n3 — injection — our brand through that format — Claude · `designs` tier — `03-injection.md`.\n4 — headlines — Claude · `designs` — `04-headlines.md`.\n5 — picture variations — Claude · `designs` — `05-image-variations.md`.\n6 — the brief — Claude · `designs` — `06-brief.md`.\nModel ids are not written in this folder: Claude steps name a tier and the id\ncomes from `components/run-kit` `model.TIERS`. `CLAUDE_MODEL` / `GEMINI_MODEL`\nin the environment force one.\n\n**Dry run — spends nothing.** `chain.py start|fill|variant ... --dry-run` (or\n`tools/run.py <run> all --brand <brand> --dry-run`) resolves every step's\nprompt and every file and variable it would be handed, prints OK or MISSING\nper step, and starts no model, opens no run, writes no file. Exit 1 if anything\nis MISSING.\n\n**Element labels.** Step 2b hands the model the teardown and the candidate rows\n(id · name · what) rendered at run time from `components/elements/library/` —\n`format/image`, `style/image`, `framework/all`, `doctrine/awareness`. The reply\nis read by `tools/elements_label.py` and every id is asked of\n`components/elements/machine/ele"}, {"doc": "image-teardown — a static ad in, its labels and brief out", "section": "Steps", "tool": "06-image-teardown", "path": "tools/06-image-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/06-image-teardown/CLAUDE.md#steps", "text": "ments.py` `check()`. An id the library does not\nhold is **refused and recorded, never guessed**. `none-fits` is a real answer\nand carries a one-line proposed row — that is how the library grows; nothing\nhere edits the library. `chain.py fill ... --labels` labels runs torn down\nbefore this step existed."}, {"doc": "image-teardown — a static ad in, its labels and brief out", "section": "The four questions", "tool": "06-image-teardown", "path": "tools/06-image-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/06-image-teardown/CLAUDE.md#the-four-questions", "text": "- **Engine + owner:** `chain.py` + `tools/` (`run.py` the steps, `paths.py`\n  every path, `elements_label.py` the labels, `gates.py` the gates and the\n  filing), Damon.\n- **Definitions + curator:** `prompts/` — one file per step, `-vN-damon.md`,\n  the highest NUMBER wins (`-v10-` beats `-v2-`); a changed prompt is a new\n  file, never an edit. `superseded/` and `parked/` never run. Damon.\n- **The plane it emits:** `runs/image-teardown/<brand>/<label>/` at the repo\n  root — `01…06-*.md`, `02b-elements.md`, `elements.json`, `source.json`,\n  `run.json`, `check.json` (and `variant.json` for a variant). Filed by\n  `tools/run.py` whenever a run's steps finish, held or not. Working files and\n  every picture stay in this folder's own `runs/` and on Drive; media never\n  enters the record.\n- **Manifest per entry:** `run.json` — machine, brand, label, the steps on\n  file, `state` (`filed` or `held`) and, when held, why."}, {"doc": "image-teardown — a static ad in, its labels and brief out", "section": "Gates", "tool": "06-image-teardown", "path": "tools/06-image-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/06-image-teardown/CLAUDE.md#gates", "text": "Two of the five shared gates (`components/quality-checks`, via `hold()` →\n`check.json` in the run):\n\n- **elements** — after step 2b. Every label is a row in the element library or\n  an honest `none-fits` with a proposed row. An unknown id, a missing label or\n  an unreadable answer holds the run before the brand-side steps start.\n- **copy** — after step 6, on the brief. `unfilled_check`: no UNFILLED note\n  left in it. `price_check`: every price in the part that gets built (documents\n  one and two — document three quotes the source ad and the market) is a price\n  the brand's own offer-bank entry for that product sells today.\n\n**A held run says why**: printed when it happens, in `check.json`, and in the\nfiled `run.json`. A held run gets no brief id, so it does not travel on to\ndrafts. `python3 tools/gates.py <run>` prints it. `--gates warn` records the\nsame verdict and carries on — for when a person has read the hold and\noverruled it.\n\nAlso checked before anything runs (not one of the five, and older than them):\n`tools/preflight.py` — no brand named in code or prompts, every `{placeholder}`\nsupplied, every brand file present."}, {"doc": "image-teardown — a static ad in, its labels and brief out", "section": "Rules that bite here", "tool": "06-image-teardown", "path": "tools/06-image-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/06-image-teardown/CLAUDE.md#rules-that-bite-here", "text": "- **No brand in code or prompts.** Brand facts arrive from `brands/<brand>/`\n  via `--brand`. There is no default brand, avatar or product; a tool that\n  needs a brand reads it off the run (`vars/brand_name.md`) or stops and says so.\n- **Paths are found, not counted.** `tools/paths.py` walks up to the workspace;\n  nothing uses `parents[N]` to find it.\n- Tests: `python3 tests/test_image_teardown.py` — no network, no model."}, {"doc": "Image teardown — the static-ad twin of the video chain", "section": "Image teardown — the static-ad twin of the video chain", "tool": "06-image-teardown", "path": "tools/06-image-teardown/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/06-image-teardown/README.md", "text": "Same chain, one frame. Built 2026-08-19 from `stage1-teardown-v6-damon.md`\nafter Damon's call to start the teardown process on image ads.\n\n**Current shape (2026-09-20):** `CLAUDE.md` is the one-page definition — steps,\nthe element labels, the two gates, where runs file. `RUN.md` is how to run it\nfor a brand, including `--dry-run`. Tests: `python3 tests/test_image_teardown.py`.\nWhat follows is the history.\n\nRead `STAGE-DELTAS.md` first: it is the whole design in one page — what\nchanges at each stage and, more importantly, what does not.\n\n**The lane takes one image per run.** Brand asset, swipe file, creator\nsubmission — it makes no difference which. There is no set pass and no\ncarousel lane; the set-map prompt is parked in `prompts/parked/` and reads\nnothing, writes nothing, and is not part of the chain."}, {"doc": "Image teardown — the static-ad twin of the video chain", "section": "What is here", "tool": "06-image-teardown", "path": "tools/06-image-teardown/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/06-image-teardown/README.md#what-is-here", "text": "Path — What it is.\n**`DRAFT-STANDARD.md`** — **The ruling on how a swipe becomes our draft (Damon, 2026-09-17: \"do not deviate\"). One command: `tools/draft.py --brand <brand>`. Prompts: `prompts/stage7-*`, `prompts/stage8-*`**.\n`STAGE-DELTAS.md` — Stage-by-stage: what the image teardown changes from the video chain.\n`prompts/stage1-image-teardown-v1-damon.md` — One static in, one reproduction spec out.\n`prompts/video-doctrine-extract-v1.md` — A teaching video in, its operating doctrine out.\n`tools/gemini_image.py` — Image-in / text-out runner — the twin of `tools/gemini_breakdown.py`.\n`tools/compose.py` — Sets the type layer over a generated plate (ImageMagick).\n`tools/iterate.py` — **Retired 2026-09-13** — ran stage 5's job list against fal.ai. Pictures are made on Higgsfield; see `../image-production/PIPELINE.md`.\n`build_artifact.py` — Renders one run folder into `artifact.html`.\n`build_prompt_library.py` — Renders every prompt into `prompt-library.html`.\n`runs/<date>-<slug>/` — One folder per run: `assets/`, `out/`, `artifact.html`."}, {"doc": "Image teardown — the static-ad twin of the video chain", "section": "Why a separate runner", "tool": "06-image-teardown", "path": "tools/06-image-teardown/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/06-image-teardown/README.md#why-a-separate-runner", "text": "`tools/gemini_breakdown.py` runs an ffprobe audio pre-flight before every\ncall — the fix for the jackie-01 fabrication, where the model invented speech\nfor a silent file. Pointed at a PNG it finds no audio stream and injects\n\"this video file contains NO audio track\" into an image prompt. So images go\nthrough `gemini_image.py` instead: inline base64, no Files API, no pre-flight.\nMultiple `--image` flags go in one call, labelled IMAGE 1..n, which is what\nstage 1B reads."}, {"doc": "Image teardown — the static-ad twin of the video chain", "section": "Running it", "tool": "06-image-teardown", "path": "tools/06-image-teardown/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/06-image-teardown/README.md#running-it", "text": "```bash\ncd content-machine/image-teardown\nR=runs/2026-08-19-<brand>-thai-secret"}, {"doc": "Image teardown — the static-ad twin of the video chain", "section": "one teardown per ad", "tool": "06-image-teardown", "path": "tools/06-image-teardown/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/06-image-teardown/README.md#one-teardown-per-ad", "text": "python3 tools/gemini_image.py \\\n  --prompt-file prompts/stage1-image-teardown-v1-damon.md \\\n  --image $R/assets/ad-01_thai-secret-editorial-1x1.png \\\n  --model gemini-3.1-pro-preview \\\n  --out $R/out/01-teardown-ad-01.md"}, {"doc": "Image teardown — the static-ad twin of the video chain", "section": "then the chain, one stage at a time", "tool": "06-image-teardown", "path": "tools/06-image-teardown/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/06-image-teardown/README.md#then-the-chain-one-stage-at-a-time", "text": "./run-chain.sh $R 2      # replication spec\n./run-chain.sh $R 3      # injection\n./run-chain.sh $R 4a     # placement\n./run-chain.sh $R 4b     # headlines\n./run-chain.sh $R 4c     # support copy\n./run-chain.sh $R 4d     # offer block\n./run-chain.sh $R 5      # build sheet"}, {"doc": "Image teardown — the static-ad twin of the video chain", "section": "rebuild the read-through", "tool": "06-image-teardown", "path": "tools/06-image-teardown/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/06-image-teardown/README.md#rebuild-the-read-through", "text": "python3 build_artifact.py $R\n bash\nsips -Z 720 -s format jpeg -s formatOptions 72 in.png --out assets/web/in.jpg\n```"}, {"doc": "Image teardown — the static-ad twin of the video chain", "section": "Chain QC for stage 1 (image)", "tool": "06-image-teardown", "path": "tools/06-image-teardown/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/06-image-teardown/README.md#chain-qc-for-stage-1-image", "text": "Read against `prompts/reference/CHAIN-QC.md`; these replace its stage-1 block.\n\n- [ ] All four sections present: Objective Record, Frame Mechanics, Psychology, Index\n- [ ] Frame Spec covers ratio / palette with hex / treatment / generation tells\n- [ ] Subject & Setting profile covers who / wardrobe / setting / props / light and camera\n- [ ] Zone table has all five columns, one row per element, no merged zones\n- [ ] Every position is a percentage of frame — no \"upper area\"\n- [ ] Type & Treatment gives class, weight, case, size as % of height, color\n- [ ] Reading path numbered, with what the viewer understands after stop 1 alone\n- [ ] Scroll-stop mechanism names one element and what survives without it\n- [ ] `Breaks if cut:` present per block; `Belief shift:` present per play\n- [ ] `(inferred)` on every specific typeface name\n- [ ] `[UNCLEAR: …]` used where the frame is unresolvable — or its absence plausible\n- [ ] `[ARTIFACT: …]` used for platform chrome or watermarks if any exist\n- [ ] Observation only — no evaluation of brand or product\n- [ ] Element registry sized as % of frame, usable as a template spec"}, {"doc": "Image teardown — the static-ad twin of the video chain", "section": "Producing the ad", "tool": "06-image-teardown", "path": "tools/06-image-teardown/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/06-image-teardown/README.md#producing-the-ad", "text": "There is no designer and no design tool. The chain ends in two JSON blocks:\n\n```bash"}, {"doc": "Image teardown — the static-ad twin of the video chain", "section": "1. the plates — image models paint the picture, no words in it", "tool": "06-image-teardown", "path": "tools/06-image-teardown/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/06-image-teardown/README.md#1-the-plates--image-models-paint-the-picture-no-words-in-it", "text": "python3 tools/iterate.py --jobs $R/out/jobs.json \\\n  --out-dir $R/iterations --ref $R/assets/character-ref.png"}, {"doc": "Image teardown — the static-ad twin of the video chain", "section": "2. the type layer — set deterministically, so exact wording survives", "tool": "06-image-teardown", "path": "tools/06-image-teardown/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/06-image-teardown/README.md#2-the-type-layer--set-deterministically-so-exact-wording-survives", "text": "python3 tools/compose.py --plate $R/iterations/<plate>.png \\\n  --layer $R/out/type-layer.json --file 01 --out $R/finals/ad-01.png\n```\n\nStage 5 decides which text the model renders and which the compositor sets.\nShort and huge goes to the model; long, small, or character-exact goes to the\ncompositor. The guarantee always goes to the compositor."}, {"doc": "Image teardown — the static-ad twin of the video chain", "section": "Open", "tool": "06-image-teardown", "path": "tools/06-image-teardown/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/06-image-teardown/README.md#open", "text": "- Calibration testing across all stages — Damon's call, scheduled after the\n  prompts existed rather than before.\n- A per-brand character reference, so the same face holds across a whole set\n  without re-describing it."}, {"doc": "copy-teardown — a piece of words in, its labels and construct out", "section": "copy-teardown — a piece of words in, its labels and construct out", "tool": "07-copy-teardown", "path": "tools/07-copy-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/07-copy-teardown/CLAUDE.md", "text": "**copy-teardown** — takes **a piece of words only — ad primary text, a caption,\nan organic post, a headline set**, makes **its element labels and the copy's\nconstruct (the structure with the source's subject stripped out, ready for any\nbrand)**. Does not **write new copy, or take pictures, video, pages or emails —\nthose have their own teardowns**. Triggered by\n`python3 tools/run.py <source file or \"-\"> --brand <brand>`.\n\n**Owner:** Damon. **Group:** teardowns. Built 2026-09-20 on Damon's ruling —\n*\"copy would just be literally copy.\"* Its parent is copy production\n(`components/copywriter/`), whose steps 0–2 do this same work inside that\nchain; nothing there was touched (see \"Not wired into copy production\")."}, {"doc": "copy-teardown — a piece of words in, its labels and construct out", "section": "Steps", "tool": "07-copy-teardown", "path": "tools/07-copy-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/07-copy-teardown/CLAUDE.md#steps", "text": "Declared once, in `tools/run.py` `STEPS`. Built on `components/run-kit` `Chain`.\n\nStep — What it does — Runs on — Writes (`runs/copy-teardown/<brand>/<label>/`).\n— — the source as words, and the free scan — handles, hashtags, web addresses, prices, damage — code, no model — `source.md`, `strip.json` → `code_found`.\n`tear1` — the read — THE COPY AS READ, SOURCE NAMES and SOURCE DEFECTS kept apart — `reads` tier — `tear1--record.md`, `strip.json` → `record_says`.\n`tear2` — element labels — which library rows the copy IS — `checks` tier — `tear2--labels.md` → `elements.json`.\n`tear3` — the construct — brand-free, subject-free, from the record with every name withheld — `designs` tier — `tear3--construct.md`.\nEvery prompt is saved as sent (`<step>--sent.md`); `run.json` holds the\nassignment, the model and prompt hash per step, the labels and the state.\nModel ids are not written in this folder: a step names a tier and the id comes\nfrom `components/run-kit` `model.TIERS`. `--model` forces one for every step.\n\n**Dry run — spends nothing.** `--dry-run` resolves every prompt, fills it, shows\nthe model each step would run on and where the run would file, makes no model\ncall and leaves no file behind (it works in a temp folder that is removed).\nExit 1, with each problem named, if anything it needs is missing.\n\n**The source.** A `.md`/`.txt` file, or `-` for text piped in. A saved swipe card\nthat keeps its copy under `## Headline` / `## Primary text` headings (the\npaid-swipe cards do) has only those parts taken; the card's own notes — counts,\nlanding page, Drive path — are left behind."}, {"doc": "copy-teardown — a piece of words in, its labels and construct out", "section": "Steps", "tool": "07-copy-teardown", "path": "tools/07-copy-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/07-copy-teardown/CLAUDE.md#steps", "text": " `run.json` says how the source was\ntaken, and `source.md` in the run is exactly what the steps were shown.\n\n**The strip is done in code, not asked for.** After the read, the code takes the\nSOURCE NAMES section out of the record and swaps every name — the record's list\nplus what the scan found — for a numbered marker, `[SOURCE NAME n]`. The\nlabelling and construct steps never see a name, so they cannot carry one.\n\n**`--brand` is whose swipe it is, and nothing else.** It names the run folder\n(`runs/copy-teardown/<brand>/…`) and must be a real folder under `brands/`. No\nbrand file is read into any prompt: a construct is for any brand, and the brand\nenters later, at injection, in copy production. No brand is named in this\nfolder's code or prompts, and there is no default brand — a missing `--brand`\nis an error.\n\n**Element labels.** `format/copy`, `placement/all`, `framework/all`,\n`doctrine/awareness`, `delivery/register`, `delivery/humor` — the lists that\nhonestly apply to words alone. Left out on purpose: `structure/*` (the only\nstructure list is video beats), `style/*` and `template/*` (copy has no look),\nand `delivery/delivery_style`, `pacing`, `reference_world` (their rows are\nwritten about a person on camera). Every id is checked by\n`components/elements/machine/elements.py`. An id the library does not hold is\nREFUSED with the real ids named. `none-fits` with a one-line proposed row is a\nlegitimate answer: it is recorded in `elements.json` and `run.json` and is how\nthe library grows — this tool never writes to the library.\n\n**Answers are read the way a model writes them.**"}, {"doc": "copy-teardown — a piece of words in, its labels and construct out", "section": "Steps", "tool": "07-copy-teardown", "path": "tools/07-copy-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/07-copy-teardown/CLAUDE.md#steps", "text": " A slot's data block is\naccepted as a tagged fence, as the slot's heading over a plain or ```json\nfence, or as a bare JSON object; headings are accepted as `#`–`####` or bold,\nwith or without a colon (`tools/source_text.py` `block()`)."}, {"doc": "copy-teardown — a piece of words in, its labels and construct out", "section": "The four questions", "tool": "07-copy-teardown", "path": "tools/07-copy-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/07-copy-teardown/CLAUDE.md#the-four-questions", "text": "- **Engine + owner:** `tools/` (`run.py`, `source_text.py`, `elements_label.py`, `gates.py`, `paths.py`), Damon\n- **Definitions + curator:** `prompts/tear*-vN-damon.md` (one per step, highest `-vN-` wins; a changed prompt is a new file), Damon\n- **The plane it emits:** `runs/copy-teardown/<brand>/<label>/` at the repo root — the source as taken, every prompt as sent, every output, `elements.json`, `strip.json`, `run.json`, `check.json`\n- **Manifest per entry:** `run.json`"}, {"doc": "copy-teardown — a piece of words in, its labels and construct out", "section": "Gates", "tool": "07-copy-teardown", "path": "tools/07-copy-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/07-copy-teardown/CLAUDE.md#gates", "text": "Three of the five shared gates (`components/quality-checks`), in `tools/gates.py`.\nEach writes its result into the gate-keyed `check.json`; a failure HOLDS the run\n(exit 2) and says why.\n\n- **inputs** — before any model runs: the source is on file, not empty and\n  WORDS ONLY (a page, an email with pictures, a picture or a video is refused\n  and told which teardown it belongs to); the brand is a real folder; every\n  step has a prompt; every element list it labels against exists.\n- **elements** — after `tear2`: every label is a real library row, or an honest\n  `none-fits` carrying a proposed row. A held run does not go on to the construct.\n- **copy** — twice. After `tear1`: the record arrived in its labelled parts\n  (the three headings and a readable STRIP block). After `tear3`: the construct\n  carries none of the source's names (the record's list plus the handles,\n  hashtags and web addresses the code found), no brand that has a folder under\n  `brands/`, no `[SOURCE NAME n]` marker, no price the source named, no handle,\n  hashtag or web address of any kind, no source defect's broken form, no merge\n  tag as code, no UNFILLED note, has its MOVES / SEQUENCE LOGIC / LOAD-BEARING\n  parts, and ends on its LEFT OUT line.\n\nA one-word name is an ordinary English word half the time, so it is swept as\nwritten and Capitalised only; a several-word name is swept whatever its case.\nA name the record lists that the source does not hold is noted in\n`strip.json` (`listed_but_not_in_the_source`) and does not hold the run.\n\nAfter a hold: fix the prompt (as a new `-vN-` file) or the source, then"}, {"doc": "copy-teardown — a piece of words in, its labels and construct out", "section": "Gates", "tool": "07-copy-teardown", "path": "tools/07-copy-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/07-copy-teardown/CLAUDE.md#gates", "text": " run\nagain with `--rerun-from <step>`."}, {"doc": "copy-teardown — a piece of words in, its labels and construct out", "section": "Not wired into copy production", "tool": "07-copy-teardown", "path": "tools/07-copy-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/07-copy-teardown/CLAUDE.md#not-wired-into-copy-production", "text": "Copy production still reads its own sources (its stage 0, 1 and 2 prompts).\nThis tool is the proper home for that read; switching production over is a\nlater, side-by-side decision — nothing here touches it. What differs, and what\nthe side-by-side needs: `README.md`.\n\nTests: `python3 tests/test_copy_teardown.py` — stubbed model, temp workspace,\nnever the real `runs/` or `brands/`."}, {"doc": "Copy teardown", "section": "Copy teardown", "tool": "07-copy-teardown", "path": "tools/07-copy-teardown/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/07-copy-teardown/README.md", "text": "Built 2026-09-20. Video teardown takes a video apart, image teardown a picture,\nemail teardown an email. **This takes apart words** — ad primary text, a\ncaption, an organic post, a headline set — and nothing else. Damon: *\"copy\nwould just be literally copy.\"*\n\nIt does not write copy. What comes out is what the copy IS (its labels, from\nthe one element library) and how it argues (its construct — the structure with\nthe source's brand, product and subject stripped out, ready for any brand's\nmessage to be put in)."}, {"doc": "Copy teardown", "section": "One command", "tool": "07-copy-teardown", "path": "tools/07-copy-teardown/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/07-copy-teardown/README.md#one-command", "text": "Three steps: **read** (the copy, the source's own names and its defects, each\nunder its own heading) → **label** (format, placement, framework, awareness,\nregister, humor — asked of the element library, never invented) →\n**construct** (built from the record with every source name withheld). The run\nfiles to `runs/copy-teardown/<brand>/<label>/` at the repo root. `--dry-run`\nspends nothing and writes nothing. A real run is three model calls at most:\none read, one check, one design.\n\n`--brand` says whose swipe it is and whose run folder it files under. There is\nno default, and no brand file is read into a prompt."}, {"doc": "Copy teardown", "section": "What is in the folder", "tool": "07-copy-teardown", "path": "tools/07-copy-teardown/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/07-copy-teardown/README.md#what-is-in-the-folder", "text": "`tools/run.py` — the one entry.\n`tools/source_text.py` — the source as words · the free scan · tolerant reading of a model's answer · withholding names · what a construct may not carry.\n`tools/elements_label.py` — the candidate rows, and checking the labels against the library.\n`tools/gates.py` — the three gates — inputs, elements, copy.\n`tools/paths.py` — where things live (walks up; honours `AI_WORKSPACE`).\n`prompts/` — `tear1-copy-read`, `tear2-element-labels`, `tear3-construct` — `-vN-damon.md`, highest wins.\n`tests/` — `python3 tests/test_copy_teardown.py` — stubbed model, temp workspace.\nLook at a held run: `python3 tools/gates.py runs/copy-teardown/<brand>/<label>`.\nSee what the code finds in a source for free: `python3 tools/source_text.py scan <source>`."}, {"doc": "Copy teardown", "section": "Where it came from — copy production's steps 0–2", "tool": "07-copy-teardown", "path": "tools/07-copy-teardown/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/07-copy-teardown/README.md#where-it-came-from--copy-productions-steps-02", "text": "The prompts here are adapted from `components/copywriter/prompts/`\n`stage0-triage-v2`, `stage1-read-v2` and `stage2-spec-v2`. Nothing in that\nfolder was edited, and copy production still runs its own three steps.\n\nCopy production — Here — What changed.\nstage 0 triage — lane, format, voice, **avatar, funnel** — folded into `tear1` (lane, speaker, about, doing) and `tear2` (format) — avatar and funnel are about OUR reader — they are briefing, not teardown, and stay in copy production. The format is now a checked label, not a line of text.\nstage 1 read — beats, opening, turn, proof, close, voice, absences, awareness — `tear1` — same read, plus two new labelled parts: SOURCE NAMES and SOURCE DEFECTS, and a data block the code checks later steps against.\nstage 2 spec — the construct — `tear3` — same construct, but it is written from a record with every source name swapped for a marker, and a gate holds it if a name, a price or a defect came through. \"Medium-bound\" became \"placement-bound\" — the source here is always words.\n— — `tear2` — new: six element labels checked against the library."}, {"doc": "Copy teardown", "section": "What a side-by-side test needs before production is switched", "tool": "07-copy-teardown", "path": "tools/07-copy-teardown/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/07-copy-teardown/README.md#what-a-side-by-side-test-needs-before-production-is-switched", "text": "1. The same source through both: `copy.py` to the end of stage 2, and this\n   tool — then the two constructs read next to each other by Damon.\n2. A way for copy production to START from a filed teardown (read\n   `tear1--record.md` as its record and `tear3--construct.md` as its spec)\n   while still running its own stage 0 for avatar and funnel. That is a new\n   flag on `copy.py`, added beside the old path — not built here.\n3. Copy production's stage 0 hands `triage` to stages 1 and 2 (lane and voice\n   binding). This tool's record carries the lane and the speaker, but not the\n   avatar or funnel — so production's later stages still need its own triage.\n4. One check that the names being withheld does not cost the construct\n   anything production's injection step relied on."}, {"doc": "Copy teardown", "section": "Not built", "tool": "07-copy-teardown", "path": "tools/07-copy-teardown/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/07-copy-teardown/README.md#not-built", "text": "- No page or board. The run folder is the record; the lead decides where it shows.\n- No batch entry — one source per run. A loop over a folder of swipe cards is a\n  few lines on top of `run.main()`, and is better added after the first real run.\n- No words-only list exists for delivery style, pacing or reference world, so\n  those three are not labelled (see `CLAUDE.md`)."}, {"doc": "email-teardown — an email in, its labels and construct out", "section": "email-teardown — an email in, its labels and construct out", "tool": "08-email-teardown", "path": "tools/08-email-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/08-email-teardown/CLAUDE.md", "text": "**email-teardown** — takes **an email — words and pictures**, makes **its\nelement labels and the email's construct**. Does not **write an email**.\nTriggered by `python3 tools/run.py <source email> --brand <brand>`.\n\n**Owner:** Damon. **Group:** teardowns. History, and the older design-bank half\nof this folder (a Figma board in, a named format set out): `README.md`."}, {"doc": "email-teardown — an email in, its labels and construct out", "section": "Steps", "tool": "08-email-teardown", "path": "tools/08-email-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/08-email-teardown/CLAUDE.md#steps", "text": "Declared once, in `tools/run.py` `STEPS`. Built on `components/run-kit` `Chain`.\n\nStep — What it does — Runs on — Writes (`runs/email-teardown/<brand>/<label>/`).\n— — the free scan — furniture lines and damage the code can see — code, no model — `strip.json` → `code_found`.\n`tear1` — the read — THE MESSAGE, PAGE FURNITURE and SOURCE DEFECTS kept apart — `reads` tier — `tear1--record.md`, `strip.json` → `record_says`.\n`tear2` — element labels — which library rows the email IS — `checks` tier — `tear2--labels.md` → `elements.json`.\n`tear3` — the construct — brand-free, subject-free, from the message only — `designs` tier — `tear3--construct.md`.\nEvery prompt is saved as sent (`<step>--sent.md`); `run.json` holds the\nassignment, the model and prompt hash per step, the labels and the state.\nModel ids are not written in this folder: a step names a tier and the id comes\nfrom `components/run-kit` `model.TIERS`. `--model` forces one for every step.\n\n**Dry run — spends nothing.** `--dry-run` resolves every prompt, fills it, shows\nthe model each step would run on and where the run would file, makes no model\ncall and leaves no file behind (it works in a temp folder that is removed).\nExit 1, with each problem named, if anything it needs is missing.\n\n**The strip is done in code, not asked for.** After the read, the code takes the\nPAGE FURNITURE section out of the record; the labelling and construct steps are\nshown the furniture's item numbers only, never its words — a step cannot carry\nwhat it was never shown.\n\n**Brand facts come from the brand.** Which words are a brand's furniture liv"}, {"doc": "email-teardown — an email in, its labels and construct out", "section": "Steps", "tool": "08-email-teardown", "path": "tools/08-email-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/08-email-teardown/CLAUDE.md#steps", "text": "es\nin `brands/<brand>/email/simple.json` → `furniture`, read at run time by\n`tools/furniture.py`. No brand is named in this folder's code or prompts, and\nthere is no default brand — a missing `--brand` is an error.\n\n**Element labels.** `format/email`, `template/email`, `framework/all` (there is\nno structure list for email yet). Every id is checked by\n`components/elements/machine/elements.py`. An id the library does not hold is\nREFUSED with the real ids named. `none-fits` with a one-line proposed row is a\nlegitimate answer: it is recorded in `elements.json` and `run.json` and is how\nthe library grows — this tool never writes to the library."}, {"doc": "email-teardown — an email in, its labels and construct out", "section": "The four questions", "tool": "08-email-teardown", "path": "tools/08-email-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/08-email-teardown/CLAUDE.md#the-four-questions", "text": "- **Engine + owner:** `tools/` (`run.py`, `furniture.py`, `elements_label.py`, `gates.py`), Damon\n- **Definitions + curator:** `prompts/tear*-vN-damon.md` (one per step, highest `-vN-` wins; a changed prompt is a new file), Damon\n- **The plane it emits:** `runs/email-teardown/<brand>/<label>/` at the repo root — every prompt as sent, every output, `elements.json`, `strip.json`, `run.json`, `check.json`. `runs/` beside this code is history: readable, never written.\n- **Manifest per entry:** `run.json`"}, {"doc": "email-teardown — an email in, its labels and construct out", "section": "Gates", "tool": "08-email-teardown", "path": "tools/08-email-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/08-email-teardown/CLAUDE.md#gates", "text": "Three of the five shared gates (`components/quality-checks`), in `tools/gates.py`.\nEach writes its result into the gate-keyed `check.json`; a failure HOLDS the run\n(exit 2) and says why.\n\n- **inputs** — before any model runs: the source email is on file and not\n  empty, the brand is a real folder, every step has a prompt, every element\n  list it labels against exists.\n- **elements** — after `tear2`: every label is a real library row, or an honest\n  `none-fits` carrying a proposed row. A held run does not go on to the construct.\n- **copy** — twice. After `tear1`: the record arrived in its labelled parts\n  (the three headings and a readable STRIP block). After `tear3`: the construct\n  carries no page-furniture phrase (the record's own list plus the brand's), no\n  name for the sender's frame, no source defect's broken form, no merge tag as\n  code, no UNFILLED note, and ends on its LEFT OUT line.\n\nOne-word furniture entries (a category name, an icon's alt text) are exact alt\ntexts and ordinary English words anywhere else, so the copy gate sweeps phrases\nonly; one-word leaks are what the construct prompt's subject-free test is for.\n\nAfter a hold: fix the prompt (as a new `-vN-` file) or the source, then run\nagain with `--rerun-from <step>`."}, {"doc": "email-teardown — an email in, its labels and construct out", "section": "Not wired into email production", "tool": "08-email-teardown", "path": "tools/08-email-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/08-email-teardown/CLAUDE.md#not-wired-into-email-production", "text": "Email production still reads old emails itself (its own stage 1 and 2 prompts).\nThis tool is the proper home for that read; switching production over is a\nlater, side-by-side decision — nothing here touches it.\n\nTests: `python3 tests/test_email_teardown.py` — stubbed model, temp workspace,\nnever the real `runs/` or `brands/`."}, {"doc": "page-teardown — a saved landing page in, its labels and construct out", "section": "page-teardown — a saved landing page in, its labels and construct out", "tool": "09-page-teardown", "path": "tools/09-page-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/09-page-teardown/CLAUDE.md", "text": "**page-teardown** — takes **a landing page already saved to a file (an `.html`\nsave or a text capture)**, makes **its section-by-section read, its element\nlabels and the page's construct — the structure stripped of the source's\nsubject, names and figures, ready for any brand to be injected**. Does not\n**fetch a page, write a page, or build, lay out or deploy one**. Triggered by\n`python3 tools/run.py <saved page file> --brand <brand>`.\n\n**Owner:** Damon. **Group:** teardowns. Built 2026-09-20 on the shape of\n`email-teardown/`. Its parent is page production (`pages/`),\nwhose first stages do this same reading inside that chain — see \"Not wired into\npage production\" below."}, {"doc": "page-teardown — a saved landing page in, its labels and construct out", "section": "Steps", "tool": "09-page-teardown", "path": "tools/09-page-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/09-page-teardown/CLAUDE.md#steps", "text": "Declared once, in `tools/run.py` `STEPS`. Built on `components/run-kit` `Chain`.\n\nStep — What it does — Runs on — Writes (`runs/page-teardown/<brand>/<label>/`).\n— — the free reduce and scan — an `.html` save becomes readable numbered lines (scripts, styles and tracking noise dropped; a second phone-sized copy of the whole page dropped); menu, footer, cookie bar, pop-up and legal lines flagged; damage flagged; the page classifier's word for the format — code, no model — `source--as-read.md`, `strip.json` → `code_found`.\n`tear1` — the read — SECTIONS AS READ (`S1`, `S2`, …), PAGE FURNITURE (`F1`…) and SOURCE DEFECTS (`D1`…) kept apart, plus a STRIP block of furniture words, defects, names and figures — `reads` tier — `tear1--record.md`, `strip.json` → `record_says`.\n`tear2` — element labels — the page's format, its framework, and one section label per numbered section — `checks` tier — `tear2--labels.md` → `elements.json`.\n`tear3` — the construct — name-free, figure-free, subject-free, from the sections only — `designs` tier — `tear3--construct.md`.\nEvery prompt is saved as sent (`<step>--sent.md`); `run.json` holds the\nassignment (the saved file, where it was saved from, sizes before and after the\nreduce), the model and prompt hash per step, the labels and the state. Model\nids are not written in this folder: a step names a tier and the id comes from\n`components/run-kit` `model.TIERS`. A prompt past run-kit's small-window size\nsteps up to the `designs` model on its own — pages are big. `--model` forces\none model for every step.\n\n**Dry run — spends nothing.** `--dry-run` redu"}, {"doc": "page-teardown — a saved landing page in, its labels and construct out", "section": "Steps", "tool": "09-page-teardown", "path": "tools/09-page-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/09-page-teardown/CLAUDE.md#steps", "text": "ces the page, resolves and fills\nevery prompt, shows sizes, the model each step would run on and where the run\nwould file, makes no model call and leaves no file behind (it works in a temp\nfolder that is removed). Exit 1, with each problem named, if anything it needs\nis missing.\n\n**It does not fetch.** `--source-url` is recorded and used to know the seller's\nname; nothing is ever requested from it. Save the page first.\n\n**The strip is done in code, not asked for.** After the read, the code takes the\nPAGE FURNITURE section out of the record; the labelling and construct steps are\nshown the furniture's item numbers only, never its words — a step cannot carry\nwhat it was never shown.\n\n**A model's formatting is not its answer.** Every slot is read through\n`tools/slots.py`: `# STRIP` over a plain or ```json fence, `**STRIP**` over a\nbare object, a heading in any case or size — all the same answer.\n\n**No brand in this folder.** `--brand` is required; there is no default. The\nbrand's own names (checked against the construct, in case the page torn down is\none of its own) are read at run time from `brands/<brand>/products/store.json`\nwhen the brand keeps one.\n\n**Element labels.** `format/page` (one) · `framework/page` if the library ever\nholds one, else `framework/all` (one) · `doctrine/section` (one per numbered\nsection). Every id is checked by `components/elements/machine/elements.py`. An\nid the library does not hold is REFUSED with the real ids named. `none-fits`\nwith a one-line proposed row is a legitimate answer: it is recorded in\n`elements.json` and `run.json` and is how the li"}, {"doc": "page-teardown — a saved landing page in, its labels and construct out", "section": "Steps", "tool": "09-page-teardown", "path": "tools/09-page-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/09-page-teardown/CLAUDE.md#steps", "text": "brary grows — this tool never\nwrites to the library. The page classifier (`components/naming/classify_pages.py`,\ncalled, never edited) gives a free second opinion on the format; it is handed\nto the labelling step and recorded beside the label (`format_agrees_with_code`),\nand it holds nothing."}, {"doc": "page-teardown — a saved landing page in, its labels and construct out", "section": "The four questions", "tool": "09-page-teardown", "path": "tools/09-page-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/09-page-teardown/CLAUDE.md#the-four-questions", "text": "- **Engine + owner:** `tools/` (`run.py`, `furniture.py`, `elements_label.py`, `gates.py`, `slots.py`, `paths.py`), Damon\n- **Definitions + curator:** `prompts/tear*-vN-damon.md` (one per step, highest `-vN-` wins; a changed prompt is a new file), Damon\n- **The plane it emits:** `runs/page-teardown/<brand>/<label>/` at the repo root — the page as read, every prompt as sent, every output, `elements.json`, `strip.json`, `run.json`, `check.json`\n- **Manifest per entry:** `run.json`"}, {"doc": "page-teardown — a saved landing page in, its labels and construct out", "section": "Gates", "tool": "09-page-teardown", "path": "tools/09-page-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/09-page-teardown/CLAUDE.md#gates", "text": "Three of the five shared gates (`components/quality-checks`), in `tools/gates.py`.\nEach writes its result into the gate-keyed `check.json`; a failure HOLDS the run\n(exit 2) and says why.\n\n- **inputs** — before any model runs: the saved page is on file and, once\n  reduced, still has words in it (a shell that fills itself by script is named\n  as one); the brand is a real folder; every step has a prompt; the element\n  lists it labels against exist.\n- **elements** — after `tear2`: the format, the framework and every numbered\n  section carry a real library row, or an honest `none-fits` with a proposed\n  row. A section the record numbers and the answer skipped holds the run. A\n  held run does not go on to the construct.\n- **copy** — twice. After `tear1`: the record arrived in its labelled parts\n  (the three headings, a readable STRIP block, numbered sections). After\n  `tear3`, the construct carries:\n  - **no money figure at all** — any `$`, `£`, `€` amount holds it, the\n\n  - none of the source's own figures (the percentages and counts the read\n\n  - none of the source's names — every seller, product, person, publication and\n\n  - no page-furniture phrase, no name for the site's frame (menu, footer,\n\n  - its THE MOVES, THE SEQUENCE LOGIC and LOAD-BEARING parts, ending on its\n\nOne-word furniture entries are ordinary English words anywhere else, so the\ngate sweeps furniture phrases only. A one-word name the read listed is matched\nas a proper noun (as written, Capitalised, CAPITALS) and left alone in lower\ncase, because a product's name is often an ordinary word.\n\nAfter a hold: fix the"}, {"doc": "page-teardown — a saved landing page in, its labels and construct out", "section": "Gates", "tool": "09-page-teardown", "path": "tools/09-page-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/09-page-teardown/CLAUDE.md#gates", "text": " prompt (as a new `-vN-` file) or the saved page, then run\nagain with `--rerun-from <step>`. A finished step is reused as it stands, so a\nplain re-run of a held construct holds again."}, {"doc": "page-teardown — a saved landing page in, its labels and construct out", "section": "Not wired into page production", "tool": "09-page-teardown", "path": "tools/09-page-teardown/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/09-page-teardown/CLAUDE.md#not-wired-into-page-production", "text": "`pages/` (`machine/run.py` stages 0–2: triage, read, spec) does this\nreading inside its own chain today, from a `page.txt` capture. **This tool does\nnot touch it and it does not call this tool.** The prompts here were adapted\nfrom those three into new files; nothing in `pages/` was edited, moved\nor rewired. Pointing page production at this teardown's record and construct is\na later decision, made from a side-by-side run on the same page — see\n`README.md`.\n\nTests: `python3 tests/test_page_teardown.py` — stdlib, stubbed model, temp\nworkspace; the real `runs/` and `brands/` are never written."}, {"doc": "Page teardown", "section": "Page teardown", "tool": "09-page-teardown", "path": "tools/09-page-teardown/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/09-page-teardown/README.md", "text": "One of the five teardowns (video · image · copy · page · email). A landing page\ngoes in; what comes out is the page taken apart: what each section does, which\nlibrary rows the page is, and its construct — the argument's mechanics with the\nseller, the product, the figures and the subject all stripped out, so any brand\ncan be injected into it later.\n\nIt reads a page that is **already saved to a file**. It never fetches one, never\nwrites a page, and never builds, lays out or deploys one. Steps, gates and where\neverything lands: `CLAUDE.md`."}, {"doc": "Page teardown", "section": "One command", "tool": "09-page-teardown", "path": "tools/09-page-teardown/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/09-page-teardown/README.md#one-command", "text": "- `<saved page file>` — an `.html` save of the page, or a `.txt`/`.md` text\n  capture (the `page.html` / `page.txt` pairs page production already keeps\n  under a run's `source/` folder both work).\n- `--brand` — required, no default. Where the run files\n  (`runs/page-teardown/<brand>/<label>/`) and whose own names the construct is\n  checked against.\n- `--source-url` — where the page was saved from. Recorded, and used to know\n  the seller's name. Never opened.\n- `--label` — defaults to the saved file's folder name when the file is called\n  `page.*` or `index.*`, otherwise the file's own name.\n- `--dry-run` — free. Shows the page's size before and after the reduce, what\n  the code flagged, the model each step would use and where it would file.\n  Calls no model, writes nothing."}, {"doc": "Page teardown", "section": "What happens, in order", "tool": "09-page-teardown", "path": "tools/09-page-teardown/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/09-page-teardown/README.md#what-happens-in-order", "text": "Who does it — What it costs.\nThe page is reduced to readable, numbered lines: scripts, styles and tracking dropped; headlines, pictures, buttons and links kept in page order; a second phone-sized copy of the whole page dropped — code — nothing.\nMenu, footer, cookie bar, pop-up and legal lines are flagged; template tags and filler showing as text are flagged; the page classifier says what format it reads — code — nothing.\n**tear1 — the read**: sections `S1…` quoted exactly, page furniture `F1…`, source defects `D1…`, each under its own heading, plus the names and figures the page carries — the `reads` model — 1 call.\nThe furniture section is cut out of the record before anyone else sees it — code — nothing.\n**tear2 — the labels**: format, framework, and one section label per `S`, each checked against the element library — an id the library does not hold is refused — the `checks` model — 1 call.\n**tear3 — the construct**: the moves, the sequence logic, proportion, the voice rules, what is load-bearing, what is page-bound — the `designs` model — 1 call.\nGates: inputs · elements · copy (no names, **no money figure at all**, no furniture, no defect, no UNFILLED) — code — nothing.\nA real run is **3 model calls**. A finished step is reused on the next run; a\nchanged saved page is read again from the top."}, {"doc": "Page teardown", "section": "Prompts", "tool": "09-page-teardown", "path": "tools/09-page-teardown/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/09-page-teardown/README.md#prompts", "text": "Step — File.\ntear1 — `prompts/tear1-page-read-v1-damon.md`.\ntear2 — `prompts/tear2-element-labels-v1-damon.md`.\ntear3 — `prompts/tear3-construct-v1-damon.md`.\nEach opens on the invented-example guard, names no brand and no product\ncategory, and asks for its answer in labelled slots. A changed prompt is a new\n`-v2-` file beside the old one; the highest number runs."}, {"doc": "Page teardown", "section": "Where this sits beside page production", "tool": "09-page-teardown", "path": "tools/09-page-teardown/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/09-page-teardown/README.md#where-this-sits-beside-page-production", "text": "Page production (`pages/`) reads a source page in its own first three\nstages and then goes on to inject, close, vary, brief and lay out. This tool is\nthat reading, stood up on its own, the same way every teardown is built.\n\nPage production today — Here.\nstage 0 triage — page job, voice, funnel, length; binds the run's avatar, angle and hand-off page — folded into `tear1`'s THE PAGE AT A GLANCE — the source's facts only; nothing about our avatar, angle or hand-off, which belong to production.\nstage 1 read — sections, headline, turn, proof, asks, voice, absences — `tear1` — the same record, plus numbered sections, furniture and defects kept apart, and a STRIP block the gates check against.\nstage 2 spec — the brand-free construct — `tear3` — the same parts, plus: no figures at all, never shown the furniture, `from S…` on every move, a LEFT OUT line, and a copy gate that holds a leak.\nformat from the classifier, looked up in `format/page` — `tear2` — the format labelled from the record against `format/page`, with the classifier's word recorded beside it; plus a framework and a section label per section, which production does not label today.\nreads a `page.txt` capture with menu and footer still in it — reads the `.html` save (or the same `.txt`), reduced and flagged in code first.\nNothing in `pages/` was changed. **A side-by-side test** before anyone\npoints production at this: run both on the same saved page, then compare (1)\nproduction's `stage2--spec.md` against `tear3--construct.md` move for move;\n(2) whether production's stage 3 injection, handed this construct in place of\n"}, {"doc": "Page teardown", "section": "Where this sits beside page production", "tool": "09-page-teardown", "path": "tools/09-page-teardown/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/09-page-teardown/README.md#where-this-sits-beside-page-production", "text": "its own spec, fills every slot; (3) the classifier's format against `tear2`'s\nlabel; (4) cost — production spends three calls (checks, checks, designs) on its\nreading; this spends three (reads, checks, designs)."}, {"doc": "Page teardown", "section": "Tests", "tool": "09-page-teardown", "path": "tools/09-page-teardown/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/09-page-teardown/README.md#tests", "text": "Standard library only, the model stubbed, everything in a temp workspace."}, {"doc": "image-production — a static brief in, finished pictures out", "section": "image-production — a static brief in, finished pictures out", "tool": "10-image-production", "path": "tools/10-image-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/10-image-production/CLAUDE.md", "text": "**image-production** — takes **a static brief** (a `batch.json`, or a teardown's\n`06-brief.md`), makes **the finished pictures, judged and named**. Does not\n**write the words**. Triggered by `python3 run.py runs/<brand>/<batch>` (a batch)\nor `python3 machine.py <swipe> --brand <brand> --avatar <avatar> --product <product>`\n(swipes, through their teardown steps and on into the build).\n\n**Owner:** Damon. **Group:** production. Why each part exists, and every failure\nit was built after: `README.md` (the eight steps of a build), `PIPELINE.md` (the\nbatch pipeline, ruled 2026-09-05), `PROMPT-SPEC.md` (prompts are slots, never\nprose). Damon never runs any of this — a session runs it and hands him a link."}, {"doc": "image-production — a static brief in, finished pictures out", "section": "Steps", "tool": "10-image-production", "path": "tools/10-image-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/10-image-production/CLAUDE.md#steps", "text": "Two ways in, one tail. The entry the board scores is `machine.py`.\n\n**A batch** — `run.py runs/<brand>/<batch>` (`PIPELINE.md`):\n\nStep — What it does — Runs on — Writes (in the batch folder).\ngates — **elements** and **copy**, before anything is made — code — `elements.json`, `check.json`.\nplates · prepare — the prompt for each ad, exactly as it will be sent, and the model it routes to — code (`tools/plates.py`) — `inbox/jobs.json`.\nplates · generate — the pictures — a session, on the Higgsfield MCP — there is no unattended path — —.\nplates · ingest — pads each 4:5 frame to 9:16 — code — `inbox/<slug>.png`.\nfinish — measured checks, then the judge, then the offer bar alone — code + a vision model (`tools/finish.py`) — `verdicts.json`, `rejected/<slug>-why.md`.\n**media gate** — a picture the judge failed is HELD from delivery, with the reason — code (`tools/gates.py`) — `check.json`.\nname · record · deliver — the 13-field name, the `made` record, Drive through the API — `components/naming`, `components/gdrive-creator` — `ads/<slug>/manifest.json`, `report.md`.\nfile — the batch's record to the repo-root `runs/` — code — `runs/image-production/<brand>/<batch>/`.\n**A swipe** — `machine.py <swipe> …` walks the swipe's teardown steps 2 → 6\n(`../image-teardown/tools/run.py`) and then `tools/run_production.py`: elements\ngate on the brief → plates → `tools/judge.py` → media gate → `tools/render.py`\n(draft · finish · type) → deliver → file.\n\n**Dry run — spends nothing.** `machine.py --dry-run <batch-folder | swipe …>`\n(or `run.py <batch-folder> --dry-run`) resolves, per concept: the "}, {"doc": "image-production — a static brief in, finished pictures out", "section": "Steps", "tool": "10-image-production", "path": "tools/10-image-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/10-image-production/CLAUDE.md#steps", "text": "`batch.json`,\nthe brand's folder, the template, the style pack, the picture format, the\nprompt as it would be sent and the model it routes to, the product elements it\nreferences and what the brand says is true of them, the packshot, the logo, the\npalette, the fonts, the roster, the offer bank — OK or MISSING — then what the\nelements and copy gates would say and where the record and the pictures would\ngo. No model, no generator, no judge; nothing written to the batch, to `runs/`\nor to Drive. Exit 1 if anything required is missing. For a swipe it also runs\nthe teardown steps' own dry run. **`tools/finish.py --dry-run` is NOT this** — it\nstill calls the judge; it only skips filing and delivery.\n\n**Elements.** Before anything is made, every id a batch or brief names is asked\nof `components/elements/machine/elements.py`:\n\nNamed by — Asked of.\n`template` (ad, else batch; a brief's `template`) — `prompted` means no layout file — `template/image` (built from `templates/*.json`).\n`pack` / `style_pack` (ad, else batch, else `slots/<slug>.json`) — `style/image` (built from `style-packs.json`).\n`picture_format` (ad, else batch) — `format/image` (built from `format-bank.json` — this is its first reader).\nAn unknown id is **refused, naming the real ids**. A draft row whose `what`\nstarts `[TO DEFINE` is refused as **named but not defined yet**. What was picked\nis recorded in the run's `elements.json`. The ad's `format` field is NOT a\npicture format — it is the ad name's own vocabulary (`components/naming`) and is\nleft alone.\n\n**The brand is never a default.** Packshot, logo, colours, offe"}, {"doc": "image-production — a static brief in, finished pictures out", "section": "Steps", "tool": "10-image-production", "path": "tools/10-image-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/10-image-production/CLAUDE.md#steps", "text": "r bank, roster\nand the judge's product tests are read from `brands/<brand>/` for the brand the\nbatch names (`batch.json` `brand`), `--brand`, or — for a swipe — the brand\nteardown recorded (`vars/brand_name.md`). With none of those, the tool stops\nand says so. The judge's product tests are built from\n`brands/<brand>/element-facts.json` (`tools/brand_facts.py`); a brand with no\nfacts on file gets one plain test and a note saying so."}, {"doc": "image-production — a static brief in, finished pictures out", "section": "The four questions", "tool": "10-image-production", "path": "tools/10-image-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/10-image-production/CLAUDE.md#the-four-questions", "text": "- **Engine + owner:** `machine.py`, `run.py` + `tools/` (`paths.py` every path,\n  `plates.py` the jobs, `prompt.py` the slots, `finish.py` and `judge.py` the\n  judges, `render.py` the compositor, `gates.py` the gates and the filing,\n  `dryrun.py` the dry run, `brand_facts.py` the brand's product truth). Damon.\n- **Definitions + curator:** `templates/*.json` (hand-tuned layouts),\n  `style-packs.json` (eight rendering registers + the result budget),\n  `format-bank.json` (25 picture shapes). The judge's standing tests live in\n  `tools/finish.py` and `tools/judge.py`. This tool keeps no `prompts/` folder:\n  a batch's prompts are its own, saved verbatim in `runs/<brand>/<batch>/prompts/`.\n  Damon.\n- **The plane it emits:** `runs/image-production/<brand>/<batch>/` at the repo\n  root — `batch.json`, `jobs.json`, `prompts-as-sent/<slug>.txt`,\n  `manifests/<slug>.json`, `verdicts.json`, `rejected/*-why.md`, `elements.json`,\n  `check.json`, `report.md`, `run.json`. **Text only — never a picture.**\n  Working files stay in this folder's `runs/<brand>/<batch>/`; pictures go to\n  Drive (`batch.json` `drive_root`, through the API). Filing never stops a\n  delivery: a failure prints `SKIP` and the reason.\n- **Manifest per entry:** `run.json` — machine, brand, label, the files on\n  record, `state` (`filed` or `held`) and, when held, why."}, {"doc": "image-production — a static brief in, finished pictures out", "section": "Gates", "tool": "10-image-production", "path": "tools/10-image-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/10-image-production/CLAUDE.md#gates", "text": "Three of the five shared gates (`components/quality-checks`, via `hold()` →\n`check.json` in the batch folder). `python3 tools/gates.py <batch-folder>` prints\nit. `run.py … --gates warn` records the same verdict and carries on.\n\n- **elements** — before anything is made. Every template, style pack and\n  picture format named is a real, defined row in the element library.\n- **copy** — before anything is made, on the words burned into the picture\n  (`offer.bar_text` and each ad's `copy` slots). `unfilled_check`: no UNFILLED\n  note. `price_check`: every price is one the brand's offer bank sells today —\n  the entry under `offer.id` when the bank has one, else every price in the bank.\n- **media** — after the judge. A picture the judge failed is HELD from\n  delivery, named with the judge's reason. **The gate does not judge** — the\n  verdicts are `finish.py`'s and `judge.py`'s, unchanged. Survivors still ship;\n  holding one picture never holds its siblings. A hold is a work item, not a\n  verdict (`PIPELINE.md` §4b): fix the prompt, never the picture.\n\nTests: `python3 tests/test_image_production.py` — stdlib, no network, no model."}, {"doc": "image-production — a static brief in, finished pictures out", "section": "Known and not fixed here", "tool": "10-image-production", "path": "tools/10-image-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/10-image-production/CLAUDE.md#known-and-not-fixed-here", "text": "- `queue/` (the RUN-01 production queue) still names one brand throughout and\n  counts its way to the workspace root. It is one brand's queue, not the tool.\n- `tools/finish.py` `add_asset()` writes one run's Drive folder name into\n  `_drive-ids.json` paths.\n- `PRODUCTION-BRIEF.md` is a dated test plan; its run command is the retired\n  `batch.py`."}, {"doc": "Stage two — brief to finished ad", "section": "Stage two — brief to finished ad", "tool": "10-image-production", "path": "tools/10-image-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/10-image-production/README.md", "text": "> The short version — what it takes, the steps, the gates, where a run files,\n> the dry run — is `CLAUDE.md`. This file is the long version: the eight steps\n> of a build and the failure each one was written after.\n\nStage one ends at a brief. This is everything after it.\n\n**It is not one generation call.** Eight steps, each with its own output and its\nown way of failing. Written out because the failures below were all found the\nhard way, by doing it by hand, and every one of them is invisible if you think\nof this as \"generate the image\".\n\n**The brief is the only input.** Not the run folder, not the stage outputs, not\nsomebody's memory of what we decided. If a fact is not in the brief, it does not\nreach the ad — that is what a brief is *for*, and it is the whole reason stage\none exists as a separate thing.\n\n---"}, {"doc": "Stage two — brief to finished ad", "section": "1 · Read the brief", "tool": "10-image-production", "path": "tools/10-image-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/10-image-production/README.md#1--read-the-brief", "text": "Take from it, and nothing else:\n\n- **the plates to make** — the control, then each picture variation\n- **the layout data** — every text element with its position, weight, size,\n  colour, tracking, measure and line count, and per-fragment colours where a\n  line carries two\n- **what is composited rather than generated**, and the file each comes from\n- **the accept tests** — the three to five checkable things\n- **the negative constraints** — what must not appear\n\nIf the brief names no plates, stop. A missing plate list is a stage-one defect\nand it is cheaper to fix there."}, {"doc": "Stage two — brief to finished ad", "section": "2 · Make the plate", "tool": "10-image-production", "path": "tools/10-image-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/10-image-production/README.md#2--make-the-plate", "text": "The photograph, **with no words in it at all**.\n\n**The source ad is attached as the layout reference.** Measured across six\nrebuilds, 2026-08-26: subject, wardrobe, props, action, environment, light,\ngrade, texture, shot size, height, angle and lens all survive being described in\nwords. **Where the camera stands does not** — it came back wrong every time,\nincluding when stated plainly and repeated in the negatives. The reference is\nhow the frame is held; the words carry everything inside it.\n\nOne plate per picture the brief names. The control first — it is the baseline\nevery variation is a bet against.\n\n**On Higgsfield, and nowhere else** (ruled 2026-09-05, restated 2026-09-13).\nIt is the only place that holds the roster identities and `flex-360-true`, the\nproduct built from seven real turntable angles — and a picture is only as good\nas its references. Generated at 4:5 and padded to 9:16 afterwards; the cast and\nthe product go in as Element ids, never as words.\n\n**Two models, not one** (2026-09-14). Generating a photoreal PERSON and\npainting everything else are different jobs, so they route to different models:\n\nThe frame — Model — Why.\nhas a person in it (`cast` is set on the ad) — `gpt_image_2` — built for human realism.\nhas no person — product, surface, scene — `nano_banana_pro` — quality, text and diagrams.\n`plates.py prepare` decides this per job and writes the reason into\n`jobs.json` beside it. An ad that names its own `model` still wins, and so\ndoes a batch that names one — a measured exception outranks a default.\n\n**Every batch.json written before today carries `\"mo"}, {"doc": "Stage two — brief to finished ad", "section": "2 · Make the plate", "tool": "10-image-production", "path": "tools/10-image-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/10-image-production/README.md#2--make-the-plate", "text": "del\": \"nano_banana_pro\"`\nat batch level, which suppresses the routing.** Drop that line from a spec to\nlet it route, or run the split as a test first:\n\n```bash"}, {"doc": "Stage two — brief to finished ad", "section": "every plate both ways, slug@model, same prompt and same refs", "tool": "10-image-production", "path": "tools/10-image-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/10-image-production/README.md#every-plate-both-ways-slugmodel-same-prompt-and-same-refs", "text": "python3 tools/plates.py prepare --run runs/<brand>/<batch> --ab nano_banana_pro\n```\n\nThe only thing a side-by-side off that can be showing you is the model.\n\nThe fal path is retired. `generate.py`, `iterate.py` and `batch.py` all stop on\ntheir first line, and the client is parked under `tools/superseded/fal/` so an\nold run's records still read."}, {"doc": "Stage two — brief to finished ad", "section": "3 · Judge the plate before anything is built on it", "tool": "10-image-production", "path": "tools/10-image-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/10-image-production/README.md#3--judge-the-plate-before-anything-is-built-on-it", "text": "The brief wrote three to five checkable things. Run them now, on the plate\nalone, before a single character of type goes near it.\n\n- Does the load-bearing action read correctly?\n- Are hands and anatomy coherent?\n- Is every band the brief declared empty actually empty?\n- **A legible third-party brand anywhere in frame is an automatic reject.**\n  Always. Every time.\n- **Is the result believable?** If the frame shows an after state, a treated\n  area or a transformation, it has a **credibility budget** and blowing it is a\n  reject: the improvement is partial (~60-70%, never cleared), 3-5 of the\n  original marks survive, freckles and texture are preserved, the same person\n  in the same light, no glow and no plastic skin. *If the treated area looks\n  like a brochure, that is a fail.*\n\n  The budget lives once, in `style-packs.json` under `_result_budget`:\n  `prompt.py` appends it to `anchors` and `negatives` for any slot file with\n  `\"result\": true`, and `judge.py` checks the same list off the picture that\n  comes back. A brief can tighten it with a `**Result budget**` block.\n\n  Added 2026-09-14. Our texture slot already held skin truth; nothing held the\n  CLAIM, so a plate that cleared the skin completely passed every test we ran.\n  That is a believable machine shipping an unbelievable ad.\n\nA plate that fails gets regenerated or killed. It never gets composited \"to see\nhow it looks\" — that is how a bad picture acquires enough sunk cost to ship."}, {"doc": "Stage two — brief to finished ad", "section": "4 · Measure the plate that actually came back", "tool": "10-image-production", "path": "tools/10-image-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/10-image-production/README.md#4--measure-the-plate-that-actually-came-back", "text": "**The step everyone skips, and the one that broke the first finished ad.**\n\nThe source's black band began at 60% of frame height. The generated plate's\nbegan at 63.9%, and its product inset sat lower than the source's. Carrying the\nsource's percentages across verbatim put the headline underneath the product\ncircle.\n\nSo: measure the real plate. Where does the empty band actually start? Where did\nthe inset ring actually land? Then **adapt the positions to this plate while\nholding the layout's shape** — same axis, same order, same block proportions,\nsame gutters. The shape is the format. The numbers belong to the picture that\ngot made."}, {"doc": "Stage two — brief to finished ad", "section": "4b · What gets generated, and what gets drawn", "tool": "10-image-production", "path": "tools/10-image-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/10-image-production/README.md#4b--what-gets-generated-and-what-gets-drawn", "text": "**The plate is a photograph and nothing else.** Every graphic element on top of\nit is drawn by the compositor, at full precision, from real assets. Getting\nthis line wrong is what makes an ad look almost-right in a way nobody can name.\n\n**Generate** — the things a camera would have captured:\nsubject · skin, texture, condition · wardrobe · setting · lighting · grade ·\ndepth of field · the whole photographic feel.\n\n**Draw or composite** — the things a designer would have made:\n\nElement — Why never generated.\n**Type** — Exact characters, real typeface, real weight. A model approximates letterforms..\n**The logo** — A wordmark is either right or it is wrong. There is no close..\n**The product** — It carries printing and a crest. Composited from the packshot..\n**Boxes, squares, rules, frames** — A generated rectangle has soft, wandering edges and an off-square corner. A drawn one is crisp at any size..\n**Stickers and their outlines** — The white stroke around a cut-out product is a uniform offset. Generated, it varies in width and eats the edges..\n**Badges, bars, price blocks** — Flat fills with exact hex values..\n**The tell is edges.** Anything with a hard edge, an exact colour or a\ncharacter in it belongs to the compositor. Anything with light falling across\nit belongs to the camera. A format that reads as sharp — a stroked square over\na soft macro, a flat white box against skin — is exactly that contrast, and it\nonly works if the sharp half is genuinely sharp.\n\n**And the joins are the craft.** In the format we swiped, the headline box is\n*flush* against the bottom edge of the t"}, {"doc": "Stage two — brief to finished ad", "section": "4b · What gets generated, and what gets drawn", "tool": "10-image-production", "path": "tools/10-image-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/10-image-production/README.md#4b--what-gets-generated-and-what-gets-drawn", "text": "arget square and shares its left edge —\none shape reading as two. A half-pixel gap or a misaligned edge is the\ndifference between designed and assembled."}, {"doc": "Stage two — brief to finished ad", "section": "5 · Composite the product", "tool": "10-image-production", "path": "tools/10-image-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/10-image-production/README.md#5--composite-the-product", "text": "From the brand's own packshot, cut out, into its container.\n\n**Never generated.** The tube carries a wordmark and a crest; a model\napproximates both, and a near-miss on a brand mark is worse than no product in\nframe. Same reasoning as the type.\n\nMatch the container fill to the packshot's own backdrop and feather the join, or\ncut the product out properly — a rectangle of one grey sitting inside a circle\nof another grey reads as a mistake even when nobody can say why."}, {"doc": "Stage two — brief to finished ad", "section": "6 · Set the type", "tool": "10-image-production", "path": "tools/10-image-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/10-image-production/README.md#6--set-the-type", "text": "From the layout data, by the compositor, never by the image model.\n\nEvery character exact. Weights come from the data — **a weight is a different\ntypeface file, not a flag**, and a bold headline recorded as prose rebuilds in\nregular with every word correct and the ad still wrong. Per-fragment colours and\npanels where a line carries two: a two-tone badge is one line of two segments,\neach with its own fill, never one box on the element.\n\nThe compositor reports every step-down it had to make. A headline that dropped\nfrom 96pt to 61pt to fit is telling you the copy is too long for the space,\nwhich is a copy decision and not a layout one."}, {"doc": "Stage two — brief to finished ad", "section": "7 · Prove it, and fix it before it leaves", "tool": "10-image-production", "path": "tools/10-image-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/10-image-production/README.md#7--prove-it-and-fix-it-before-it-leaves", "text": "**A known flaw is never delivered with a note attached.** Damon, 2026-08-31:\n*\"if you see the problem, then don't fucking deliver it.\"* Handing over a\nbroken ad and describing what's broken moves the work of noticing onto the\nperson who asked, which is the one thing this machine exists to take off them.\n\nIf a check fails, the fix happens here. If the fix needs a regeneration, it\ngets regenerated. The ad leaves when it is right, not when it is explained.\n\nSide by side with the source ad, at the same height.\n\nPlus the compositor's notes: every size step-down, every keep-clear overlap,\nevery measure it had to fight. A finished ad with three warnings behind it is\nnot finished — it is a draft that rendered."}, {"doc": "Stage two — brief to finished ad", "section": "8 · Then multiply, cheaply", "tool": "10-image-production", "path": "tools/10-image-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/10-image-production/README.md#8--then-multiply-cheaply", "text": "**A headline swap is a recomposite.** Same plate, words reset over it —\nseconds, no generation, no spend. Six headlines over one approved plate is six\nrecomposites.\n\n**A picture swap is a generation.** That is the expensive axis, and it is why\nthe brief prices the two separately and says which variations can be reached by\nrecompositing instead.\n\nSo the order is: approve one plate, spin every headline against it, and only\nthen spend on the pictures the brief argued for — cheapest and most-defensible\nfirst, stopping wherever the returns stop.\n\n---"}, {"doc": "Stage two — brief to finished ad", "section": "What runs each step", "tool": "10-image-production", "path": "tools/10-image-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/10-image-production/README.md#what-runs-each-step", "text": "Step — Runs on — Output.\n1 · read the brief — — — the plate list and the layout data.\n2 · make the plate — `tools/plates.py prepare` &rarr; a session on the Higgsfield MCP &rarr; `plates.py ingest` — `inbox/<slug>.png`.\n3 · judge the plate — `tools/judge.py` (plates) · `tools/finish.py` (finished ads) — measured checks, then a vision model against the brief's tests; then the **media gate** (`tools/gates.py`) holds what failed — pass, or held with the reason in `check.json`.\n4 · measure the plate — code — positions adapted to this plate.\n5 · composite the product — ImageMagick — the plate with its product in.\n6 · set the type — ImageMagick, from the layout data — the finished ad.\n7 · prove it — ImageMagick — `finals/source-vs-output.jpg`.\n8 · multiply — ImageMagick for headlines, `make_variations.py` + a session for pictures — the set.\n**Three of the eight are a person's, and they are the three that matter**: which\nplate is good enough (3), whether the finished ad is actually right (7), and\nwhere to stop spending (8)."}, {"doc": "Stage two — brief to finished ad", "section": "The format is the swipe's, not the last one's", "tool": "10-image-production", "path": "tools/10-image-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/10-image-production/README.md#the-format-is-the-swipes-not-the-last-ones", "text": "**Measured cause, 2026-08-31.** The compositor carried one hardcoded layout —\ncentred wordmark, white square framing the damage, headline in a box beneath,\nproduct as a rotated sticker on the corner. That is angle 02's format. It was\ndrawn over angle 06 and angle 09, which share none of it, and both shipped.\n\nThe layout data was in each brief the whole time: margins, per-zone position,\nmeasure, cap height, alignment, case, tracking, colour, max lines,\ndecorations, keep-clear rectangles. Nothing read it. The teardown had done its\njob and the last step threw the answer away.\n\nSo, in `render.py`:\n\n1. **Every element is drawn from the format's own layout data.** Position,\n   measure, alignment, cap height, colour, tracking, case, line count.\n2. **Nothing is drawn unless a zone names it.** A format with no wordmark zone\n   gets no wordmark. Angle 02 has one; 06 and 09 do not, and both were being\n   stamped with a logo their format never had.\n3. **A chromatic colour that is not ours is refused, not painted.** Injection\n   is meant to replace the swipe's accent. When it does not, the renderer\n   draws the neutral and flags it — a headline shipped in the competitor's\n   pink on 2026-08-31.\n4. **The product fits its zone's box, not just its height.** Sizing to height\n   alone pushed a tube off the frame edge.\n5. **The product's zone is checked for content before the product lands on\n   it.** Gross occupation only — calibrated against a real plate, and the\n   limit is written where the check is.\n\nThe same failure has one shape every time: a measurement exists, and the step\nthat neede"}, {"doc": "Stage two — brief to finished ad", "section": "The format is the swipe's, not the last one's", "tool": "10-image-production", "path": "tools/10-image-production/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/10-image-production/README.md#the-format-is-the-swipes-not-the-last-ones", "text": "d it guessed instead. When a stage produces data, the next stage\nreads that data. Not a default, not the last run's, not a sentence describing\nit."}, {"doc": "email-production — the writing, the pictures, the Klaviyo draft", "section": "email-production — the writing, the pictures, the Klaviyo draft", "tool": "11-email-production", "path": "tools/11-email-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/11-email-production/CLAUDE.md", "text": "**email-production** — takes **a calendar send**, makes **the email — copy,\npictures, layout — and a Klaviyo draft**. Does not **send**. Triggered by\n`python3 run_month.py <calendar run> --only <send ids>` (a month or a week), or\n`python3 email.py <source> --brand <brand> …` then\n`python3 simple_email.py <run folder> --brand <brand>` for one email.\n\n**Owner:** Damon · **Station:** `components/` — moved in 2026-09-19\n\n> Moved from `email-production/` on Damon's word, 2026-09-19:\n> *\"move these tools out of the lab… straight into the real production repo,\n> components… so we can actually run QC on it as well… and everybody can have\n> access to these things.\"* Graduation sign-off under `lab/CLAUDE.md` (Dayu or\n> Valentina) is still owed — the move was made on the owner's word and waits\n> on theirs.\n\n**Stage two of the email line.** Stage one — brand record → calendar → review\n→ hand-off — is `components/marketing-calendar/`. This component takes each\nsend that stage hands it and turns it into an email in a Klaviyo draft:\n\n1. **Writing** — `email.py`, twelve steps, each with its own prompt in\n   `prompts/` and its own model (Haiku reads, Sonnet checks, Opus designs; a\n   prompt too big for a small model moves up on its own).\n2. **Pictures** — `simple_email.py`: GPT makes the hero picture from the\n   direction step 8 wrote, and the offer picture FROM the real product photo.\n3. **Layout** — the same simple shape for every email (Damon, 2026-09-19):\n   logo, headline, hero picture, the copy, an optional offer picture, one\n   button, sign-off, footer. Each brand's look is `brands/<"}, {"doc": "email-production — the writing, the pictures, the Klaviyo draft", "section": "email-production — the writing, the pictures, the Klaviyo draft", "tool": "11-email-production", "path": "tools/11-email-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/11-email-production/CLAUDE.md", "text": "brand>/email/simple.json`,\n   read off that brand's own last sent email.\n4. **Klaviyo** — `simple_email.py --push`: a DRAFT campaign to the send's own\n   segment, main subject line, set for the calendar's day at the brand's send\n   hour in each recipient's time zone. There is no send call. A person presses\n   Schedule.\n\nThe full chain, every prompt word for word and the knobs in order:\n**The Email Chain** — https://claude.ai/artifact/U6zNmn1D8qCKVjtgaGnU4G\n(`python3 chain_page.py` rebuilds it from the code)."}, {"doc": "email-production — the writing, the pictures, the Klaviyo draft", "section": "Running it", "tool": "11-email-production", "path": "tools/11-email-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/11-email-production/CLAUDE.md#running-it", "text": "`--brand` is always named. There is no default brand anywhere in this folder —\na command without one stops and says so, and a run folder whose `run.json`\nnames no brand is an error that names the file.\n\n**The dry run is free, and it writes nothing** (2026-09-20). `email.py --dry-run`\nstops before step 0: no model call, no folder under `runs/`. It prints every\ninput with where it was found, the language rows each step would get, which\nmodel each step would run on, and where the run would file. What only step 0\ncan know — lane, format, sender — is reported as \"decided at triage\". The\ndoctor uses the same dry run and no longer has anything to clean up.\n\n**The send's type is checked before anything is spent.** A calendar send's\n`type` is an email format. `brief.py` passes it to the chain as `--type`;\n`copy_plan.py` blocks, `run_month.py` skips, and `email.py` refuses a type that\nis in neither the element library (`components/elements`, `format/email`) nor\nthe brand's own `email/email-types.json` — with the real ones named. The layout\n(`simple`) is checked against `template/email`. Both are recorded in\n`run.json` under `elements`.\n\n**The old email's furniture never reaches the writer** (2026-09-20). Step 1\n(`stage1-read-v2`) records THE MESSAGE, PAGE FURNITURE (logo row, nav, category\nlinks, social row, footer, legal) and SOURCE DEFECTS (dropped letters, dead\nmerge tags, typos) under separate headings. Step 2 (`stage2-spec-v2`) builds\nfrom the message only. Every step after step 2 is handed the record with the\nfurniture section taken out (`email.message_only`).\n\n**Brand words li"}, {"doc": "email-production — the writing, the pictures, the Klaviyo draft", "section": "Running it", "tool": "11-email-production", "path": "tools/11-email-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/11-email-production/CLAUDE.md#running-it", "text": "ve with the brand.** `machine/brand_facts.py` reads, at run\ntime: the brand's furniture words (`brands/<brand>/email/simple.json` →\n`furniture`, plus its name, tagline and sign-off), its product names\n(`products/store.json` + the product files), its store and key names\n(`email/machine.json` → `shopify`, `loop_key`), and which brand a run belongs\nto (the run's own `run.json`, never a folder-name prefix)."}, {"doc": "email-production — the writing, the pictures, the Klaviyo draft", "section": "The four questions (components rule, answered once)", "tool": "11-email-production", "path": "tools/11-email-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/11-email-production/CLAUDE.md#the-four-questions-components-rule-answered-once", "text": "- **Engine + owner** — `email.py`, `simple_email.py`, `run_month.py`,\n  `copy_plan.py`, `brief.py`, and `machine/` (new code: `brand_facts.py`,\n  `email_elements.py`). Damon. The older Python files stay at the folder root\n  until the rollout's renaming pass, because the calendar's hand-off and two\n  scheduled jobs call them there.\n- **Definitions + curator** — `prompts/` (the twelve steps) and each brand's\n  `email/simple.json`. Damon curates both; a prompt changes by a new `-vN-`\n  file, and the highest wins.\n- **The plane it emits** — `runs/email-production/<brand>/<label>/`: every\n  step's prompt as sent beside its output, `run.json` (what the email was told\n  to argue, which model wrote each step), and `deliverable/` — `email.json`,\n  `preview.html`, `check.json`, `pushed.json`. Pictures are media and go to\n  `Shared Assets/runs/email-production/<brand>/<label>/` on the Drive. A records\n  adapter onto `platform/data/records/` is owed.\n- **Manifest per entry** — `run.json`: the assignment, the model per step, the\n  prompt file and its hash per step."}, {"doc": "email-production — the writing, the pictures, the Klaviyo draft", "section": "QC", "tool": "11-email-production", "path": "tools/11-email-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/11-email-production/CLAUDE.md#qc", "text": "The checks and the hold are the shared ones — `components/quality-checks`\n(`import quality_checks as Q`). This folder keeps no copy of them.\n\n- **The elements gate — before any spend.** The send's type and the layout are\n  real ones (above). `email.py` exits `HELD at the elements gate`.\n- **The delivery gate — before Klaviyo.** `simple_email.py` runs three shared\n  checks and calls `Q.hold(\"delivery\", problems, deliverable/)`:\n  - `Q.price_check` + `Q.offer_block` — every price in the email is one the\n\n  - `Q.read_check_block` — the chain's own check step (8c,\n\n  - `Q.unfilled_check` — no `[UNFILLED …]` note in the finished email.\n  Anything found: nothing goes to Klaviyo, the command prints\n  `HELD — not sent to Klaviyo:` with every problem, and\n  `deliverable/check.json` says so in the shared, gate-keyed shape —\n  `{\"delivery\": {\"result\": \"pass\" | \"HELD\", \"problems\": [...]}}`. (It used to\n  write `{\"prices\": \"FAIL\"}` whatever the failure was.)\n- **The team's checks** (`components/qc-checks/`): run the text check and the\n  voice check on a deliverable — `/qc-check runs/email-production/<brand>/<label>/deliverable`."}, {"doc": "email-production — the writing, the pictures, the Klaviyo draft", "section": "History kept here, not live", "tool": "11-email-production", "path": "tools/11-email-production/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/11-email-production/CLAUDE.md#history-kept-here-not-live", "text": "`design_email.py`, `render_email.py`, `figma_*.py`, `render.py` and\n`push_klaviyo.py` built the designed-template email. The simple shape replaced\nthem on 2026-09-19. They are kept because runs made with them point at them,\nnot because anything calls them. `results/` is the record of runs made before\noutput moved to `runs/` (2026-09-17) and is history."}, {"doc": "marketing-calendar — what runs, when, and why", "section": "marketing-calendar — what runs, when, and why", "tool": "12-marketing-calendar", "path": "tools/12-marketing-calendar/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/12-marketing-calendar/CLAUDE.md", "text": "**marketing-calendar** — takes **a brand and a month**, makes **the month's\nsends: dates, audiences, arguments, offers**. Does not **write the emails**.\nTriggered by `python3 machine/calendar.py <month> --brand <brand>`.\n\n**Owner:** Damon · **Station:** `components/` — moved in 2026-09-19\n\n> **Where this lives, and why.** Built as a component on 2026-09-13, moved back\n> to `./` the same day to be perfected, and moved here for good on\n> 2026-09-19 on Damon's word: *\"move these tools out of the lab… straight into\n> the real production repo, components… so we can actually run QC on it as\n> well… and everybody can have access to these things.\"* Graduation sign-off\n> under `lab/CLAUDE.md` (Dayu or Valentina) is still owed — the move was made\n> on the owner's word and waits on theirs. Every path it uses is FOUND rather\n> than counted, which is why the move cost nothing.\n>\n> **Stage one of the email line.** Brand record → calendar → review → hand-off\n> is the planning side; `components/email-production/` is stage two, the writing\n> and the pictures, and reads what this component hands it.\n\nThe first station of the creative line. It takes a brand's own record and\nproduces a month: every send placed at a real date, typed, carrying an offer\nor explicitly carrying none, with the reason it was chosen written beside it.\nThen a human reads that month on a board, corrects it, and hands the approved\nsends to whatever writes them."}, {"doc": "marketing-calendar — what runs, when, and why", "section": "Why it is its own thing and not part of the email machine", "tool": "12-marketing-calendar", "path": "tools/12-marketing-calendar/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/12-marketing-calendar/CLAUDE.md#why-it-is-its-own-thing-and-not-part-of-the-email-machine", "text": "It planned email because email is what was built first. Nothing in layers 1,\n2, 4 or 9 is about email — a holiday lands on the same date for a static ad,\na cultural moment has the same window, and spacing is spacing. Split out\n2026-09-13 on Damon's word: *\"that counter … is also going to be relevant for\nour ads as well, that's why it needs to be a separate component, not just in\nthe email flow alone.\"*\n\nWhat makes a month an EMAIL month is the type catalogue it is typed against\nand the hand-off that writes it — both of which are configuration, not code.\nAn ads lane is a catalogue plus a `handoff.json` entry, not a second engine."}, {"doc": "marketing-calendar — what runs, when, and why", "section": "Graduation checklist (components rule: four questions, answered once)", "tool": "12-marketing-calendar", "path": "tools/12-marketing-calendar/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/12-marketing-calendar/CLAUDE.md#graduation-checklist-components-rule-four-questions-answered-once", "text": "- **Engine + owner** — `machine/`, Damon. Nine layers declared in\n  `machine/chain.py`; seven are code-only and free to re-run.\n- **Definitions + curator** — `prompts/` (the two AI stages) and\n  `definitions/send-types.seed.json` (the starter type catalogue a brand\n  copies and then owns). Damon curates both. A brand's own catalogue always\n  wins over the seed.\n- **The plane it emits** — `platform/data/records/calendar-run/`, one record\n  per planned month, filed by `machine/records.py`. Pointers and counts only.\n- **What the manifest declares per entry** — see\n  `platform/data/kinds/calendar-run.json`: brand, month, slot count, breaks,\n  warnings, where the human got to in review, and which prompt version\n  planned it."}, {"doc": "marketing-calendar — what runs, when, and why", "section": "Rules for this folder", "tool": "12-marketing-calendar", "path": "tools/12-marketing-calendar/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/12-marketing-calendar/CLAUDE.md#rules-for-this-folder", "text": "1. **The prompt is the product.** Both AI layers write the prompt *as\n   actually sent* beside its output, and `run.json` stamps the file's sha.\n   A month whose prompt version cannot be named is not reproducible.\n2. **Brand-agnostic (workspace rule 7).** No brand name appears in this\n   folder. Every fact — segments, moments, offers, avatars, learnings —\n   resolves through `brands/<brand>/`, read-only, per rule-9 (brand home).\n3. **The planner never writes the human's layer.** `slots.json` is the\n   machine's record; `review.json` is the person's. Re-planning a month\n   must never quietly re-approve it, and a reviewer's rewrite must never\n   be lost to a re-run.\n4. **Nothing here writes copy.** If this folder ever grows a sentence meant\n   for a customer, the split has failed.\n5. **The hand-off is configuration.** `handoff.json` names what turns\n   approved slots into finished work. Never hardcode a downstream tool.\n6. **Every write goes under `--out`.** No machine assumptions, credentials\n   from the environment — the rule components hold to, met here already.\n7. **Paths are found, never counted.** No `parents[N]` may point at another\n   folder. This tool has moved twice in a day; each move broke a counted\n   path silently, because a wrong path only means a file \"isn't there\"."}, {"doc": "marketing-calendar — what runs, when, and why", "section": "The layers", "tool": "12-marketing-calendar", "path": "tools/12-marketing-calendar/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/12-marketing-calendar/CLAUDE.md#the-layers", "text": "Each is dated before the next is laid on it — Damon, 2026-09-02, *\"ground up\nthe calendar fundamentally\"*. Seven are code and cost nothing to re-run.\n\n**The chain is declared in `machine/chain.py` and nowhere else.** This table is\nprinted from it (`python3 machine/chain.py`), the run record is written through\nit, and the board draws it from the same list — so the sequence cannot say one\nthing here and another in the code.\n\nLayer — Who — What.\n1 — The holidays — code — The public holidays that land in this month, at their real dates. Calendar facts — the same for every brand..\n2 — The brand's moments — code — The brand's own cultural moments, each resolved to a real date from its anchor. A moment falling outside the month belongs to the month it falls in..\n3 — Who is live — **AI** — Which segments and avatars are in play this month and which rest. No counts, no ceiling, no floor — every why cites the record..\n4 — Pinned to the date — code — Every dated holiday and moment PINNED to the month at its real date, as one send to each live segment it fits. A retail holiday becomes a full arc — a build-up scaled by what it earned before, the ask, the close. (Named 'the anchors' until 2026-09-14, which collided with a moment's own `anchor` field — that is the rule for FINDING a date, this is the act of putting sends on one.).\n5 — The concepts — **AI** — The rest of the month, in order: an offer for every anchored ask, then the concepts the month makes timely, then education and the other categories..\n6 — Typed — code — Every send typed against the brand's own catalogue; any offer-car"}, {"doc": "marketing-calendar — what runs, when, and why", "section": "The layers", "tool": "12-marketing-calendar", "path": "tools/12-marketing-calendar/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/12-marketing-calendar/CLAUDE.md#the-layers", "text": "rying ask completed into its full arc. A type the catalogue does not know is dropped, never invented around..\n7 — Offers checked — code — Every offer checked against the brand's offer bank — the only place an offer may come from. An offer no variant can carry is stripped; an arc left with no offer collapses to its holiday send..\n8 — The affiliate floor — code — One affiliate partner a week, every week, if the brand has any on file..\n9 — The order — code — Dates resolved around the anchors, then hours, sources and links. One send a day. No send budget exists — the only thing ever refused is an exact duplicate..\n`--from-anchors` re-runs 4–9 from disk, `--reorder` just 9 and the board.\nNeither spends anything."}, {"doc": "marketing-calendar — what runs, when, and why", "section": "Two words that are not the same word", "tool": "12-marketing-calendar", "path": "tools/12-marketing-calendar/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/12-marketing-calendar/CLAUDE.md#two-words-that-are-not-the-same-word", "text": "**A moment's `anchor`** is the RULE that turns it into a real date — a\npublic-holiday key, a fixed window, a window with a peak, `any`, or `live`.\nIt lives on the moment, in the brand's file.\n\n**Layer 4** is the ACT of putting sends on those dates. It was called \"the\nanchors\" until 2026-09-14, which made the two indistinguishable in\nconversation; it is \"Pinned to the date\" now.\n\nSo: layer 2 asks every moment *when are you?* (that is the anchor answering),\nand layer 4 says *then a send goes there, and here is how many.*"}, {"doc": "marketing-calendar — what runs, when, and why", "section": "Where a send's FORMAT comes from", "tool": "12-marketing-calendar", "path": "tools/12-marketing-calendar/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/12-marketing-calendar/CLAUDE.md#where-a-sends-format-comes-from", "text": "A send is written off a real email whose structure it borrows. Layer 9 picks\none per slot: the newest of the same type from the brand's own classified\nsends, else the same category.\n\n**A brand that has never sent anything has no formats of its own**, and its\nwhole month comes out unfilled. That is what\n`brands/<brand>/email/format-sources.json` is for — a brand may declare that it\nborrows shapes from another brand, with who approved it and why. **No brand is\nnamed in this folder**; borrowing is a per-brand decision, written down in that\nbrand's own file, and empty by default. A borrowed slot carries\n`source_brand`, so the send stays this brand's while the shape comes from\nsomewhere else."}, {"doc": "marketing-calendar — what runs, when, and why", "section": "Gates", "tool": "12-marketing-calendar", "path": "tools/12-marketing-calendar/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/12-marketing-calendar/CLAUDE.md#gates", "text": "The words are `components/quality-checks`' own: a **check** reports, a **gate**\nis a fixed point where checks run, a **review** is a person's verdict.\n\n**Here a gate is RECORDED and never raised.** Everywhere else `hold()` stops\nfailing work. The calendar's rule is *\"adoption stays a human act\"* — so a month\nthat fails a gate is still written and still drawn, `check.json` says `HELD`\nwith the problems, and a person decides. **The review board is the Review**\n(`machine/serve.py`, `review.json`): approve · needs work · cut, per send.\nNothing downstream writes a send the board has not approved.\n\nGate — When — What it checks — If it fails.\n**inputs** — before any model runs — `chain.readiness(brand)` — every file each layer needs. A degraded layer passes, with a note saying what the month loses — blocked → `HELD`, and the run stops as it always has.\n**elements** — after layer 6, and on the finished slots — every send's `type` is an email format: in the element library (`components/elements`, `format/email`) **or** the brand's own `email/email-types.json` — in neither → a **Break** in `checks.md` naming the real ids, and `HELD`.\n**the month's checks** — after layer 9 — `checks.md` — Breaks (spacing, warm-ups, offers the bank lacks, dead angles, the commercial floor, two sends one argument) vs Warnings — Breaks are listed on the board above the sends; the person fixes or cuts.\n**Review** — the board — a person — an unapproved send is never handed off.\n`check.json` sits at the month's root beside `checks.md`, in `hold()`'s shape:\n`{gate: {result: pass|HELD, problems: [], notes: []"}, {"doc": "marketing-calendar — what runs, when, and why", "section": "Gates", "tool": "12-marketing-calendar", "path": "tools/12-marketing-calendar/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/12-marketing-calendar/CLAUDE.md#gates", "text": "}}`. `run.json` carries\n`gates` (the two results) and `elements` (every type the month used, where it\nwas found, and how many sends carry it). The code is `machine/gates.py`."}, {"doc": "marketing-calendar — what runs, when, and why", "section": "Which model thinks", "tool": "12-marketing-calendar", "path": "tools/12-marketing-calendar/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/12-marketing-calendar/CLAUDE.md#which-model-thinks", "text": "Both thinking layers (3 and 5) are the run kit's **`designs`** tier\n(`components/run-kit/run_kit/model.py`) — they make calls nobody wrote down.\n`--model <name>` forces one model onto both, as it always did. Out of usage is\nnot a broken layer: the run stops with one line, retries nothing, and everything\nalready written stays on disk — run the same command again when usage is back."}, {"doc": "marketing-calendar — what runs, when, and why", "section": "Where a month is filed", "tool": "12-marketing-calendar", "path": "tools/12-marketing-calendar/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/12-marketing-calendar/CLAUDE.md#where-a-month-is-filed", "text": "The working month lives in `runs/calendar-<month>[-<brand>]/` **inside this\nfolder**, and stays there: the board and `components/email-production` read it\nthere. On completion a **copy of the text** — `run.json`, `slots.json`,\n`checks.md`, `check.json`, every layer's output and prompt-as-sent — is also\nfiled to the repo's one home for run records,\n`runs/marketing-calendar/<brand>/<month>/`. Never the board's HTML, never the\nhuman's `review.json`, nothing over 1 MB. A filing problem prints `SKIP` and the\nmonth is whole. A month written with `--out` somewhere else is never filed.\n`python3 machine/gates.py` files every month on the machine;\n`MC_NO_REPO_FILING=1` switches it off."}, {"doc": "marketing-calendar — what runs, when, and why", "section": "Running it", "tool": "12-marketing-calendar", "path": "tools/12-marketing-calendar/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/12-marketing-calendar/CLAUDE.md#running-it", "text": "Full command surface and the shape of everything a run emits: `CONTRACT.md`."}, {"doc": "marketing-calendar — what runs, when, and why", "section": "Who reads its output", "tool": "12-marketing-calendar", "path": "tools/12-marketing-calendar/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/12-marketing-calendar/CLAUDE.md#who-reads-its-output", "text": "- `components/email-production/` — `run_month.py` writes the copy for a month,\n  and is what `handoff.json` points at today.\n- The board itself, for the human step.\n\nBoth read the run folder, never this folder's internals."}, {"doc": "pages — the page machine", "section": "pages — the page machine", "tool": "13-pages", "path": "tools/13-pages/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/13-pages/CLAUDE.md", "text": "**page-production** — takes **a source page and a brief** (brand, avatar, signed angle, funnel, product, offer), makes **the page — copy, layout, pictures**. Does not **deploy**. Triggered by `python3 machine/run.py <page.txt> --brand <brand> …`."}, {"doc": "pages — the page machine", "section": "pages — the page machine", "tool": "13-pages", "path": "tools/13-pages/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/13-pages/CLAUDE.md", "text": "One swipe page in, one pre-sell page out, in our avatar's own words, then one\nvariation per sub-avatar — then each laid out on the section library with a\npicture per section and built in the brand's skin. Ten stages, run under the operator's own\n`claude` login. Established 2026-09-11 on Damon's call (\"same process we just\ndid for ICON — ensure that's an established chain\"): the ICON salespage was\nrebuilt from the Norse acne page by hand, following the copy machine's stage-3\nmethod; this is that method as a chain, with a prompt per stage, every prompt-\nas-sent and every output kept, and a board that shows all of it.\n\nFolder — Purpose.\n`prompts/` — the stage prompts, versioned `-vN-damon.md`, highest wins; retired ones go to `prompts/superseded/`.\n`machine/` — `run.py` (stages 0–6), `deliver.py` (the words into the brand's funnel folder), `layout.py` (stage 7), `pictures.py` (stage 8), `scaffold.py` + `build_site.py` (stage 9), `board.py` (draws the board), `page_gates.py` (the gates), `page_paths.py` (where the workspace and a run live — found by walking up, never counted), `test_rollout.py`.\n`runs/page-machine/<brand>/<label>/` **at the repo root** — one folder per run (since 2026-09-20; the `runs/` beside this code is the old home — still read by every script, never written to again): the source as read, every stage's prompt-as-sent (`stageN--sent.md`), every output (`stageN--<label>.md`), `run.json`; from stage 7 on, `layout[-sub].json`, `references.json` (the brand photographs uploaded to the generator), `pictures-request[-sub].json`, `pictures[-sub].json`.\n`kit/` — the "}, {"doc": "pages — the page machine", "section": "pages — the page machine", "tool": "13-pages", "path": "tools/13-pages/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/13-pages/CLAUDE.md", "text": "section library and layout the pages are built on — Sellmore's `landing/` blocks (77) plus the machine's own `legal-1`, with `_blocks.json` as the bank a layout picks from. Brand-free by rule; a brand's skin comes from its own `identity/web-tokens.md` at scaffold time.\n`pages/` — generated HTML, never hand-edited.\n`docs/` — `the-chain.md` — the stages and why the chain has this shape.\n`context/` — `artifacts.md` — the board's URL; republish over it, never open a second page."}, {"doc": "pages — the page machine", "section": "What it takes", "tool": "13-pages", "path": "tools/13-pages/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/13-pages/CLAUDE.md#what-it-takes", "text": "A captured swipe page (`page.txt` from the swipe library — a landing page, an\nadvertorial, a listicle) and a brand, avatar, angle and funnel. The page's\n**format is read by the classifier** (`components/naming/classify_pages.py`),\nnever chosen; its **name** is built by the convention in\n`components/naming/PAGES.md` from what the run declares; its **angle** must be\nsigned in the brand's `strategy/angles.json`. Nothing here coins a name."}, {"doc": "pages — the page machine", "section": "The chain", "tool": "13-pages", "path": "tools/13-pages/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/13-pages/CLAUDE.md#the-chain", "text": "0 — **Triage** — Page job (pre-sell or offer), the classifier's format confirmed, voice, funnel level. Everything downstream binds to it..\n1 — **Read** — The source page as an objective record — sections in order, headline, turn, proof, asks, voice markers, what it never does..\n2 — **Spec** — The record abstracted into a brand-free, category-free construct: moves, slots, sequence logic, proportion, what is page-bound..\n2b — **Context scout** — Indexes the brand tree and picks what THIS construct needs; says what it left out..\n3 — **Injection** — Substitution, never rewriting — the construct is the template, the brand's own files fill the slots, holes stay marked..\n4 — **Close** — The objection at the flinch and the handoff to the offer page; the base page end to end..\n5 — **Variation** — One page per sub-avatar off the base: same moves, her words. Runs once per sub..\n6 — **Brief** — What the builder opens: the manifest, the copy in page order, the offer verbatim, what is still open. One per page..\n7 — **Layout** — Which block from the section library carries each move, every slot filled from the words, a picture brief for every picture slot. Once per page. `machine/layout.py`.\n8 — **Pictures** — The briefs made real: the brand's own product photographs attached as references, no text drawn, nobody younger than she is. `machine/pictures.py` writes the requests and records the results; the session submits them to the generator..\n9 — **Build** — The page rendered on the section library in the brand's skin, placed where the funnel serves it. `machine/scaffold.py` makes the bran"}, {"doc": "pages — the page machine", "section": "The chain", "tool": "13-pages", "path": "tools/13-pages/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/13-pages/CLAUDE.md#the-chain", "text": "d's page-kit project from `brands/<brand>/identity/`; `machine/build_site.py` builds and places..\nPrompts 0–4 and 6 are the copy machine's prompts (`copy/prompts/`)\nre-cut for a page; the shape is inherited on purpose, so a fix in one chain\nports to the other. Stage 5 is this chain's own."}, {"doc": "pages — the page machine", "section": "Gates", "tool": "13-pages", "path": "tools/13-pages/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/13-pages/CLAUDE.md#gates", "text": "Three of the five shared gates (`components/quality-checks`) apply here. The\nchecks and the hold are the shared ones; the lists are the shared element\nlibrary (`components/elements`). `machine/page_gates.py` is the only place they\nare wired.\n\nGate — When — What it checks — If it fails.\n**inputs** — the signed-angle gate — before anything runs — `--angle` is signed `active` in the brand's `strategy/angles.json`; the brand folder and an offer file exist — refused; nothing is called, no run folder is made.\n**elements** — before anything runs — the format the classifier read is a real row in the library's `format/page` — refused, naming the real ids.\n**elements, recorded** — after stage 3 — the doctrine sections stage 3's SECTIONS CARRIED names, against `doctrine/section` — an unknown one is written into `run.json` (`elements`) as unknown — never a stop.\n**copy** — after the words are final (stages 3–6), before layout or deliver — the base page and every variation: no `UNFILLED` note left. A figure the `--offer` files don't sell (the brand's `offers/offer-bank.md` when no `--offer` is given) is FLAGGED in `check.json`, never a hold — an offer page carries values, savings and struck totals that are not sale prices. Only THE PAGE is checked — the change log and the check quote the source's figures on purpose — **HELD**: everything is saved, `check.json` says why, the run exits 2, `deliver.py` refuses it unless `--force`. `--no-gate` skips it.\nA hole is still honest in a working copy — `[UNFILLED: …]` survives every stage\ninto the brief, as before. What changed is that a page carr"}, {"doc": "pages — the page machine", "section": "Gates", "tool": "13-pages", "path": "tools/13-pages/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/13-pages/CLAUDE.md#gates", "text": "ying one is not\n*delivered* without a person saying so."}, {"doc": "pages — the page machine", "section": "Rules this chain keeps", "tool": "13-pages", "path": "tools/13-pages/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/13-pages/CLAUDE.md#rules-this-chain-keeps", "text": "- **Brand context is read-only.** Nothing here writes to `brands/`. The finished\n  words are copied into `brands/<brand>/funnels/<funnel>/` by a person, once read.\n- **Every figure comes from a file.** Prices and the guarantee from the offer\n  file, verbatim; ingredient phrasing from the product card; reviews reused,\n  never written; the narrator a cast persona, never a customer.\n- **The brand's voice rules bind over everything**, the ban list absolutely,\n  the never-pair rules (no fake urgency, no fake scarcity, no dated result\n  promise) hardest at the close.\n- **A hole stays visible.** `[UNFILLED: …]` survives every stage into the brief.\n- **Models per job:** each stage carries its own `tier` in `machine/run.py`'s `STAGES` — reading stages `checks` (Sonnet), writing stages and layout `designs` (Opus); the tiers are `components/run-kit`'s. `--model` forces one. Out of usage stops the run with one line; `--resume` picks it up; pictures on the generator's general photoreal model with references.\n- **A picture is never freehand.** Every picture slot gets a written brief the way an ad does (`GENERATION-METHOD.md`): the reference carries the material, words carry the subject; nothing typeset is drawn; nobody who has not agreed to appear appears.\n- **Brand-agnostic, by construction.** The kit, the prompts and the scripts name no brand. What varies per brand is read from `brands/<brand>/`: identity (`web-tokens.md`, logos), product photographs, the funnel's `legal.md`. A second brand needs those three things and nothing in this folder changes."}, {"doc": "pages — the page machine", "section": "Where the words go once read", "tool": "13-pages", "path": "tools/13-pages/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/13-pages/CLAUDE.md#where-the-words-go-once-read", "text": "`brands/<brand>/funnels/<funnel>/<format>/<page-name>.md` — the page's words,\nfront matter carrying its key — and the construct beside it as\n`<page-name>-construct.md`. The funnel's own repo builds the page from those."}, {"doc": "page-production — the page machine", "section": "page-production — the page machine", "tool": "13-pages", "path": "tools/13-pages/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/13-pages/README.md", "text": "**page-production** — takes **a source page and a brief** (brand, avatar, signed\nangle, funnel, product, offer), makes **the page — copy, layout, pictures**.\nDoes not **deploy**. Triggered by `python3 machine/run.py <page.txt> --brand <brand> …`.\n\nOne swipe page in, one page out in our avatar's own words, then one variation\nper sub-avatar — each laid out on the section library with a picture brief per\nsection. How the chain is shaped and why: `CLAUDE.md`, `docs/the-chain.md`."}, {"doc": "page-production — the page machine", "section": "The commands", "tool": "13-pages", "path": "tools/13-pages/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/13-pages/README.md#the-commands", "text": "Everything runs from this folder. `--brand` is always said; nothing here has a\ndefault brand.\n\n**See what would be sent, spending nothing**\n\nFills every stage's prompt from the brand's files and writes each one as\n`stageN--sent.md` in the run folder. Calls no model. The copy gate does not run\n(there are no words yet).\n\n**Run it**\n\nSame command without `--dry`. Useful extras:\n\nFlag — What it does.\n`--offer <file> [<file> …]` — the offer files the page's prices come from (default: `brands/<brand>/offers/offer-bank.md`).\n`--subs <sub> [<sub> …]` — one variation per sub-avatar.\n`--swipe <id>` — the swipe registry id of the source page.\n`--context-drop <fragment> …` — files the context scout may not load (a sister product's card).\n`--no-gate` — skip the copy gate.\n`--model <model>` — force one model for every stage (default: each stage's tier — reading stages on the checks model, writing stages on the designs model).\n**Pick a run back up**\n\nKeeps every finished stage whose prompt is unchanged and whose inputs did not\nrerun; reruns the rest. A bumped prompt version reruns that stage and everything\nit feeds. This is also what to run after the account runs out of usage — the run\nstops with one line and loses nothing.\n\n**Hand the words over**\n\n**Then**"}, {"doc": "page-production — the page machine", "section": "Gates", "tool": "13-pages", "path": "tools/13-pages/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/13-pages/README.md#gates", "text": "Gate — When — What it checks — If it fails.\n**inputs** — before anything runs — the angle is signed active in `brands/<brand>/strategy/angles.json`; the brand and an offer file exist — refused, nothing called.\n**elements** — before anything runs — the page format the classifier read is a real row in the element library (`format/page`) — refused, naming the real formats.\n**elements (recorded)** — after stage 3 — the doctrine sections the page says it carries, against `doctrine/section` — unknown ones are written into `run.json` as unknown — never a stop.\n**copy** — after the words are final — the base page and every variation: no `UNFILLED` note left. A figure the offer files don't sell is FLAGGED in `check.json` (a page carries \"$7 Value\" and \"Save $26\", which are not sale prices) — never a hold — **HELD** — everything is saved, `check.json` says why, the run exits 2, `deliver.py` refuses it.\nThe checks and the hold are the shared ones (`components/quality-checks`); the\nlists are the shared library (`components/elements`). This folder owns neither."}, {"doc": "page-production — the page machine", "section": "Where a run lives", "tool": "13-pages", "path": "tools/13-pages/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/13-pages/README.md#where-a-run-lives", "text": "`runs/page-machine/<brand>/<label>/` at the repo root: `run.json` (what was\ndeclared, the elements picked, every stage's model, prompt name and hash), the\nsource as read, every `stageN--sent.md` and output, `check.json`, and from\nlayout on the layout and picture records. The old `runs/` beside the code is\nstill read by every script here; nothing new is written to it."}, {"doc": "page-production — the page machine", "section": "Tests", "tool": "13-pages", "path": "tools/13-pages/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/13-pages/README.md#tests", "text": "No network, no model. They file into a temp folder and never touch a real run."}, {"doc": "copy production (formerly the copy machine / copywriter)", "section": "copy production (formerly the copy machine / copywriter)", "tool": "14-copywriter", "path": "tools/14-copywriter/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/14-copywriter/CLAUDE.md", "text": "**copy-production** — takes **a source and a brief**, makes **the copy: primary\ntext, captions, headlines**. Does not **make pictures**. Triggered by\n`python3 machine/copy.py <source> --brand <brand> --product <product-file>`.\n\n**Owner:** Damon · **Station:** `components/` — moved in 2026-09-19\n\n> Moved from `copy/` and renamed on Damon's word, 2026-09-19: *\"let's\n> move copy into components, and then we'll just change the name to\n> copywriter… we use the original.\"* The same tool, same prompts, same runs —\n> only its home and its name changed. It is the one place copywriting is meant\n> to live; which other chains route their writing through it is NOT decided —\n> each one is proven side by side before it is switched. Graduation sign-off\n> under `lab/CLAUDE.md` (Dayu or Valentina) is still owed. Its run records keep\n> the name `runs/copy-machine/` because Dayu's Mac mini setup files under it.\n\n> **WHERE RUNS FILE (done 2026-09-20).** `copy.py` writes every new run to\n> `runs/copy-machine/<brand>/<label>/` at the repo root (`runs/README.md` is the\n> authority; the folder name is fixed — the Mac mini files under it).\n> `results/` is history: nothing new is written there, and every reader — the\n> board, `publish.py`, `rerender.py`, `batch_status.py`, the research shim —\n> looks in BOTH through `machine/paths.py` (`all_runs()`, `find_run()`).\n> `--rerun-from` on an old label carries it over to its new home once.\n> `output/` (what `publish.py` lifts out) has not moved yet."}, {"doc": "copy production (formerly the copy machine / copywriter)", "section": "The folders — what lives where", "tool": "14-copywriter", "path": "tools/14-copywriter/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/14-copywriter/CLAUDE.md#the-folders--what-lives-where", "text": "Folder — Purpose.\n`machine/` — the code: the runner (`copy.py`), the language layer, the board, the server, the publishers, every page generator.\n`prompts/` — the stage prompts, versioned `-vN-damon.md`, highest wins.\n`bank/` — the system's data, brand-agnostic by rule: `formats.json` (what the machine writes), `format-bank.json` (every format on every channel), `channel-map.json`. Brand context NEVER lives here — each brand's `variables/copy.md` sits in its own folder and is read at run time.\n`library/` — the evidence: every piece of copy we hold as rows (`index.json`), the sourcing rulings (`sources.json`), their builders.\n`results/` — HISTORY — run records made before 2026-09-20. Still read, never written. New runs: `runs/copy-machine/<brand>/<label>/` at the repo root.\n`output/` — finished copy, published per source — what `publish.py` lifts out of results/.\n`pages/` — generated HTML, drawn from data by machine/ generators. Never hand-edited, never committed.\n`docs/` — design notes and the md mirrors of the artifacts.\n`context/` — `artifacts.md` — every artifact's URL; republish over those, never open second pages.\nRun everything from anywhere; paths resolve through `machine/paths.py`, whose\n`HERE` is this folder. The board serves `pages/` on :8778.\n\nOne source in, ad copy out. Eleven prompted stages (0–9, with 2b) plus one\nresearch step, all Claude. `copy.py` runs it, `board.py` draws it, `serve.py`\nserves it live.\n\n**Social copy only** (Damon, 2026-08-25). No advertorials, no listicle pages,\nno landing pages. Ad copy for social, and that is the whole scope."}, {"doc": "copy production (formerly the copy machine / copywriter)", "section": "Where this lives", "tool": "14-copywriter", "path": "tools/14-copywriter/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/14-copywriter/CLAUDE.md#where-this-lives", "text": "`components/copywriter/` since 2026-09-19. Before that `copy/` —\ngraduated out of daemn on 2026-08-26, on Damon's instruction, per the route he\nand Dayu agreed: **build alone in daemn until it actually runs, then move it to\nthe lab so Dayu can break it into components and scale it.**\n\nBrand context is read from `brands/<brand>/` (the one brands tree since\n2026-09-02; `brands/` no longer exists and is no longer probed) —\nstill resolved by name rather than by counting folders (`paths.py`,\n`--brand-root`, `AI_WORKSPACE`). That separation is what let the build move\ntwice without breaking, and it is what will let the brand banks move without\nbreaking it again.\n\n**Nothing here writes to a brand folder.** Read-only, always.\n\n**There was a second copy of this machine** at `copy-machine/`,\ncommitted 2026-08-26 on the way out of daemn — same machine, one day older,\nand its CLAUDE.md still claimed it lived in daemn. Folded into this folder\nand removed 2026-08-27 (Damon: \"merge whatever is needed so it's just one\").\nThe only things it held that this one did not were the prompt-as-sent dumps\nand the run posters, both gitignored here; both were copied across, so the\nboard can still show every old run's prompt as it was actually sent.\n\n**Media never enters this repo.** The source video is a symlink to the\nteardown's own mp4 and the poster is cut from it; both are remade on every\nrun and both are gitignored, along with the generated pages and the\nprompt-as-sent dumps (11MB of inlined brand context, rebuildable by replaying\nthe substitution). What is kept is what each stage RETURNED — the p"}, {"doc": "copy production (formerly the copy machine / copywriter)", "section": "Where this lives", "tool": "14-copywriter", "path": "tools/14-copywriter/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/14-copywriter/CLAUDE.md#where-this-lives", "text": "art worth\ndiffing."}, {"doc": "copy production (formerly the copy machine / copywriter)", "section": "What it takes", "tool": "14-copywriter", "path": "tools/14-copywriter/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/14-copywriter/CLAUDE.md#what-it-takes", "text": "One `source`, which is either:\n\n- **a teardown record** — of a video ad, an organic video, an image ad, an\n  organic image, or a caption\n- **raw copy** — a swipe, pasted in\n\nDamon's two live <brand> ads are the second kind. They were briefly modelled\nas a \"copy bank\" to match against; that was wrong. Existing copy is a\n**source to swipe**, same as a video, which is why the chain has one input\nslot and no bank-match stage."}, {"doc": "copy production (formerly the copy machine / copywriter)", "section": "The chain", "tool": "14-copywriter", "path": "tools/14-copywriter/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/14-copywriter/CLAUDE.md#the-chain", "text": "Mirrors the video teardown chain (`components/video-teardown`) stage for stage, because\nthat chain already solved this problem properly.\n\nmodel tier.\n— — **Formats check** — Every `--write-as` id against the element library (`format/copy`). Not a model. — —.\n0 — **Triage** — Lane, format, avatar, funnel. Everything downstream binds to it. — reads.\n1 — **Read** — One source in, one objective record out. Handles either input kind. — checks.\n2 — **Spec** — The record abstracted into a brand-free construct. — designs.\n2b — **Context scout** — Picks which brand files this source actually needs. — reads.\n3 — **Injection** — Our brand into the construct. Substitution, never rewrite. — designs.\n4 — **Placement** — Where the product enters, and the length budget. Decides, writes nothing. — checks.\n5 — **Hooks** — The control plus variations. All ship — the machine never picks. — designs.\n— — **Research** — The live research gatherer, pulled ONCE for stages 6 and 7 (`machine/research.py`). Not a prompt of ours; kept on a rerun. — —.\n6 — **Expansion** — **Gated: runs only when the lane is ORGANIC.** — designs.\n7 — **Close** — The objection at the flinch, the landing, the whole piece assembled. — designs.\n8 — **Render** — The argument becomes the actual copy, in each requested format, ready to paste. — designs.\n— — **Copy gate** — Prices + unfilled notes. A failure holds the run before the brief. Not a model. — —.\n9 — **Brief** — The document a media buyer opens. — reads.\nThe stage list lives in ONE place — `STAGES` in `machine/copy.py` — and the\nboard reads it from there. **Tiers** co"}, {"doc": "copy production (formerly the copy machine / copywriter)", "section": "The chain", "tool": "14-copywriter", "path": "tools/14-copywriter/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/14-copywriter/CLAUDE.md#the-chain", "text": "me from `components/run-kit`\n(`reads` / `checks` / `designs`); a prompt too big for a small window is moved\nup to the `designs` model automatically. `--tier reads=<model>` repoints one\ntier for a run; `--model <model>` forces one model for every stage, which is\nwhat this machine did before tiers (`--model claude-opus-5` is the old run,\nexactly). The model each stage actually used is in `run.json`.\n\n**The gate is the point.** An `ALREADY AN AD` source has commercial structure\nalready; it is inherited at injection and stage 6 is skipped. An `ORGANIC`\nsource has none, so it has to be built from nothing. Lifted unchanged from the\nvideo chain's `ROUTES`.\n\n**Spec is the stage the first chain lacked**, and its absence is why output\nkept arriving in brand voice: writing from a record produces copy *about* our\nbrand in roughly the source's direction; writing from a construct puts our\nbrand *into* the source's actual shape."}, {"doc": "copy production (formerly the copy machine / copywriter)", "section": "Two rules that bind everything", "tool": "14-copywriter", "path": "tools/14-copywriter/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/14-copywriter/CLAUDE.md#two-rules-that-bind-everything", "text": "**Organic source → the creator's voice, never the brand's.** The copy is that\nperson speaking. Not brand copy with their name on it. This outranks house\nstyle, and stages 3, 5, 6 and 7 each restate it because the close is where\nreplicated copy most often reverts.\n\n**All hooks ship.** The machine writes a control (the source's own opening,\nswiped straight, never \"improved\") plus variations, and does not rank them.\nChoosing is Damon's."}, {"doc": "copy production (formerly the copy machine / copywriter)", "section": "Gates", "tool": "14-copywriter", "path": "tools/14-copywriter/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/14-copywriter/CLAUDE.md#gates", "text": "Two fixed points where work is checked, and a failure stops it.\n\n1. **Elements — before anything runs.** Every format in `--write-as` must be on\n   the element library's `format/copy` list (`components/elements`, fed by\n   `bank/formats.json`). An unknown format is refused with the real ids named;\n   no run folder is made. A format the brand adds in its own `formats.json` is\n   let through and named as the brand's own. `--channel` is NOT checked: it is\n   free text for a person, and the library's `placement/all` list is what gets\n   written (primary text, headline), not where it runs. The picks are recorded\n   in `run.json` under `elements`.\n2. **Copy — after stage 8 (Render), before the brief.** From\n   `components/quality-checks`: `unfilled_check` on the finished copy (the part\n   before `CHECKS`), and `price_check` of every price in the copy against ALL\n   live prices in the brand's `offers/offer-bank.md` (a copy run has no single\n   offer key; `$49` and `$49.00` are the same price). A failure writes\n   `check.json` in the run folder, keeps every file made so far, marks stage 9\n   as held, and exits 2 — the brief is not written, because the brief is the\n   act of handing the copy on. `--no-gate` skips it on purpose and says so in\n   `run.json`.\n\nAlso: an account out of usage stops the run with one line (`USAGE LIMIT — …`)\non the first refusal — it is never retried. Ordinary failures still get three\nattempts."}, {"doc": "copy production (formerly the copy machine / copywriter)", "section": "Formats", "tool": "14-copywriter", "path": "tools/14-copywriter/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/14-copywriter/CLAUDE.md#formats", "text": "`bank/formats.json` — the full list, as data. Triage reads it via `machine/formats.py`, so **adding\na format is adding a row**, not a code change."}, {"doc": "copy production (formerly the copy machine / copywriter)", "section": "A delivery of finished ads → Meta copy", "tool": "14-copywriter", "path": "tools/14-copywriter/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/14-copywriter/CLAUDE.md#a-delivery-of-finished-ads--meta-copy", "text": "When an editor delivers finished ads (Jackie's 7, 2026-08-31, was the first):\n\n \n\nPer ad: a records teardown (no brief), then the Meta set written from the\nrecord. Then `/usr/bin/python3 machine/gdoc_out.py --all --folder <drive-id>`\nputs every published set into a Drive folder as Google Docs — pass the\ndelivery folder's id and the copy sits next to the creative it was written\nfor. Docs update in place on republish."}, {"doc": "copy production (formerly the copy machine / copywriter)", "section": "1. dry run — checks every input, format, prompt field and model. Spends nothing, writes nothing.", "tool": "14-copywriter", "path": "tools/14-copywriter/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/14-copywriter/CLAUDE.md#1-dry-run--checks-every-input-format-prompt-field-and-model-spends-nothing-writes-nothing", "text": "python3 machine/copy.py <source> --brand <brand> \\\n  --product brands/<brand>/products/<product>.md --dry-run [--avatar <key>]"}, {"doc": "copy production (formerly the copy machine / copywriter)", "section": "2. the run", "tool": "14-copywriter", "path": "tools/14-copywriter/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/14-copywriter/CLAUDE.md#2-the-run", "text": "python3 machine/copy.py <source> --brand <brand> \\\n  --product brands/<brand>/products/<product>.md \\\n  [--write-as caption,short-form] [--source-reference \"…\"] [--video source.mp4] \\\n  [--hooks 5] [--label <name>] [--avatar <key>] [--tier reads=<model>] [--model <model>] [--no-gate]"}, {"doc": "copy production (formerly the copy machine / copywriter)", "section": "3. change one stage without buying the rest again", "tool": "14-copywriter", "path": "tools/14-copywriter/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/14-copywriter/CLAUDE.md#3-change-one-stage-without-buying-the-rest-again", "text": "python3 machine/copy.py <source> --brand <brand> --product <…> --label <name> --rerun-from stage8\n```\n\nOutput lands in `runs/copy-machine/<brand>/<label>/` at the repo root: per\nstage the output, the prompt **as actually sent**, `check.json` from the gates,\nand one `run.json` with status, timing, chars, model + tier, prompt version,\nthe picked elements and which inputs each stage fed on. The board is rebuilt at\nthe end of every run. There is no default brand — a missing or unknown\n`--brand` is an error that names the brands that exist.\n\n`machine/rerender.py [label …]` re-runs stage 8 for finished runs against each\nrun's OWN brand, avatar and product (read from its `run.json`); a run that does\nnot record them is skipped with a line saying so. `machine/batch.sh <brand>\n<product-file>` runs the list inside it for the brand you name.\n\nTests: `python3 machine/test_rollout.py` — no model, no network, no real brand."}, {"doc": "copy production (formerly the copy machine / copywriter)", "section": "The board", "tool": "14-copywriter", "path": "tools/14-copywriter/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/14-copywriter/CLAUDE.md#the-board", "text": "Rebuilds on every load, no-cache, byte-range for video. Polls `board.json`\nevery 3s so a run started after the page opened appears without a reload.\n\n`board.build(embed=True)` writes `board-embedded.html` — media inlined as data\nURIs — for publishing as an artifact, where a file path would resolve to\nnothing. An embedded build stops polling and labels itself a snapshot.\n\n**A run is rendered under the chain it actually ran**, read off its own\nfilenames — never today's stage table. The chain was rebuilt once and stage\nkeys were reused for different jobs; without this, an old bank-match output\nappears under a heading called \"Spec\"."}, {"doc": "copy production (formerly the copy machine / copywriter)", "section": "Things a session here must not do", "tool": "14-copywriter", "path": "tools/14-copywriter/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/14-copywriter/CLAUDE.md#things-a-session-here-must-not-do", "text": "- **Never bridge anything into ai-workspace** — prompts, the copy bank,\n  machine code — without Damon saying so for that specific piece.\n  **AMENDED 2026-09-17 (Dayu, with Damon's agreement): RUN RECORDS AND\n  DELIVERABLES ARE NOT A BRIDGE.** They belong at\n  `runs/<machine>/<brand>/<run-label>/` and go there without asking each\n  time. The rule still covers everything else it names.\n- **Never write into the brand tree.** It is read-only; a run that writes there\n  breaks the boundary that lets it read freely.\n- **Never let the machine pick a hook**, rank them, or mark one recommended.\n- **Never improve VERSION 0.** It is the control; an upgraded control measures\n  nothing.\n- **Never reuse a stage number for a new job without retiring the old prompt**\n  to `prompts/superseded/`. Two prompts matching `stage2-*` at the same\n  version is a coin flip over which one runs, and it cost a broken run to find."}, {"doc": "copy production", "section": "copy production", "tool": "14-copywriter", "path": "tools/14-copywriter/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/14-copywriter/README.md", "text": "**copy-production** — takes **a source and a brief**, makes **the copy: primary\ntext, captions, headlines**. Does not **make pictures**.\n\n(Formerly the copy machine / copywriter. The folder keeps the name\n`components/copywriter/`; its runs keep the name `runs/copy-machine/`.)\n\nA source is a teardown record (video or image, ad or organic) or a piece of raw\ncopy to swipe. The brief is what you pass with it: the brand, the product, the\nformats to write, the channel, how many hooks."}, {"doc": "copy production", "section": "The three commands", "tool": "14-copywriter", "path": "tools/14-copywriter/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/14-copywriter/README.md#the-three-commands", "text": "Run from the repo root. `<brand>` is a folder under `brands/` — there is no\ndefault brand.\n\n**1. Dry run — check everything, spend nothing**\n\n \n\nPrints OK / MISSING with character counts for: the source, the product, the\nbrand's variable map, the offer bank, every doctrine slice, every output\nformat (checked against the element library), each avatar's profile, language\nrules and language rows per stage, and — stage by stage — every `{field}` the\nprompt names against what the stage is handed, plus the model each stage would\nuse. No model is called and no file is written. Exits non-zero when something\na run needs is missing. Add `--avatar <key>` to check one avatar instead of\nthe whole roster.\n\n**2. The run**\n\n \n\nFiles to `runs/copy-machine/<brand>/<label>/`: each stage's output, the prompt\nas sent, `run.json`, `check.json`. Each stage runs on the model its tier names\n(`reads` / `checks` / `designs`); `--tier reads=<model>` repoints a tier,\n`--model <model>` forces one model for everything.\n\nAfter the copy is written (stage 8) the **copy gate** checks it: every price\nmust be one the brand's offer bank sells today, and no UNFILLED note may be\nleft in it. A held run keeps everything it made, says why, and does not write\nthe brief. `--no-gate` skips the gate on purpose.\n\n**3. Rerun from a stage — keep what is already written**\n\n \n\nEvery saved stage before the named one is read back, not bought again\n(`stage8`, `8` and `render` all mean the same stage). An older run that still\nsits in `results/<label>/` is carried over to `runs/copy-machine/` once."}, {"doc": "copy production", "section": "Around it", "tool": "14-copywriter", "path": "tools/14-copywriter/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/14-copywriter/README.md#around-it", "text": "`machine/serve.py` — the board, live on :8778 — every run from both run homes.\n`machine/publish.py [label]` — lifts finished copy into `output/`.\n`machine/rerender.py [label …]` — re-runs stage 8 against each run's own brand, avatar and product.\n`machine/batch.sh <brand> <product-file>` — the list inside it, a few at a time.\n`machine/test_rollout.py` — the tests — no model, no network, no real brand.\nRules, the stage table and the gates: `CLAUDE.md`."}, {"doc": "outlier-brief — Damon's own idea in, an approved concept brief and its hand-offs out", "section": "outlier-brief — Damon's own idea in, an approved concept brief and its hand-offs out", "tool": "15-outlier-brief", "path": "tools/15-outlier-brief/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/15-outlier-brief/CLAUDE.md", "text": "**outlier-brief** — takes **Damon's own idea (a few spoken or typed sentences —\nthere is no swipe) plus a brand**, makes **one rounded-out, format-free concept\nbrief he approves, and — only after that approval — the formats it should be\nmade in, picked from the element library, with a hand-off file for each\nproduction chain**. Does not **make the asset, write the final copy, start any\nother chain, or coin a format, style, framework, angle or avatar — those are\nlooked up in their banks**. Triggered by\n`python3 tools/run.py <idea file or \"-\"> --brand <brand>`, then\n`python3 tools/approve.py <label> --brand <brand>`.\n\n**Owner:** Damon. **Group:** briefs — the second door (`SPEC.md` is the spec and\nis his; build to it, do not redesign it). Built 2026-09-20."}, {"doc": "outlier-brief — Damon's own idea in, an approved concept brief and its hand-offs out", "section": "Steps", "tool": "15-outlier-brief", "path": "tools/15-outlier-brief/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/15-outlier-brief/CLAUDE.md#steps", "text": "Declared once, in `tools/steps.py` `STEPS`. Built on `components/run-kit` `Chain`.\n\nStep — What it does — Runs on — Writes (`runs/outlier-brief/<brand>/<label>/`).\n— — the idea exactly as given; the brand's files gathered (`tools/brand_context.py`) — code — `idea.md`.\n`idea1` — round it out — what the idea really is, who it is for, what it promises, what we have, what we do not, questions for him — `reads` — `idea1--round-out.md`.\n`idea2` — the awareness read — the awareness entry and sophistication stage, from the doctrine's own slices — `checks` — `idea2--awareness.md`.\n`idea3` — the concept brief — format-free, whole sentences — `designs` — `idea3--concept-brief.md`.\n`idea4` — check the brief against the brand's files — unsupported claims, chopped thoughts, typos — `checks` — `idea4--check.md`, then `deliverable/concept-brief.md`.\n**STOP** — **the review stop — `run.py` ends here, `awaiting approval`** — Damon — `run.json` → `review`.\n`hand1` — pick the formats, only from the element library's lists — `checks` (or none, with `--formats`) — `hand1--formats.md`, `formats.json`.\n— — the hand-offs — the approved brief word for word plus the picked rows — code, no model — `deliverable/handoff--<medium>.md`, `deliverable/handoff--video.json`.\nEvery prompt is saved as sent (`<step>--sent.md`). Model ids are not written in\nthis folder: a step names a tier and the id comes from `components/run-kit`\n`model.TIERS`. `--model` forces one.\n\n**Cost.** First half: up to 4 model calls (3 when awareness and sophistication\nare both pinned). Second half: 1 call, or 0 when the formats are pi"}, {"doc": "outlier-brief — Damon's own idea in, an approved concept brief and its hand-offs out", "section": "Steps", "tool": "15-outlier-brief", "path": "tools/15-outlier-brief/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/15-outlier-brief/CLAUDE.md#steps", "text": "cked by hand\nwith `--formats`. The hand-offs are written by code — nothing can be claimed\nbetween his approval and the chain that writes the asset.\n\n**Dry run — spends nothing.** `--dry-run` on either command resolves and fills\nevery prompt, shows the model per step and where it would file, makes no model\ncall and leaves no file (it works in a temp folder that is removed; the\napproval dry run records no approval). Exit 1 with each problem named if\nanything it needs is missing.\n\n**Brand context comes from `brands/<brand>/` at run time, and only from there:**\n`position.md`, `story.md`, `offers/offer-bank.md`, `core-avatars/objection-bank.md`,\nthe avatar's `profile.md` (and sub-avatar card), `strategy/angles.json`, and the\ncustomer's own words through the shared `components/language-layer` (called,\nnever edited; this chain's stage map is in `tools/brand_context.py`). A file\nthat is not there is SAID to be not there in the prompt. There is no default\nbrand, and no brand is named in this folder's code or prompts.\n\n**Looked up, never coined.** `--avatar`, `--sub`, `--angle`, `--offer` are\nchecked against the brand's own files; `--awareness`, `--sophistication`, every\nframework, format and style against `components/elements`. An unknown one is\nrefused with the real ones named. An avatar the round-out proposes must be a\nreal one too.\n\n**The doctrine is bound, never re-typed.** Awareness, sophistication, mass\ndesire and the ad frameworks reach a prompt as the rendered slices of\n`components/marketing-doctrine` (`tools/doctrine_slices.py`). A test fails if a\nprompt spells an awareness"}, {"doc": "outlier-brief — Damon's own idea in, an approved concept brief and its hand-offs out", "section": "Steps", "tool": "15-outlier-brief", "path": "tools/15-outlier-brief/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/15-outlier-brief/CLAUDE.md#steps", "text": " level.\n\n**Answers are read the way a model writes them** (`tools/slots.py`): a data\nblock as a tagged fence, a heading over a plain or ```json fence, or a bare JSON\nobject; headings as `#`–`####` or bold, numbered or not, with or without a colon."}, {"doc": "outlier-brief — Damon's own idea in, an approved concept brief and its hand-offs out", "section": "The four questions", "tool": "15-outlier-brief", "path": "tools/15-outlier-brief/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/15-outlier-brief/CLAUDE.md#the-four-questions", "text": "- **Engine + owner:** `tools/` (`run.py`, `approve.py`, `steps.py`, `gates.py`, `brand_context.py`, `doctrine_slices.py`, `elements_pick.py`, `handoff.py`, `slots.py`, `paths.py`), Damon\n- **Definitions + curator:** `prompts/*-vN-damon.md` (one per step, highest `-vN-` wins; a changed prompt is a new file), Damon. `SPEC.md` is the spec.\n- **The plane it emits:** `runs/outlier-brief/<brand>/<label>/` at the repo root. Nothing is written into `brands/`.\n- **Manifest per entry:** `run.json` — the assignment, the model and prompt hash per step, `roundout`, `awareness`, `concept`, `review`, `formats`, `handoffs`, `state`."}, {"doc": "outlier-brief — Damon's own idea in, an approved concept brief and its hand-offs out", "section": "Gates", "tool": "15-outlier-brief", "path": "tools/15-outlier-brief/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/15-outlier-brief/CLAUDE.md#gates", "text": "Three of the five shared gates (`components/quality-checks`), in `tools/gates.py`.\nEach writes its result into the gate-keyed `check.json`; a failure HOLDS the run\n(exit 2), says why, and marks `run.json` `state: held at the <gate> gate` — a\nheld brief cannot be approved.\n\n- **inputs** — before any model runs: the idea is not empty; the brand is a real\n  folder; a pinned avatar, sub-avatar, angle or offer is on file; a pinned\n  awareness level or sophistication stage is a doctrine row; every step has a\n  prompt; every doctrine slice and element list is there. Again after the\n  round-out: the avatar it proposed is a real one.\n- **elements** — after the awareness read (both ids are doctrine rows), after\n  the brief (every framework it names is a library row), and after the format\n  pick (every format, style and framework is a real row on its own list). A row\n  whose definition still says `[TO DEFINE` may be NAMED as a candidate; it is\n  flagged **\"named, not defined yet\"**, its medium is not handed off, and it\n  never holds the run. An unknown id is refused with the real ids named.\n- **copy** — after the brief, and again at approval (he may have edited it):\n  the brief arrived in its nine labelled parts; no UNFILLED note; every price is\n  one the offer bank sells today (`price_check`, against the named offer's own\n  entry); the angle and offer it names are on file; and the check step found no\n  claim the brand's files do not carry, no chopped thought, no typo."}, {"doc": "outlier-brief — Damon's own idea in, an approved concept brief and its hand-offs out", "section": "The review stop", "tool": "15-outlier-brief", "path": "tools/15-outlier-brief/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/15-outlier-brief/CLAUDE.md#the-review-stop", "text": "Not a check — a person. `run.json` carries `review.state`: **`awaiting`** |\n**`approved`** | **`sent back`**, with who, when, the note, the sha of the brief\nthat was judged, and the history of earlier verdicts.\n\n- `tools/run.py` always ends at the stop. It never picks a format or writes a hand-off.\n- `tools/approve.py <label> --brand <brand> [--note \"…\"]` records the approval, then runs `hand1` and the hand-offs.\n- `tools/approve.py … --send-back --note \"…\"` records `sent back` and runs nothing; `run.py … --rerun-from idea3` rewrites the brief and is told the note (and whatever the copy gate held).\n- **The later steps refuse without an approval on file** (`gates.require_approval`) — and refuse if the brief changed after it was approved. A rerun that rewrites the brief sets the review back to `awaiting`.\n- To edit the brief by hand before approving: edit `idea3--concept-brief.md` in the run; the approval re-runs the code checks on it."}, {"doc": "outlier-brief — Damon's own idea in, an approved concept brief and its hand-offs out", "section": "Hand-offs — nothing is started", "tool": "15-outlier-brief", "path": "tools/15-outlier-brief/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/15-outlier-brief/CLAUDE.md#hand-offs--nothing-is-started", "text": "`tools/handoff.py` writes files and prints the exact command; it imports and\ninvokes no other tool.\n\n- **video** — `deliverable/handoff--video.json` is the CHOICE the existing\n  second door (`components/video-teardown/machine/compose.py`) takes, in the\n  names of its own flags, with the concept brief carried beside it. That door\n  has no slot for the brief itself yet (`SPEC.md` §1) — see `README.md`.\n- **image · copy · page · email** — `deliverable/handoff--<medium>.md`, a plain\n  brief, with the command that chain is started with today. Each says plainly\n  that the chain's door for a brief is still to build. Anything the brief cannot\n  know is left as a `<blank>`, never guessed.\n\nTests: `python3 tests/test_outlier_brief.py` — stubbed model, temp workspace,\nnever the real `runs/` or `brands/`."}, {"doc": "The Outlier Brief", "section": "The Outlier Brief", "tool": "15-outlier-brief", "path": "tools/15-outlier-brief/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/15-outlier-brief/README.md", "text": "The second door. The first door starts from somebody else's ad; this one starts\nfrom **your idea**. You say it rough, the system rounds it out with everything\nthe brand has on file, and writes one concept brief. **You approve it.** Only\nthen does it name the formats it should be made in and write the hand-off for\neach production chain. It never makes the asset.\n\n`SPEC.md` is the spec (Damon's, settled 2026-09-20). `spec.html` / `page.py` are\nits page. `CLAUDE.md` is how the tool is built and gated."}, {"doc": "The Outlier Brief", "section": "1 — the idea, up to the concept brief. Stops: awaiting approval.", "tool": "15-outlier-brief", "path": "tools/15-outlier-brief/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/15-outlier-brief/README.md#1--the-idea-up-to-the-concept-brief-stops-awaiting-approval", "text": "python3 outlier-brief/tools/run.py <idea file, or \"-\" for piped text> --brand <brand>"}, {"doc": "The Outlier Brief", "section": "2 — his verdict, then the formats and the hand-offs.", "tool": "15-outlier-brief", "path": "tools/15-outlier-brief/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/15-outlier-brief/README.md#2--his-verdict-then-the-formats-and-the-hand-offs", "text": "python3 outlier-brief/tools/approve.py <label> --brand <brand> [--note \"...\"]\n\n```\n\nIn a worktree, set `AI_WORKSPACE=<that worktree>` on both.\n\nHalf — Model calls — What you get.\n`run.py` — up to 4 (3 with awareness + sophistication pinned) — `deliverable/concept-brief.md` — the brief, your idea quoted back, and the questions the files could not answer.\n`approve.py` — 1 — or 0 with `--formats` — `formats.json`, `deliverable/handoff--video.json` + `.md`, and a `handoff--<medium>.md` for every other medium asked for.\nEverything files to `runs/outlier-brief/<brand>/<label>/`."}, {"doc": "The Outlier Brief", "section": "The idea file", "tool": "15-outlier-brief", "path": "tools/15-outlier-brief/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/15-outlier-brief/README.md#the-idea-file", "text": "A few sentences, as rough as you like. Anything inside `` is a note\nto people and is stripped before the idea is read. `examples/example-idea.md`\nis an EXAMPLE, clearly marked, naming no real claim — replace it with a real\nidea."}, {"doc": "The Outlier Brief", "section": "What it reads", "tool": "15-outlier-brief", "path": "tools/15-outlier-brief/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/15-outlier-brief/README.md#what-it-reads", "text": "From `brands/<brand>/` only, at run time: the position, the stories, the offer\nbank, the objection bank, the avatar's card, the angles, and the customer's own\nwords (through `components/language-layer`). From `components/marketing-doctrine`:\nthe rendered slices for mass desire, awareness, sophistication and the ad\nframeworks. From `components/elements`: every format, style and framework list."}, {"doc": "The Outlier Brief", "section": "Where each hand-off stands today", "tool": "15-outlier-brief", "path": "tools/15-outlier-brief/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/15-outlier-brief/README.md#where-each-hand-off-stands-today", "text": "Medium — Hand-off — The chain's door for a brief.\n**video** — `handoff--video.json` — the choice `compose.py` takes (brand · avatar · sub · awareness · sophistication · format · framework · route), plus the brief — **the choice is accepted today; the brief itself is not read yet.** `compose.py` has no slot for an idea (SPEC §1). The printed `plan` command makes no model call (it opens a run folder in the video chain and proves the choice resolves); the brief reaches the writer only once `compose.py` binds it — a change in `components/video-teardown`, outside this tool..\nimage — `handoff--image.md` — to build — the image chain only knows swipes.\ncopy — `handoff--copy.md` — not proven — copy production takes a source file; the hand-off can be that file.\npage — `handoff--page.md` — to build — the pages chain starts from a captured page.\nemail — `handoff--email.md` — close, not proven — email production takes a source and an argument.\nNothing is started by this tool. Each hand-off prints the command and files it."}, {"doc": "The Outlier Brief", "section": "Not built yet (SPEC §6, in its order)", "tool": "15-outlier-brief", "path": "tools/15-outlier-brief/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/15-outlier-brief/README.md#not-built-yet-spec-6-in-its-order", "text": "The grid (one idea × several awareness stages × several formats in one run),\nthe research gatherer's live rooms as an input, and the Control Room trigger.\nOne idea, one awareness entry, one brief — proven on a real idea first."}, {"doc": "video-edit (lab — Damon)", "section": "video-edit (lab — Damon)", "tool": "16-video-edit", "path": "tools/16-video-edit/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/16-video-edit/CLAUDE.md", "text": "**video-edit** — takes **an approved kit: one voice track, A-roll and B-roll\nclips, music and sound effects**, makes **the finished, captioned video**. Does\nnot **make, regenerate or restyle a clip, and never rewrites a line**.\nTriggered by `python3 machine/run.py start <kit-folder> --brand <brand> --label <label>`\n(`--dry-run` checks everything and renders nothing).\n\nBuilt 2026-09-20 on Damon's word: *\"I basically need to get rid of video\neditors… We're going to use hyperframes… build a foundationally sound video\nediting system.\"* It replaces the ffmpeg assembly tool in his private repo,\nwhich is not carried over. The video machine (`ai-video-production`)\nsays the edit is \"a separate process, deliberately out of scope here\" — this is\nthat process."}, {"doc": "video-edit (lab — Damon)", "section": "The principle", "tool": "16-video-edit", "path": "tools/16-video-edit/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/16-video-edit/CLAUDE.md#the-principle", "text": "**The model decides, the sheet records, the machine executes, Damon approves.**\nThe cut sheet (`machine/cutsheet.py`) is the one file that IS the edit. Nothing\nelse knows what is in the video. A change is a new version of the sheet —\nnever a regeneration — and every change is kept with its reason.\n\nThe engine is HyperFrames (HeyGen, Apache-2.0, installed on this Mac\n2026-09-20): a video is a web page with timing marks, rendered frame by frame,\nso the same sheet always gives the same video. `machine/compose.py` is the only\nfile that knows HyperFrames exists; swap the engine and only that file changes."}, {"doc": "video-edit (lab — Damon)", "section": "The steps — `machine/steps.json` is the list; the page and the runner both read it", "tool": "16-video-edit", "path": "tools/16-video-edit/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/16-video-edit/CLAUDE.md#the-steps--machinestepsjson-is-the-list-the-page-and-the-runner-both-read-it", "text": "gather → cut → gate → **STOP 1 timeline** → **STOP 2 captions** → export →\nchecks → file → learn.\n\nDamon's order, 2026-09-20: see the timeline with the dead space out; then the\ncaptions on; then the final export. Every change runs back through learning."}, {"doc": "video-edit (lab — Damon)", "section": "Hard rules", "tool": "16-video-edit", "path": "tools/16-video-edit/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/16-video-edit/CLAUDE.md#hard-rules", "text": "- **A talking clip keeps its own sound.** That is the only thing that keeps the\n  lips right (Damon, 2026-09-20, on the first proof: \"the lip sync is off… the\n  A-roll was already good\"). A separate voice file laid over a talking clip is\n  refused as two voices. The finished file is measured against the source clip\n  (`machine/syncheck.py`); more than one frame of slip is HELD.\n- **Dead air is cut ON the clip.** Talking A-roll is split at every pause longer\n  than the pace dial — picture and sound together, a breath left each side — and\n  alternate cuts punch in 7% so a jump reads as a choice. B-roll hides the rest.\n- **B-roll over the voice read has no lips**, so its voice slice is cut freely\n  and the picture runs on (`voice_cuts`). A piece with no talking clip at all\n  may use one whole voice track instead.\n- **B-roll is placed by the WORDS it covers**, never by seconds, so it survives\n  re-timing (`covers: {\"from\": \"is why\", \"to\": \"worked\"}`).\n- **A headline that opens the video is there on frame one** — no fade, no slide —\n  and lives exactly as long as the clip under it (`with: <clip id>`).\n- **Nothing unapproved reaches the edit.** `gather` refuses a clip without\n  `approved: true`, and a clip with no roll label.\n- **A-roll carries the line, B-roll covers it.** B-roll with nothing under it —\n  no A-roll and no voice cut — is refused. The roll labels come from the teardown and the brief, not from here.\n- **Nothing renders red.** The gate names the field: a hole in the picture, a\n  trim past the source, a caption or overlay inside the safe zone, a look or\n  transition "}, {"doc": "video-edit (lab — Damon)", "section": "Hard rules", "tool": "16-video-edit", "path": "tools/16-video-edit/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/16-video-edit/CLAUDE.md#hard-rules", "text": "that is not on its list, a cut with no `why`.\n- **Text is a layer, never a generation.** Captions, hooks and the end card are\n  composed on top. A caption look is a SETTING (`looks/bank.json`); switching it\n  never re-edits the footage. `match-swipe` builds a one-off look from the\n  teardown's caption read.\n- **The editing rules are checks, not taste:** nothing moves in a straight\n  line; never a lone fade; exits twice as fast as entrances; one loud colour per\n  frame; no frozen stills; captions 2–4 words, one highlight, inside the safe\n  zone; a sound effect lands two frames early; music 18 dB under the voice.\n- **The approved read is never touched** — levelling to −16 LUFS is done on a copy.\n- **Tests never touch live runs** — they pass their own `--runs-root`.\n- Brand-agnostic: no brand name in any code, prompt or look."}, {"doc": "video-edit (lab — Damon)", "section": "Files", "tool": "16-video-edit", "path": "tools/16-video-edit/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/16-video-edit/CLAUDE.md#files", "text": "`machine/run.py` — the one command: `from-run · start · approve · change · status`.\n`machine/kit_from_run.py` — THE JOIN — reads a video machine run record and writes the edit kit; reads only, approves nothing.\n`machine/syncheck.py` — are the lips in time — the finished sound measured against the talking clip it came from.\n`learned.json` — what Damon has taught the cut; the cut prompt reads it and it outranks the prompt's own rules.\n`machine/steps.json` — the declared steps.\n`machine/defaults.json` — the dials; a run's `controls.json` overrides any of them.\n`machine/cutsheet.py` — the cut sheet's shape and the gate.\n`machine/compose.py` — cut sheet → HyperFrames page (the only engine-aware file).\n`machine/timeline_page.py` — the page Damon sees at each stop.\n`looks/bank.json` · `looks/transitions.json` — caption looks and transitions — element lists.\n`prompts/stage-2-cut/` · `prompts/stage-9-learn/` — one folder per model step, `-vN-`, the highest wins; never edit in place.\n`context/artifacts.md` — its pages.\n`tests/` — the gate and the caption grouping.\nOutput: `runs/video-edit/<brand>/<label>/` — `run.json`, every `cutsheet-vN.json`,\n`changes.jsonl`, `timeline.html`, `deliverable/` — and the video to the same\npath on the Drive (`runs/README.md`)."}, {"doc": "video-edit (lab — Damon)", "section": "The handoff — joined 2026-09-20", "tool": "16-video-edit", "path": "tools/16-video-edit/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/16-video-edit/CLAUDE.md#the-handoff--joined-2026-09-20", "text": "`python3 machine/run.py from-run <video-machine-run>` reads the production\nrun's own record (`plan.json` scenes, `media/<id>.mp4`, `vo/<id>.mp3`,\n`verdicts.json`, `media/<id>.parity.json`) and writes the kit to\n`runs/video-edit/<brand>/<label>/kit/`. A type-A to-camera scene becomes talking\nA-roll; a type-B scene becomes B-roll over its voice slice. A clip is approved\nonly when the record says so — motion and director verdicts on file, the words\ncheck PASS — and everything else is listed by name. The production run is never\nwritten to. Cutaways in the plan are not read yet (no run has any)."}, {"doc": "video-edit (lab — Damon)", "section": "The cut — the model is ON", "tool": "16-video-edit", "path": "tools/16-video-edit/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/16-video-edit/CLAUDE.md#the-cut--the-model-is-on", "text": "`start` cuts with the model by default (`--cut auto` for the mechanical one).\nThe machine first makes the lip-safe draft — A-roll cut at its pauses, every\nword timed on the final clock — and the model (Opus, through\n`components/run-kit`) only decides where B-roll and on-screen text go, reading\nthe brief, the roll map and `learned.json`. The prompt as sent and the reply\nare saved in the run. A red sheet gets one retry with the gate's reasons; a\nsecond red, or no model, falls back to the auto cut and the sheet says so."}, {"doc": "video-edit (lab — Damon)", "section": "Not built yet", "tool": "16-video-edit", "path": "tools/16-video-edit/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/16-video-edit/CLAUDE.md#not-built-yet", "text": "`match-swipe` caption looks · 4:5 and 1:1 outputs · the contrast check over\none-second windows · the Drive copy and the naming gate at `file` · the learn\nstep writing into `learned.json` by itself (today a session files it) · text\nplacement that knows where the face is · music and sound-effect stations\nupstream, so those layers have never carried real files."}, {"doc": "video-edit", "section": "video-edit", "tool": "16-video-edit", "path": "tools/16-video-edit/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/16-video-edit/README.md", "text": "Turns an approved kit — voice, A-roll, B-roll, music, sound effects — into the\nfinished captioned video, with two stops for Damon: the timeline, then the\ncaptions. No timeline software, no editor.\n\nRules, files and what is not built yet: CLAUDE.md. The page: context/artifacts.md."}, {"doc": "story-builder — rules for this machine", "section": "story-builder — rules for this machine", "tool": "18-story-builder", "path": "tools/18-story-builder/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/18-story-builder/CLAUDE.md", "text": "**research-story** — takes **a brand's own files**, makes **its story file — who\ntells the brand's story and what happened to them, every quote checked**. Does\nnot **invent a teller**. Triggered by\n`python3 story-builder/build.py --brand <brand>`.\n\n(Called the story builder until 2026-09-20; the folder keeps that name for now.\nShape: `machine/` code · `prompts/` · `tests/` · `build.py` and `page.py` at\nthe root are forwarders so every old command still works.)\n\nThe story builder drafts `brands/<brand>/story.md` — the storytelling framework\nthat sits beside a brand's `position.md` — for any brand. Built 2026-09-19 off\nthe hand-built <brand> file Damon approved that day (\"story looks good\").\n`CLAUDE.md` governs where output goes; this file governs how the\nmachine works."}, {"doc": "story-builder — rules for this machine", "section": "Rules", "tool": "18-story-builder", "path": "tools/18-story-builder/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/18-story-builder/CLAUDE.md#rules", "text": "1. **Brand-, product- and avatar-agnostic.** Nothing in `build.py` or the\n   prompts names a brand, product, avatar or customer. Brand content enters\n   only from `brands/<brand>/`, through the prompt's labelled slots. The\n   invented-example guard sits at the top of every prompt.\n2. **The shape is the template.** `brands/_TEMPLATE/story.md` is the contract\n   and `components/marketing-doctrine/lint_story.py` is its gate. When the\n   shape changes, change the template and the lint together — <brand>'s\n   approved file is the reference the lint must keep passing.\n3. **Quote only what was handed over.** The draft may quote only the rows and\n   brand excerpts stage 1 gathered. Stage 3 checks every quoted string\n   verbatim against exactly that material. A story with an unfound quote is\n   demoted to `open`; an unfound quote outside a story is marked\n   ⟨unverified⟩. Never loosen the check to make a draft pass.\n4. **Never overwrite a brand's story.** `brands/<brand>/story.md` is written\n   only when it does not exist and the draft passed the copy gate (rule 9).\n   Otherwise the\n   draft stays in the run folder's `deliverable/` and a human merges it.\n   **This is an exception to `CLAUDE.md` §5 (brands/ is read-only to\n   machines)** — made on the build request of 2026-09-19 (\"the builder writes\n   `brands/<brand>/story.md` ONLY when that file does not already exist\"). It\n   is Damon's to keep or revoke; revoking it is one branch in `build.py`.\n5. **`confirmed by: open` always.** A human locks the block, never the machine.\n6. **Model per job.** Gather, verify and lint are plain P"}, {"doc": "story-builder — rules for this machine", "section": "Rules", "tool": "18-story-builder", "path": "tools/18-story-builder/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/18-story-builder/CLAUDE.md#rules", "text": "ython — no model.\n   The one model call (the draft) goes through the video machine's own\n   helper (`components/video-teardown/machine/run.py` → `claude`,\n   `with_retry`), so it signs in the same way and gets the same chatter\n   guard. No keys: it uses the machine's own Claude sign-in.\n7. **Prompts are versioned Markdown.** `prompts/stageN-<name>-vN-damon.md`;\n   the highest `-vN-` wins. A change is a new version, never an edit in\n   place, and every changed prompt goes verbatim into the prompt-library\n   page.\n8. **Runs are never pruned.** A run label that already exists is refused.\n   Runs file to `runs/research-story/<brand>/<run-label>/`; the ones made\n   before 2026-09-20 stay under `runs/story-builder/` and a label used there is\n   refused too.\n9. **Three gates, the shared hold.** `machine/story_gates.py` runs\n   `components/quality-checks` `hold()`, which writes the gate-keyed\n   `check.json` into the run. **inputs** and **elements** run before the model:\n   the template and prompt are on file, and the story framework and delivery\n   the ARC slot names (`story-testimonial`, `storyteller`) are real rows in the\n   element library (`components/elements`) — an unknown one is refused with the\n   real ids named, before anything is spent. **copy** runs after the lint: a\n   quote still standing unchecked outside a story, or a broken shape, HOLDS the\n   build — nothing goes to the brand, exit code 2. A story demoted to `open` is\n   the quote check's own remedy and does not hold.\n10. **`--dry-run` is free.** No model call, no folder, no file; it prints where\n\n11. **Paths are "}, {"doc": "story-builder — rules for this machine", "section": "Rules", "tool": "18-story-builder", "path": "tools/18-story-builder/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/18-story-builder/CLAUDE.md#rules", "text": "found, not counted.** `machine/story_paths.py` walks up to the"}, {"doc": "intake — one front door for every swipe", "section": "intake — one front door for every swipe", "tool": "19-intake", "path": "tools/19-intake/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/19-intake/CLAUDE.md", "text": "**intake** — takes **every swipe already saved** (a video, an image or\ncarousel, words, a page, an email — paid or organic, ours or a competitor's),\nmakes **one swipe record per swipe**: its `source`, its asset kind, where the\nasset is, the brand it was swiped for, its format if its pool knows one, and\nwhich teardown it goes to — filed as one index under\n`runs/intake/<brand>/<label>/`. Does not **tear anything down, fetch, scrape,\nlabel a format, or call a model**. Triggered by\n`python3 intake/tools/run.py --brand <brand>`.\n\n**Owner:** Damon. Built 2026-09-20, rollout Step 4 (BLUEPRINT.md §6): *\"We\nshouldn't be organic swipes and paid swipes… where a swipe came from is a\nfield, not a tool.\"*"}, {"doc": "intake — one front door for every swipe", "section": "The two swipe folders are still where swipes LIVE", "tool": "19-intake", "path": "tools/19-intake/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/19-intake/CLAUDE.md#the-two-swipe-folders-are-still-where-swipes-live", "text": "`swipe-paid/` and `swipe-organic/` are live tools with\ntheir own boards (My Feeds on 8820, Swipe Studio on 8765, the paid library on\n8780), their own morning jobs and other sessions working in them. **Swipes are\nstill SAVED there, by those tools, in the shape those tools write.** Nothing\nwas moved, renamed, or reshaped, and this tool never writes to either folder.\n\n**This folder is the one place they are READ from as one.** Anything that needs\n\"every swipe, whatever its source\" asks intake; anything that finds or saves a\nswipe still does it in the swipe folder it always did."}, {"doc": "intake — one front door for every swipe", "section": "What a record is", "tool": "19-intake", "path": "tools/19-intake/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/19-intake/CLAUDE.md#what-a-record-is", "text": "field — what it says.\n`id` — `swipe:<lane>:<where>:<ref>` — the same id `swipe-paid/corpus.py` mints, so it joins the paid library.\n`source` — `paid` · `organic` · `own` — where we found it.\n`owner` — `competitor` · `own` · `creator` — whose it is (`--source competitor` matches this).\n`kind` — `video` · `image` · `carousel` · `copy` · `page` · `email` — read off the asset; `unknown` when the asset does not say.\n`route` — `video-teardown` · `image-teardown` (images and carousels) · `copy-teardown` · `page-teardown` · `email-teardown` · `unrouted`.\n`swiped_for` — the brand, discovered from the folders (a feed named `<brand>--…`, an item's `brand_fit`, the ad account in `brands/<brand>/meta/account.json`) — or `shared` when the pool does not say.\n`asset` — `url` · `file` (repo path) · `drive` (Shared Assets path or link) · `thumb`.\n`format` / `format_state` — the label its pool carries · `known` / `empty` / `unknown-format`.\n`structure` / `structure_state` — organic videos only — the organic-structure read · `known` / `empty` / `unknown-structure`.\n**Format and structure are different elements.** A paid ad's `format` is\nchecked against `format/<kind>`. What swipe-organic calls a post's \"format\" is\nan organic **structure** and is checked against `structure/video`. Neither list\nis asked the other's question."}, {"doc": "intake — one front door for every swipe", "section": "Gates", "tool": "19-intake", "path": "tools/19-intake/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/19-intake/CLAUDE.md#gates", "text": "Both run through `quality_checks.hold`; `check.json` is keyed by gate.\n\nGate — Sits — Asks — Mode.\n**inputs** — before anything is read into an index — are both swipe pools where they live, holding something readable? is `--brand` a real folder under `brands/`? — always holds.\n**elements** — after every swipe is normalised — is every `format` (and organic `structure`) a swipe carries a real row in the element library? — `--gates hold\\ — warn` — **default `warn`**.\n**Why `warn` is the default, plainly:** today's pools carry labels the library\nhas never seen (one judged paid format, ~50 organic structure reads named\n`new:…`). On `hold` no index could be built until those are fixed at their\nsource or signed into the library — Damon's call, not this tool's. On `warn`\nthe index still files, **each such swipe is marked `unknown-format` /\n`unknown-structure` with the real ids beside it, and `check.json` still says\nHELD.** Never silently passed, never invented. Flip to `--gates hold` once the\npools are clean."}, {"doc": "intake — one front door for every swipe", "section": "Rules for working here", "tool": "19-intake", "path": "tools/19-intake/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/19-intake/CLAUDE.md#rules-for-working-here", "text": "- **Read-only on both pools.** No write, move, rename or reshape — ever, from here.\n- **No format is ever filled in here.** An empty format stays empty and is\n  counted. Labelling is a separate pass that needs a model and Damon's sign-off.\n- **No brand in code.** Brands come from `brands/`, advertisers and feeds from\n  the pool folders.\n- **No model, no network.** Kind comes from the asset. Every run is free.\n- Paths are found by walking up; `AI_WORKSPACE` points it at another workspace\n  (a worktree reading the live pools; the tests' fake one).\n- Tests: `python3 intake/tests/test_intake.py` — a temp workspace\n  with tiny fake pools; never the real pools, `runs/` or `brands/`."}, {"doc": "asset-index (lab — Damon)", "section": "asset-index (lab — Damon)", "tool": "20-asset-index", "path": "tools/20-asset-index/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/20-asset-index/CLAUDE.md", "text": "**asset-index** — takes **a folder of footage, for any brand**, makes **one\nsearchable record per clip: what is IN it, not what the file is called**. Does\nnot **choose, cut or ship anything**. Triggered by\n`python3 machine/extract.py <footage folder> --brand <brand> [--label <name>]`.\n\n`IMG_5476.MOV` becomes *\"man patting his face with a grey towel in a bathroom,\nprofile reveal, usable as demo\"* — so the edit can ask for a shot instead of\nsomeone scrubbing through files.\n\nMoved out of Damon's own repo 2026-09-20 on his word: *\"that should definitely\nbe a real thing because we need to run that on any brand.\"* The folder already\nexisted here with its data and no code at all; this is the code."}, {"doc": "asset-index (lab — Damon)", "section": "Two halves, deliberately split", "tool": "20-asset-index", "path": "tools/20-asset-index/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/20-asset-index/CLAUDE.md#two-halves-deliberately-split", "text": "Half — What it does — Cost.\n`machine/extract.py` — frames + transcript + measurements, on this Mac — free.\n`prompts/index1-clip-v1-damon.md` — frames + transcript → one JSON record — about $0.002 a clip.\nThe free half never calls a model, so a whole drive of footage can be prepared\nbefore a penny is spent."}, {"doc": "asset-index (lab — Damon)", "section": "Rules", "tool": "20-asset-index", "path": "tools/20-asset-index/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/20-asset-index/CLAUDE.md#rules", "text": "- **Any brand.** `--brand` is required and nothing in the code names one\n  (workspace rule 7). Records land at `runs/asset-index/<brand>/<label>/`,\n  per `runs/README.md`; media stays where it is and is never copied.\n- A clip already done is skipped, so a run can be stopped and resumed.\n- `projects/` and `work/` hold what was already here — Damon's own pulls."}, {"doc": "asset-index (lab — Damon)", "section": "Not done yet", "tool": "20-asset-index", "path": "tools/20-asset-index/CLAUDE.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/20-asset-index/CLAUDE.md#not-done-yet", "text": "The paid half is still run by hand from the prompt — there is no `run.py` that\ndoes both halves and files the record. That is the next piece."}, {"doc": "Asset index", "section": "Asset index", "tool": "20-asset-index", "path": "tools/20-asset-index/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/20-asset-index/README.md", "text": "Turns a folder of footage into records you can search by what is *in* the\nclip, not what the file is called. `IMG_5476.MOV` becomes \"man patting his\nface with a grey towel in a bathroom, profile reveal, usable as demo\".\n\nTwo halves, deliberately split:\n\nHalf — What it does — Cost.\n`extract.py` — frames + transcript + measurements, on this Mac — free.\nthe prompt — frames + transcript -> one JSON record — ~$0.002/clip.\n`content-machine/asset-index/prompts/index1-clip-v1-damon.md` is the prompt."}, {"doc": "Asset index", "section": "Running the free half", "tool": "20-asset-index", "path": "tools/20-asset-index/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/20-asset-index/README.md#running-the-free-half", "text": "Resumable — a clip with a `clip.json` is skipped, so it can be stopped and\nrestarted. Writes one folder per clip: frames at 384px, and `clip.json` with\npath, duration, dimensions, frame timestamps and the transcript."}, {"doc": "Asset index", "section": "Measured (2026-09-09, Rashad folder, 20 clips)", "tool": "20-asset-index", "path": "tools/20-asset-index/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/20-asset-index/README.md#measured-2026-09-09-rashad-folder-20-clips", "text": "- 28.6 min of footage -> 73 frames, 3,545 words transcribed\n- ~6s per clip end to end, including pulling from Drive\n- 3.65 frames per clip average"}, {"doc": "Asset index", "section": "Traps", "tool": "20-asset-index", "path": "tools/20-asset-index/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/20-asset-index/README.md#traps", "text": "- **Whisper lives in `.venv`.** Homebrew Python refuses global installs\n  (PEP 668) and breaking that would risk his system Python.\n- **Frames win over rotation flags.** iPhone footage reports landscape\n  dimensions with a `-90` rotation; the frame is the truth.\n- **`base.en` is the model.** Good enough for indexing speech, fast on CPU.\n  Bigger models cost hours across thousands of clips and change nothing about\n  whether a clip is findable."}, {"doc": "editor-onboarding", "section": "editor-onboarding", "tool": "21-editor-onboarding", "path": "tools/21-editor-onboarding/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/21-editor-onboarding/README.md", "text": "The front door for the people who make our ads — video editors and graphic\ndesigners. They never install anything: they work inside **Higgsfield\nSupercomputer**, which reads the brand folder on Google Drive and these tools\nfrom GitHub. Briefs come to them there; finished work goes back to Drive.\n\n**The page:** daemn.co/onboarding.html —\nthe walkthrough video, the SOP and the prompts, in one place.\n\n`DESK.md` — the other side — what the person onboarding an editor does: folders by link once per brand, one message per editor, what to check. Rendered as `docs/onboarding-desk.html`..\n`SOP.md` — the way we work — once to set up, then the loop every session. What the page says, in Markdown..\n`prompts/01-day-one-v2-damon.md` — pasted once: connects, clones the tools, finds the brand folder, reads the queue.\n`prompts/02-pull-briefs-v2-damon.md` — pasted every session: sync → pick → lay out → review → the asks → the edit → deliver.\n`machine/queue.py` — the brief queue per brand, written to `briefs/QUEUE.md` on Drive from what the folder holds. Runs hourly..\n`walkthrough/cut-list.json` + `cut.py` — the edited walkthrough video — a cut list over the recorded call, rendered with ffmpeg. The recording is not in the repo..\n`context/artifacts.md` — the pages that show this work."}, {"doc": "editor-onboarding", "section": "What the brief now carries", "tool": "21-editor-onboarding", "path": "tools/21-editor-onboarding/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/21-editor-onboarding/README.md#what-the-brief-now-carries", "text": "The handoff every editor receives ends with **`## THE ASKS`** — scroll\nstoppers, headlines, variations, extra scenes, formats, styles — each specified\nor refused with a reason. The list and its defaults:\n`../05-ai-video-production/ASKS-SPEC.md`.\nThe Pull-briefs prompt reads it; when a handoff predates it, the prompt applies\nthe default menu."}, {"doc": "editor-onboarding", "section": "Two homes, on purpose", "tool": "21-editor-onboarding", "path": "tools/21-editor-onboarding/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/21-editor-onboarding/README.md#two-homes-on-purpose", "text": "The **tools** live here, in the public repo. The **briefs** — runs, packs, brand\nmaterial — never do: they live in the private workspace (`AI_WORKSPACE`,\ndefault `~/Projects/ai-workspace`) and on Google Drive, which is where editors\nread them. Nothing in this folder reads a brief from the repo."}, {"doc": "editor-onboarding", "section": "Keeping Drive and the tools in step", "tool": "21-editor-onboarding", "path": "tools/21-editor-onboarding/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/21-editor-onboarding/README.md#keeping-drive-and-the-tools-in-step", "text": "Three directions, all automatic:\n\n- **Workspace → Drive.** `queue.py ship --auto` (hourly) finds every run in\n  the private workspace whose pack cleared its gates (`deliverable/EDITOR-PACK.md`\n  exists), zips it into the brand's `briefs/ai-video-production/` on Drive —\n  once; a brief already on the queue is never duplicated or overwritten.\n\n- **Tools → editors.** The Pull-briefs prompt pulls the repo before anything\n  else, every session. A change committed here reaches every editor the next\n  time they sit down.\n- **Drive → queue.** `queue.py build --all` runs hourly on the owner's Mac\n  and rewrites each brand's `briefs/QUEUE.md` from the folder: a package makes\n  a brief **open**, a claim file makes it **claimed**, files in\n  `delivered/<brief>/` make it **delivered**. Bounty and due dates are the\n  owner's, set with `queue.py set`, and survive every rebuild."}, {"doc": "editor-onboarding", "section": "Run it", "tool": "21-editor-onboarding", "path": "tools/21-editor-onboarding/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/21-editor-onboarding/README.md#run-it", "text": "`DRIVE_ACCOUNT=<google account>` picks the Drive mount; unset, the first one\nfound is used."}, {"doc": "How creative marketers work with us — the SOP", "section": "How creative marketers work with us — the SOP", "tool": "21-editor-onboarding", "path": "tools/21-editor-onboarding/SOP.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/21-editor-onboarding/SOP.md", "text": "You never install anything and never touch code. You work inside **Higgsfield\nSupercomputer**, which reads our Google Drive and our tools directly. Briefs\ncome to you there; finished work goes back to Drive from there.\n\nThe walkthrough video and this page, together, are the whole onboarding:\n**daemn.co/onboarding.html**.\n\n---"}, {"doc": "How creative marketers work with us — the SOP", "section": "Once — set up (ten minutes)", "tool": "21-editor-onboarding", "path": "tools/21-editor-onboarding/SOP.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/21-editor-onboarding/SOP.md#once--set-up-ten-minutes", "text": "1. **Get the brand folder link.** One Google Drive link from the owner —\n   no invitation, no account to be added to; the folder opens for anyone\n   with the link. A Higgsfield account of your own is enough (the owner's\n   workspace only matters if you are using its credits).\n2. **In Higgsfield Supercomputer → Connectors → Explore**, connect **Google\n   Drive** and **GitHub**. Sign in to each when asked.\n3. **Put the brand folder in your own Drive.** Open the link, then in Google\n   Drive: right-click the folder → *Organize* → *Add shortcut* → into a\n   folder you make in *My Drive*. Supercomputer can only see what is in your\n   own Drive.\n4. **New chat in Supercomputer → the `+` button → Connectors** — check Google\n   Drive and GitHub are both ticked for that chat.\n5. **Paste the Day-one prompt** (`prompts/01-day-one-v2-damon.md`) with the\n   brand's name and the folder link filled in. It clones the tools, finds the brand folder, reads\n   the queue and tells you what is open. When it ends with *\"Set up for\n   <brand> — N briefs open\"*, you are done."}, {"doc": "How creative marketers work with us — the SOP", "section": "Every session — the loop", "tool": "21-editor-onboarding", "path": "tools/21-editor-onboarding/SOP.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/21-editor-onboarding/SOP.md#every-session--the-loop", "text": "1. **New chat → paste the Pull-briefs prompt** (`prompts/02-pull-briefs-v2-damon.md`)\n   with the brand's name. Name a brief if you have one; otherwise it takes\n   the newest open one.\n2. **It syncs first** — pulls the tools for updates, reads the live queue —\n   then claims the brief and downloads its package.\n3. **It lays the brief out**: the scenes with their clips and voices (or the\n   image drafts), the intended cut, what is locked, the known issues, the loop.\n   Read it. Watch every clip.\n4. **Review** — it walks the checklist with you: same person, same wardrobe,\n   same product, slop, sound, words, anything odd. You get a table: KEEP /\n   FIX AT CUT / REROLL. Then three to five ideas. **You decide** what gets\n   rerolled and which ideas to take.\n5. **The asks** — the extras the brief wants: scroll stoppers, headlines,\n   variations, extra scenes, formats, styles. It makes them in Higgsfield\n   from the same cast and product references. You approve each one.\n6. **The edit** — for video, it packs everything into `<brief>--edit` with a\n   cut sheet; you take that into Premiere or CapCut. For statics, the finals\n   go into `<brief>--finals`.\n7. **Deliver** — finished files to `briefs/delivered/<brief>/` on Drive,\n   named `<brief>--<what>--v1`, with a `DELIVERED.md` saying what was made,\n   what was rerolled, and what is still open. The queue updates itself from\n   the folder within the hour."}, {"doc": "How creative marketers work with us — the SOP", "section": "The queue", "tool": "21-editor-onboarding", "path": "tools/21-editor-onboarding/SOP.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/21-editor-onboarding/SOP.md#the-queue", "text": "`briefs/QUEUE.md` in each brand folder on Drive. One row per brief: type,\nstatus, who, bounty, due, package. It is rebuilt every hour from the folder —\n**open** because the package is there, **claimed** because your claim file is\nin `briefs/claims/`, **delivered** because your files are in\n`briefs/delivered/<brief>/`. Nobody edits the table by hand; the owner sets\nbounty and due dates."}, {"doc": "How creative marketers work with us — the SOP", "section": "The rules that never move", "tool": "21-editor-onboarding", "path": "tools/21-editor-onboarding/SOP.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/21-editor-onboarding/SOP.md#the-rules-that-never-move", "text": "- **Nothing invented.** Not a product detail, not a claim, not a person. If\n  the brief does not say it, the answer is \"not in the brief\" — ask.\n- **Every face and every product carries its reference.** A person or product\n  generated without their reference is a reroll, not a delivery.\n- **One ratio.** Everything is made and cut at 9:16, with faces, the product\n  and every word inside the centred 4:5 crop — the middle 70% of the frame.\n  Nothing is made at 4:5 or 1:1. A swipe that arrives 4:5 or 1:1 is rebuilt at\n  9:16 the same way.\n- **Never write into the source package.** Deliveries go to `delivered/`.\n- **Sync first, every session.** A brief made from yesterday's tools or a\n  stale queue is the one that gets sent back.\n- **When a door fails — Drive, GitHub, Higgsfield — say which one, in one\n  line.** Do not work around it silently."}, {"doc": "How creative marketers work with us — the SOP", "section": "When it goes wrong", "tool": "21-editor-onboarding", "path": "tools/21-editor-onboarding/SOP.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/21-editor-onboarding/SOP.md#when-it-goes-wrong", "text": "what you see — what it means — what to do.\n\"Something went wrong\" opening the brand folder — Supercomputer picked the wrong folder or cached an old one — Paste the folder's Drive link directly into the chat.\nNo credits / \"no chats\" — Wrong Higgsfield workspace, or the free credits are used — Switch workspace (top-left) and refresh, or ask the owner.\nIt found the wrong `briefs` folder — Two folders share the name — Give it the path or the link; it must not guess.\nThe queue shows a brief you already delivered as open — The files are not in `delivered/<brief>/` under that exact brief name — Check the folder name matches the queue's Brief column.\n\"Pull my GitHub repository\" says no updates but the SOP changed — The clone is stale — Say \"clone https://github.com/daemnapps/prizm-labs again, fresh\"."}, {"doc": "How creative marketers work with us — the SOP", "section": "Coming next (not yet — do not wait for it)", "tool": "21-editor-onboarding", "path": "tools/21-editor-onboarding/SOP.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/21-editor-onboarding/SOP.md#coming-next-not-yet--do-not-wait-for-it", "text": "- AI editing inside the tools: the cut made from the cut sheet without a\n  timeline. Until it lands, the edit is yours in Premiere or CapCut.\n- A brief-download skill in Higgsfield, so \"pull briefs\" is one word."}, {"doc": "swipe-library", "section": "swipe-library", "tool": "22-swipe-library", "path": "tools/22-swipe-library/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/22-swipe-library/README.md", "text": "One front door to everything that has been swiped — organic formats, the\ntorn-down videos, one feed per customer type, every competitor's live ads —\nwritten where the team already looks, and a public page for the half that\ncan be public.\n\n**The page:** daemn.co/swipes/\n\n`machine/library.py` — reads the private workspace and writes: `SWIPE LIBRARY.md` at the root of the shared drive, a `FEED.md` in every feed's folder on Drive, and `docs/swipes/`.\n`context/artifacts.md` — the pages that show this work."}, {"doc": "swipe-library", "section": "What is in the library, and where each half lives", "tool": "22-swipe-library", "path": "tools/22-swipe-library/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/22-swipe-library/README.md#what-is-in-the-library-and-where-each-half-lives", "text": "What — Public (this repo) — Team (Drive, read through Higgsfield).\n**Organic formats** — the structure under posts that worked — `tools/03-organic-swipe-pack/formats/` · the zip on the site — —.\n**Swipe videos** — every post torn down, filed under its format — the rebuild sheets, text only — `lab/damon/swipe-organic/records/swipe-videos/<code>/` — the video, the sheet, the beat strip.\n**My Feeds** — one feed per sub-avatar: what that person watches — never — `lab/damon/swipe-organic/records/avatar-feeds/<feed>/FEED.md` — every kept post, linked.\n**Paid sweeps** — every competitor's live ads, blocked by angle — `tools/04-paid-ad-swipe-pack/angle-shapes/` — the recurring shapes, brands described not named — `lab/damon/swipe-paid/<brand>/angles/NN_…/` — video, photos, page captures.\n**The index** — this README — `SWIPE LIBRARY.md` — counts, every feed, every code by format, every sweep.\nNothing private crosses the line. The public page carries formats, angle\nshapes and counts; a brand, a creator or a competitor never appears on it.\nThe build refuses nothing — it simply never reads those into the public half."}, {"doc": "swipe-library", "section": "Run it", "tool": "22-swipe-library", "path": "tools/22-swipe-library/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/22-swipe-library/README.md#run-it", "text": "The two packs the page links to are built by their own tools in the private\nworkspace (`swipe-organic/bundle.py --public`, `swipe-organic/paidpack.py`);\nthis tool only indexes what they and the feeds produce. `AI_WORKSPACE` and\n`DRIVE_ACCOUNT` pick the two planes."}, {"doc": "swipe-library", "section": "For editors, in Higgsfield", "tool": "22-swipe-library", "path": "tools/22-swipe-library/README.md", "url": "https://github.com/daemnapps/prizm-labs/blob/main/tools/22-swipe-library/README.md#for-editors-in-higgsfield", "text": "`../21-editor-onboarding/prompts/03-swipe-library-v1-damon.md` — \"open the\nswipe library for <brand>\": reads the index and the brand's feeds, shows the\nposts, and hands any one of them to the teardown."}]}