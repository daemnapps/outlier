The creator may be anyone — any gender, any age, any brand's roster. Write
about them in THEIR pronouns, taken from their own bio and content; the
machinery assumes nothing.

Today is: {today}
Brand: {brand_name}
Creator handle: {handle}

Here is the spec this profile must follow — its template IS the shape of
your output: {profile_spec}

Here is the creator's record — who they are, every post we hold, their
captions and the comments they drew: {creator_record}

Here are the creator's teardown runs — the objective reads of their own
videos, and the briefs they became: {teardown_material}

Here is the creator's profile as it stands today: {existing_profile}

---

A teardown run of the creator's content just finished. Rebuild their
profile so it says what the machine now knows about them.

**Every example inside the spec or this prompt is illustrative only.**
Nothing from an example may appear in the profile. Every claim comes from
the creator's record and runs — the inputs above and nowhere else. A detail the
inputs do not contain does not exist.

**The spec's template is the output, exactly.** Same sections, same order,
same headings, and every rule the spec states binds here. Where the spec and
this prompt disagree, the spec wins — it is the brand's ruling on what a
profile is.

**This is a refresh, not a rewrite from a blank page.** What the existing
profile states and the records still support, keep — including its wording
where that wording is already verbatim-sourced. What the new material
answers, update. An OPEN item the new material resolves gets resolved, with
its source; an OPEN item nothing answers stays OPEN. Never drop a sourced
claim just to say the same thing differently.

**Verbatim beats paraphrase.** The creator's language is quoted from their
own posts, captions and comment replies, in quotation marks. A paraphrase where a quote
exists is a defect; an invented detail is a worse one. Unknown means OPEN —
never a guess dressed as a fact.

**No terms, rates, or deal status.** That is operational and it churns; the
roster owns it, not this profile. If the existing profile still carries any
of it, dropping it is correct, not a loss.

Your entire response is the profile document, starting at its `# ` title
line. No preamble, no commentary, no report of what changed.
