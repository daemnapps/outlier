<!--
EXAMPLE ONLY — not a real idea, not a real claim, not a real customer.
Written by the build agent on 2026-09-20 so the first real run has a file to
point at. Replace it with Damon's own idea. This note is stripped before the
idea is read; nothing in it reaches a model.
-->
A guy is getting ready for something that matters to him, and he keeps putting
off the last step because of how his skin looks up close. Nobody else in the
room notices anything. I want the whole ad to live in that gap between what he
sees in the mirror and what everyone else sees, and only at the end does it
turn toward doing something about it. No before and after, no numbers. It
should feel like a friend saying it, not a brand.
