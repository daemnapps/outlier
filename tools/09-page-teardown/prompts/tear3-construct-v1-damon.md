**Everything named in this prompt as an example is an example of a SHAPE, not
of content.** No brand, product, person, price or phrase in this file belongs in
your answer. Your answer's content comes only from the input below.

Here is the record of one landing page — its SECTIONS AS READ and its SOURCE
DEFECTS. The site's page furniture was taken out by the code before you; only
its item numbers are left: {record}

Turn the record into the page's **CONSTRUCT** — the persuasion mechanics with
the seller, the product, the product's whole category AND what the page was
actually about all stripped out, so that any brand can later be injected into
it. You are not writing a page, and you are not improving this one.

**What transfers is HOW it argued, never WHAT it argued.** The order of the
moves, the job each one does on the reader, what has to be true for that job to
land, and where the weight sits.

- *carries across* — "counts the alternatives that failed before the winner is
  named, so the winner arrives already compared"
- *does NOT carry across* — the same line with the source's own subject in it

If a line of your answer would stop being true for a page about something
completely different, it is subject matter and does not belong here.

**No names. No figures.** No seller, product, range, person, publication,
ingredient, trademarked term or named mechanism from the source appears
anywhere in your answer. **No money figure appears at all** — not a price, not a
saving, not a "worth" or "value" amount, not a percentage off — and none of the
source's own counts, ratings or durations. Where the source used a figure, the
construct carries a slot for one and says what kind of figure fills it: `[SLOT:
the anchor price the offer is set against]`, never the number. A number from
the source written into the construct is a fact about another company's offer
pretending to be structure.

**What the page is made of is a fact about the page, not a constraint on the
next one.** No section counts, no word counts, no screen positions, no "above
the fold" anywhere in your answer.

**Furniture never becomes a move.** Nothing every page on the site arrives
in — its menus, its identity row, its site-wide strips, its consent notices,
its policy links, its legal lines — is a job done on the reader by THIS page. Do
not name those parts, do not describe them, and do not give them a slot. You
were not shown their words; do not guess at them.

**A source defect is never carried.** Where the record marks a quote `[D1]`…,
you are looking at damage, not a choice. Do not abstract a rule from it, do not
quote its broken form, and never write a template tag as code. If what it was
meant to say is plain, work from that; if it is `unclear`, work without it.

**Never leave a hole.** If the record does not support a part, say so in one
plain sentence under that heading — never a placeholder, never a note to fill
in later.

Your answer has exactly these labelled parts, under exactly these headings, in
this order:

# CONSTRUCT

**THE MOVES** — numbered, in the page's order. Each move names the record's
section or sections it came from (`from S3–S4`). For each:

1. **The job it does on the reader.** Stated so it could be performed for three
   unrelated kinds of product without changing what it accomplishes.
2. **What it needs to work.** The conditions the move depends on: that it comes
   before the product is named; that it is shorter than what follows; that
   every item in it is a real, checkable specific.
3. **Its slots** — `[SLOT: name]`, each named by what it needs, with one line
   on what would legitimately fill it.

**THE SEQUENCE LOGIC** — why these moves in this order: what the headline buys,
where the tension peaks, what the payoff spends it on, why the ask lands where
it does and how many times. If a move were removed, this says what breaks.

**PROPORTION, RELATIVELY** — how the page distributed its weight, as shares in
words ("roughly a third elapses before the winner is named; the asks take a
tenth between them"). Never in sections, screens, words or pixels.

**THE VOICE CONSTRUCT** — the record's voice markers restated as followable
rules: sentence length, punctuation habits, vocabulary register, what this
writer never does. Rules only — none of the source's own phrases.

**LOAD-BEARING** — the two or three things this argument cannot survive
losing, and what each carries. A later step may substitute into these; it may
not cut them.

**WHAT IS PAGE-BOUND** — the devices the source used that only work on a page
(a running timer, a byline with a credential, a row of stars, a button that
follows the reader down, a drag-to-compare picture), each with the JOB it was
doing, so a later step can do the same job honestly or decide not to. Name the
device in plain generic words and the job it did — never the source's own
wording for it.

**LEFT OUT:** one line — the furniture items (`F1`…) and defects (`D1`…) you set
aside, by their numbers only, never their words.

**The test for this whole answer — three parts:**

1. Someone handed it, with no access to the source, could write this page for
   three unrelated kinds of product — three different objects that all make the
   same argument.
2. **They could not tell you what the source was selling, who was selling it,
   what it cost, or what it was about.**
3. **They could not tell you what the rest of the seller's site looks like**,
   and nothing the saved page got wrong is repeated here.
