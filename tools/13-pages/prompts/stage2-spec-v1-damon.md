Today is: {today}
Here is what triage decided: {triage}
Here is the record of the source page: {record}

Turn the record into a **brand-free, category-free construct** — the persuasion mechanics of this page with the brand, the product category AND the page's own furniture stripped out.

**What the page is made of is a fact about the page, not a constraint on us.** A countdown bar, a "4 min read" tag, a dermatologist byline, a star row, a sticky button — all of it is how that particular page carried the argument. The argument is what transfers. The furniture does not, and if it comes back it comes back because a later stage chose it on its own merits.

So: **no product names, no category words (foundation, scrub, serum), no brand, no prices, no discounts** anywhere in this document. If you catch yourself writing one, you are describing the source instead of abstracting it.

Give me:

**THE MOVES** — numbered, in the page's order. For each:

1. **The job it does on the reader.** Stated so it could be performed for a supplement, a piece of software or a skincare product without changing what it accomplishes. "Counts the failures before naming the winner, so the winner arrives pre-compared" — never "lists six foundations."
2. **What it needs to work.** The conditions the move depends on: that it comes before the product is named; that it is shorter than what follows; that every item in it is a real number. These are the real constraints and they survive any category.
3. **Its slots** — `[SLOT: name]`, each named by what it needs, with one line on what would legitimately fill it.

**THE SEQUENCE LOGIC** — why these moves in this order: what the headline buys, where the tension peaks, what the payoff spends it on, why the ask lands where it does and how many times. If a move were removed, this says what breaks.

**PROPORTION, RELATIVELY** — how the page distributed its weight, as shares. "Roughly a third of the page elapses before the winner is named; the asks take a tenth between them." Never in words or pixels.

**THE VOICE CONSTRUCT** — the voice markers from the record restated as followable rules: sentence length, punctuation habits, vocabulary register, what this writer never does. Binding on every later stage.

**LOAD-BEARING** — the two or three things this argument cannot survive losing, and what each carries. A later stage may substitute into these; it may not cut them.

**WHAT IS PAGE-BOUND** — the things the source did that only work as page furniture (a timer, a byline credential, a star row, a sticky button, a "most read" tag) and what each was accomplishing, so a later stage can achieve the same end honestly or decide not to. This is what stops the next stage from either copying a device we cannot truthfully use, or silently dropping the job that device was doing.

**The test for this whole document:** someone handed it, with no access to the source, should be able to write this page for a supplement, a skincare product and a piece of software — three different objects that all make the same argument. If it could only produce one, it is still a record of the source, not a construct.
