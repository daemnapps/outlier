# proposed/ — prompts written for the owner to read, NOT in the run

Nothing in this folder runs. The chain picks the highest-numbered prompt in a
stage's own folder (`machine/chain.py`, `PROMPT_HOMES`); this folder is not one
of those homes, so a file here can never become live by accident.

| File | What it changes | To make it live |
|---|---|---|

`stage1c-doctrine-v3-damon.md` went live on 2026-09-20 on Damon's word — it now sits in `stage-1c-doctrine/`, v2 is in that folder's `archive/`.
