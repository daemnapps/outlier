# Artifacts — marketing-calendar

One artifact per subject; republish over the same link, never a second copy.
Read this file before creating any artifact for this component.

| Artifact | URL | Subject | Mirror |
|---|---|---|---|
| The Marketing Calendar | https://claude.ai/code/artifact/5906f59a-3216-4861-ada9-073c1e38232a | The calendar only — the nine layers, the words in the chain, and the board a human corrects a month on. Deliberately does NOT cover copy, design or images: those are separate things and belong on their own pages (Damon, 2026-09-13). **Damon's working copy is the LIVE board at `http://127.0.0.1:8788`** (`machine/serve.py`) — that page is the tool; the artifact is the shareable explanation of it. | `README.md` · page source `PAGE.html` |

The review board itself is deliberately **not** an artifact. It writes to
disk, triggers runs, and is one person's workbench at a time; an artifact is
a shareable page. The board stays local, the artifact explains it.
